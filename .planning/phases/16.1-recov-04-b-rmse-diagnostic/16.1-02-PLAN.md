---
phase: 16.1-recov-04-b-rmse-diagnostic
plan: 02
type: execute
wave: 2
depends_on: ["16.1-01"]
files_modified:
  # REQUIREMENTS.md is modified only on outcomes C.1 (full flip) and C.2
  # (annotation-only); under C.3 (RECOV-04 still failing) REQUIREMENTS.md
  # is NOT modified — see Task 3 outcome branches and the conditional
  # verify-step-8 scope-creep guard.
  - benchmarks/runners/task_bilinear.py
  - tests/test_task_bilinear_benchmark.py
  - .planning/REQUIREMENTS.md
  - cluster/submit_phase16_1.sh
  - cluster/run_phase16_1_acceptance.slurm
  - .planning/phases/16.1-recov-04-b-rmse-diagnostic/16.1-02-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "`_BILINEAR_INIT_SCALE` in `benchmarks/runners/task_bilinear.py` is updated to the value chosen by Plan 16.1-01, with a docstring referencing the diagnostic artifact."
    - "The retry policy (`_BILINEAR_INIT_SCALE_RETRY = 0.001`) is replaced by a 'halve once on NaN at step 0' rule (`init_scale * 0.5`) regardless of starting init_scale, so the inversion problem under raised init_scale is resolved."
    - "The Phase 16.1 cluster acceptance-gate sbatch infrastructure exists (reuses Phase 16 scaffolding) and the 10-seed acceptance run is dispatched with the chosen knob."
    - "All 4 RECOV gates (03, 04, 05, 06) pass on >=10 seeds in the cluster run, AND RECOV-07 shrinkage_nonnull means land in [0.05, 0.7] range (NOT pinned at ~0.008 indicating stuck-at-init, NOT at ~1.0 indicating non-identifiability). Threshold sourced from REQUIREMENTS.md line 59 (`std_post / std_prior <= 0.7`)."
    - "REQUIREMENTS.md RECOV-04 is flipped to `[x]` (Complete) ONLY after the cluster run passes; if it fails, RECOV-04 stays `[ ]` and the SUMMARY documents the escalation path."
    - "No regression: RECOV-03/05/06/08 thresholds remain unchanged in REQUIREMENTS.md; the cluster run continues to pass them."
  artifacts:
    - path: "benchmarks/runners/task_bilinear.py"
      provides: "Updated _BILINEAR_INIT_SCALE constant + replaced retry policy"
      contains: "_BILINEAR_INIT_SCALE ="
    - path: "cluster/submit_phase16_1.sh"
      provides: "Phase 16.1 cluster submit wrapper (reuses Phase 16 push job)"
      min_lines: 40
    - path: "cluster/run_phase16_1_acceptance.slurm"
      provides: "Phase 16.1 sbatch acceptance-gate run (10 seeds × n_svi_steps × chosen init_scale)"
      min_lines: 100
    - path: ".planning/phases/16.1-recov-04-b-rmse-diagnostic/16.1-02-SUMMARY.md"
      provides: "Final phase summary with cluster job ID, RECOV gate outcomes, and milestone-closure status"
      contains: "Cluster job ID"
    - path: ".planning/REQUIREMENTS.md"
      provides: "RECOV-04 status flip + threshold-justification note"
      contains: "RECOV-04"
  key_links:
    - from: ".planning/phases/16.1-recov-04-b-rmse-diagnostic/16.1-01-SUMMARY.md"
      to: "benchmarks/runners/task_bilinear.py:_BILINEAR_INIT_SCALE"
      via: "Chosen init_scale value consumed verbatim from 16.1-01 SUMMARY"
      pattern: "_BILINEAR_INIT_SCALE\\s*=\\s*0\\.\\d+"
    - from: "cluster/submit_phase16_1.sh"
      to: "cluster/run_phase16_1_acceptance.slurm"
      via: "sbatch dependency chain"
      pattern: "sbatch.*run_phase16_1_acceptance"
    - from: "cluster results branch (results/phase16_1-acceptance-*)"
      to: ".planning/phases/16.1-recov-04-b-rmse-diagnostic/16.1-02-SUMMARY.md"
      via: "Cluster autopush via slurm-autopush skill (existing infra; same as Phase 16)"
      pattern: "results/phase16_1-acceptance-"
---

<objective>
Apply the chosen `_BILINEAR_INIT_SCALE` from Plan 16.1-01 to the runner, replace
the now-inverted retry policy with a "halve once on NaN at step 0" rule,
optionally lift `n_svi_steps` to 1500 if 16.1-01 flagged the convergence as
"trending" (NOT plateaued), and re-run the 10-seed acceptance gate on the
Monash M3 cluster via reused Phase 16 sbatch infrastructure. On pass, flip
RECOV-04 to Complete in REQUIREMENTS.md and write the phase-closing SUMMARY.

Purpose: Closes the v0.3.0 milestone-blocking RECOV-04 acceptance failure with
a versioned, defensible single-knob fix backed by the Plan 16.1-01 diagnostic
evidence.

Output: Updated runner constant + retry policy, Phase 16.1 cluster scripts,
cluster job submitted, post-run SUMMARY documenting outcomes and gate status.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16.1-recov-04-b-rmse-diagnostic/16.1-RESEARCH.md
@.planning/phases/16.1-recov-04-b-rmse-diagnostic/16.1-01-SUMMARY.md
@benchmarks/runners/task_bilinear.py
@tests/test_task_bilinear_benchmark.py
@cluster/run_phase16_acceptance.slurm
@cluster/submit_phase16.sh
@cluster/99_push_phase16_results.slurm
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply chosen init_scale, replace retry policy, optionally lift n_svi_steps</name>
  <files>
    benchmarks/runners/task_bilinear.py
    tests/test_task_bilinear_benchmark.py
  </files>
  <action>
PRECONDITION: Read `.planning/phases/16.1-recov-04-b-rmse-diagnostic/16.1-01-SUMMARY.md`
FIRST. Extract two values:
  - `chosen_init_scale`: the new `_BILINEAR_INIT_SCALE` value (e.g., 0.05, 0.1).
  - `convergence_caveat`: either "plateaued" (n_svi_steps=500 sufficient) or
    "trending" (lift to 1500).

If `chosen_init_scale == NONE` (escalation): STOP. Do NOT proceed to any
sub-step below. Instead, write `16.1-02-SUMMARY.md` documenting that Plan
16.1-01 escalated, no cluster run was dispatched, and the recommended next
step is either Experiment 3 (2D mini-grid; would be Plan 16.1-03) or Phase
16.2 (deeper guide investigation). Mark this plan's status as "escalated;
deferred". This branch is the SAFE failure path.

Otherwise (winner found), perform these edits:

A. **`benchmarks/runners/task_bilinear.py` line 78-79 region.** Replace the
   block:
   ```python
   _BILINEAR_INIT_SCALE = 0.005
   _BILINEAR_INIT_SCALE_RETRY = 0.001
   ```
   With:
   ```python
   # Phase 16.1 (2026-04-24): raised from 0.005 -> {chosen_init_scale}.
   # Plan 16.1-01 diagnostic on seed 42 showed init_scale=0.005 placed the
   # B-posterior at 0.5% of the prior std (B_PRIOR_VARIANCE=1.0), causing
   # AutoNormal to stick at the init basin (B-RMSE 0.342 across 10 cluster
   # seeds in job 54933838 vs 0.20 RECOV-04 threshold). The new value moves
   # the init to {pct}% of the prior std, escapes the basin, and reproduces
   # SPM12-style VL initialization on the order of sigma_prior. See
   # 16.1-01-SUMMARY.md for the chosen-knob rationale and the sweep table
   # (full diagnostic JSON/MD on the autopushed cluster results branch
   # recorded in the SUMMARY's `results_branch` frontmatter field).
   _BILINEAR_INIT_SCALE = {chosen_init_scale}
   ```
   (Substitute `{chosen_init_scale}` and `{pct}` from the SUMMARY.)
   The `_BILINEAR_INIT_SCALE_RETRY = 0.001` constant is REMOVED entirely.

B. **`benchmarks/runners/task_bilinear.py` `_fit_bilinear_with_retry`
   function (around line 494-534).** Replace the body so the retry uses
   `_BILINEAR_INIT_SCALE * 0.5` rather than the deleted
   `_BILINEAR_INIT_SCALE_RETRY` constant. The new retry behavior:
     - If NaN at step 0 -> retry once at `init_scale = _BILINEAR_INIT_SCALE * 0.5`.
     - Print message: `"NaN at step 0 with init_scale={start}; retrying once at halved init_scale={start * 0.5}"`.
     - Return `(posterior, elapsed, init_scale_used)` where `init_scale_used`
       is either `_BILINEAR_INIT_SCALE` or `_BILINEAR_INIT_SCALE * 0.5`.
   Update the docstring to reflect the new policy.

C. **`benchmarks/runners/task_bilinear.py` `metadata` dict (around line 818-820).**
   The references to `_BILINEAR_INIT_SCALE_RETRY` must be replaced. Change:
   ```python
   "init_scale_bilinear": _BILINEAR_INIT_SCALE,
   "init_scale_bilinear_retry": _BILINEAR_INIT_SCALE_RETRY,
   "init_scale_bilinear_n_retries": int(
       sum(
           1 for v in init_scale_used_bilinear_list
           if v == _BILINEAR_INIT_SCALE_RETRY
       )
   ),
   ```
   to:
   ```python
   "init_scale_bilinear": _BILINEAR_INIT_SCALE,
   "init_scale_bilinear_retry": _BILINEAR_INIT_SCALE * 0.5,
   "init_scale_bilinear_n_retries": int(
       sum(
           1 for v in init_scale_used_bilinear_list
           if v != _BILINEAR_INIT_SCALE
       )
   ),
   ```

D. **`benchmarks/runners/task_bilinear.py` docstring around line 550** ("Guide:
   AutoNormal with `init_scale=0.005`..."). Update to cite the new value AND
   reference the Phase 16.1 diagnostic.

E. **(CONDITIONAL) `tests/test_task_bilinear_benchmark.py:472`.** If
   16.1-01-SUMMARY says `convergence_caveat: trending`, REMOVE the line
   `config.n_svi_steps = 500` (let `full_config` provide its 1500 default per
   Plan 16-01 L4). Update the convergence-caveat comment block at lines
   452-460 to reflect the Phase 16.1 update. If `convergence_caveat: plateaued`,
   leave the override at 500 and add a one-line comment:
   `# Phase 16.1: 500 confirmed sufficient under _BILINEAR_INIT_SCALE={chosen_init_scale} (see 16.1-01-SUMMARY.md).`

F. **No other edits.** DO NOT change:
   - `B_PRIOR_VARIANCE` in `task_dcm_model.py` (D1 lock).
   - `init_scale=0.01` for the linear baseline at line 741 (linear baseline is fine; only bilinear fix needed).
   - Modulator schedule (`_EPOCH_TIMES` etc.) — frozen.
   - `_B_10`, `_B_21` magnitudes — frozen.
   - `create_guide` API in `src/pyro_dcm/models/guides.py`.
   - The seed-pool `_MAX_POOL_MULTIPLIER` retry path (orthogonal to init_scale fix).

G. **Run the fast test suite locally** to confirm no regression:
   ```
   pytest tests/test_task_bilinear_benchmark.py -m "not slow" -x
   ```
   This catches typos / import breaks before cluster spend. The slow
   acceptance test runs only on cluster.
  </action>
  <verify>
1. `grep -n "_BILINEAR_INIT_SCALE = " benchmarks/runners/task_bilinear.py` shows ONE assignment with the chosen value (no `_RETRY` constant).
2. `grep -c "_BILINEAR_INIT_SCALE_RETRY" benchmarks/runners/task_bilinear.py` returns 0 (constant fully removed).
3. `grep -n "init_scale=.*\* 0\.5" benchmarks/runners/task_bilinear.py` shows the retry uses runtime `* 0.5`.
4. `pytest tests/test_task_bilinear_benchmark.py -m "not slow" -x` passes.
5. `python -c "from benchmarks.runners.task_bilinear import _BILINEAR_INIT_SCALE; print(_BILINEAR_INIT_SCALE)"` prints the chosen value.
  </verify>
  <done>
- `_BILINEAR_INIT_SCALE` updated to chosen value with diagnostic-citing docstring.
- `_BILINEAR_INIT_SCALE_RETRY` constant removed; retry policy now `init_scale * 0.5` runtime.
- Metadata dict updated to compute retry tracking from non-default usage.
- Test override at `tests/test_task_bilinear_benchmark.py:472` adjusted per 16.1-01 convergence verdict.
- Fast tests green locally.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 16.1 cluster sbatch scripts (reuse Phase 16 scaffolding) and dispatch the 10-seed acceptance run</name>
  <files>
    cluster/submit_phase16_1.sh
    cluster/run_phase16_1_acceptance.slurm
  </files>
  <action>
**PRECONDITION CHECK (FIRST ACTION):** Read
`.planning/phases/16.1-recov-04-b-rmse-diagnostic/16.1-01-SUMMARY.md`. If
`chosen_init_scale == NULL` or `status == escalated` or
`status == harness-mismatch-triage-needed`:

  1. SKIP this task (Task 2) AND Task 3 entirely. Do NOT create cluster
     scripts. Do NOT dispatch any cluster run.
  2. Write a brief `16.1-02-SUMMARY.md` with `status: blocked-by-16.1-01-escalation`
     and a copy of the escalation reason from `16.1-01-SUMMARY.md` (e.g.
     "harness-mismatch-triage-needed", "escalation: no init_scale won the
     diagnostic", etc.).
  3. STOP Phase 16.1. The orchestrator/user will decide whether to spawn
     Phase 16.2 (deeper guide investigation, 2D mini-grid, or cluster runner
     triage) based on the SUMMARY.

Otherwise (Plan 16.1-01 produced a winner), proceed with the cluster
infrastructure work below.

Reuse the Phase 16 cluster harness with minimal renaming. The Phase 16 push
job (`cluster/99_push_phase16_results.slurm`) is reused unchanged because the
slurm-autopush skill (extracted globally per planning context) handles result
branch naming; only the acceptance + submit-wrapper need Phase-16.1 variants.

A. **`cluster/run_phase16_1_acceptance.slurm`** (NEW). Copy
   `cluster/run_phase16_acceptance.slurm` verbatim, then change:
     - `#SBATCH --job-name=phase16_acceptance` -> `phase16_1_acceptance`
     - `#SBATCH --output=cluster/logs/phase16_%j.out` -> `phase16_1_%j.out`
     - `#SBATCH --error=cluster/logs/phase16_%j.err` -> `phase16_1_%j.err`
     - The header comment block: replace `# Phase 16 v0.3.0 -- Bilinear DCM Acceptance Gate (CPU)` with `# Phase 16.1 v0.3.0 -- RECOV-04 Re-Run with Updated init_scale (CPU)`.
     - Update the `# Expected wall time:` comment to reflect 16.1-01's chosen
       `n_svi_steps` (~80-150 min at 500 steps; ~150-250 min at 1500 steps).
     - The `pytest` invocation line is identical (it picks up the new
       `_BILINEAR_INIT_SCALE` automatically because the test imports from the
       runner).
     - Result file paths: `phase16_acceptance_${SLURM_JOB_ID}.{json,md}` ->
       `phase16_1_acceptance_${SLURM_JOB_ID}.{json,md}`.
     - Pytest log: `pytest_phase16_${SLURM_JOB_ID}.log` ->
       `pytest_phase16_1_${SLURM_JOB_ID}.log`.

B. **`cluster/submit_phase16_1.sh`** (NEW). Copy `cluster/submit_phase16.sh`
   verbatim, then change:
     - The script header comment to reference Phase 16.1.
     - `cluster/run_phase16_acceptance.slurm` -> `cluster/run_phase16_1_acceptance.slurm`.
     - The push-job line keeps `cluster/99_push_phase16_results.slurm` (the
       autopush script is generic and writes a results branch with a
       timestamp; rename of the push script not required for Phase 16.1).
     - Job-ID variable names (`JOB1`, `PUSH_JOB`) and echo messages updated to
       reference Phase 16.1.

C. **`chmod +x cluster/submit_phase16_1.sh cluster/run_phase16_1_acceptance.slurm`**
   to keep the existing executable-bit convention (Phase 16 scripts are 755).

D. **DISPATCH the cluster run.** Make sure you are on the
   `gsd/phase-16.1-recov-04-b-rmse-diagnostic` branch with all Task 1 changes
   committed. Then dispatch:
   ```
   git push -u origin gsd/phase-16.1-recov-04-b-rmse-diagnostic   # required for cluster checkout
   ssh m3 'cd ~/dcm_pytorch && git fetch origin && git checkout gsd/phase-16.1-recov-04-b-rmse-diagnostic && git reset --hard origin/gsd/phase-16.1-recov-04-b-rmse-diagnostic && bash cluster/submit_phase16_1.sh'
   ```
   (If the user does not have an `m3` alias configured, prompt them with the
   exact command and stop here for them to run it manually. Per cluster job
   submission policy in CLAUDE.md memory, the assistant SHOULD attempt the
   submission via available tools but MUST NOT fabricate output if SSH is not
   reachable from this environment.)
   
   If SSH dispatch is not feasible from the executor environment, this task
   ends with the scripts written + committed + pushed and a clear instruction
   block in the SUMMARY for the user to dispatch manually. The expected
   wall-clock turnaround is 80-150 min (per Phase 16 envelope) and the
   slurm-autopush skill auto-pushes results to a
   `results/phase16_1-acceptance-YYYYMMDD-HHMMSS` branch.

E. **DO NOT** modify `cluster/99_push_phase16_results.slurm` (the autopush
   script is generic — see Phase 16 results branch
   `origin/results/phase16-acceptance-20260424-214708` as evidence; the
   timestamped branch naming makes it phase-agnostic).

F. **DO NOT** rebuild any cluster harness primitives. Reuse the conda env
   activation, pip install, sacct reporting blocks verbatim from the Phase 16
   slurm script.
  </action>
  <verify>
1. `ls cluster/submit_phase16_1.sh cluster/run_phase16_1_acceptance.slurm` (both exist).
2. `head -1 cluster/run_phase16_1_acceptance.slurm` is `#!/bin/bash`.
3. `grep "phase16_1_acceptance" cluster/run_phase16_1_acceptance.slurm` returns at least one match (job-name update).
4. `grep "run_phase16_1_acceptance.slurm" cluster/submit_phase16_1.sh` returns one match (script reference update).
5. `bash -n cluster/submit_phase16_1.sh && bash -n cluster/run_phase16_1_acceptance.slurm` (syntax check passes for both).
6. After dispatch, `git fetch origin && git branch -r | grep results/phase16_1-acceptance-` shows the autopushed results branch within ~3 hours, OR the SUMMARY contains a manual-dispatch instruction block.
  </verify>
  <done>
- Phase 16.1 sbatch infrastructure created by minimal copy-rename from Phase 16 scaffolding.
- Cluster acceptance run dispatched (or manual-dispatch instruction block in SUMMARY).
- No edits to existing Phase 16 cluster scripts or autopush.
  </done>
</task>

<task type="auto">
  <name>Task 3: After cluster results land, flip RECOV-04 in REQUIREMENTS.md and write 16.1-02-SUMMARY.md</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/phases/16.1-recov-04-b-rmse-diagnostic/16.1-02-SUMMARY.md
  </files>
  <action>
**PRECONDITION CHECK (FIRST ACTION):** Run:

```
git fetch origin && git branch -r | grep -E 'results/phase16_1-acceptance-[0-9]'
```

If no match (i.e. the cluster job has not finished and autopushed yet), STOP
Task 3 and emit the following resume instruction block:

```
Cluster results not yet available. To resume:
  1. Confirm SLURM job (ID recorded in 16.1-02-SUMMARY.md) has completed via
     `squeue -u $USER` on M3.
  2. Run `git fetch origin` to pull the autopushed results branch.
  3. Re-execute Task 3 of Plan 16.1-02.
```

Write that resume-instruction block into `16.1-02-SUMMARY.md` under a NEW
`## Awaiting Cluster Completion` section (with the cluster job ID, dispatch
timestamp, and expected wall-time envelope from Task 2). Mark Task 3 as
`pending_cluster` in the plan-state. DO NOT proceed to any sub-step (A through
E) below until the precondition check passes.

This guards against autonomous executors running Task 3 against missing data,
which would either corrupt the SUMMARY or trigger false RECOV-04 flips on
empty input.

Otherwise (results branch present), proceed:

This task runs AFTER the cluster job in Task 2 completes and the autopushed
results branch is fetched.

A. **Fetch results.** `git fetch origin` and identify the latest
   `origin/results/phase16_1-acceptance-*` branch. Read the pytest log at
   `cluster/logs/pytest_phase16_1_${JOB_ID}.log` on that branch (use
   `git show origin/results/phase16_1-acceptance-<timestamp>:cluster/logs/pytest_phase16_1_<JOB_ID>.log`
   or check it out into a temp worktree).

B. **Parse acceptance gate outcomes.** Extract the 4 RECOV gate observed values
   + pass/fail from the pytest log (the test prints them via the `print(...)`
   block at lines 516-538 of the test file before assertions). Also extract
   the RECOV-07 shrinkage_nonnull means.

C. **Branch on outcome:**

   **C.1. ALL 4 GATES PASS + RECOV-07 shrinkage_nonnull means in [0.05, 0.7]:**

   - In `.planning/REQUIREMENTS.md`, flip the **gate-evidence-driven**
     requirements only: RECOV-03, RECOV-04, RECOV-05, RECOV-06, RECOV-07,
     RECOV-08 (these are the cluster-acceptance gates whose pass/fail is
     determined by the Phase 16.1 cluster run). Change each from
     `- [ ] **RECOV-NN**:` to `- [x] **RECOV-NN**:`. They should have been
     flipped at the end of Phase 16 but the Phase-16 acceptance failure
     prevented the flip.

   - **Separately, flip RECOV-01 and RECOV-02 with an explicit cross-reference
     citation.** RECOV-01 and RECOV-02 are STRUCTURAL runner-architecture
     requirements (runner exists; fixture infrastructure integrated) — they
     are NOT cluster-gate items, and Phase 16.1 produces no direct evidence
     about them. Flip them here on the basis that Phase 16 plans 16-01,
     16-02, and 16-03 already delivered the runner architecture and fixture-
     infrastructure integration (see `.planning/phases/16-bilinear-recovery-benchmark/16-01-SUMMARY.md`,
     `16-02-SUMMARY.md`, `16-03-SUMMARY.md`); Phase 16.1's cluster acceptance
     pass confirms the runner is functionally correct end-to-end. Add an
     inline citation comment on each of RECOV-01 / RECOV-02 lines:
     `(Delivered by Phase 16 plans 16-01/16-02/16-03; flipped Phase 16.1 after
     end-to-end cluster validation — see 16.1-02-SUMMARY.md.)`.

   - This split makes the audit trail explicit: RECOV-03..08 are flipped on
     Phase 16.1 cluster evidence; RECOV-01/02 are flipped with cross-reference
     to Phase 16 deliverables. Verify by
     `grep "^- \[ \] \*\*RECOV-" .planning/REQUIREMENTS.md` returning no rows
     for the v0.3.0 RECOV section.
   - DO NOT modify the RECOV-04 wording (threshold stays `<= 0.20`); this plan
     does not relax the gate. If the cluster results comfortably pass under
     the new init_scale, no threshold revision is needed.
   - Add a one-line citation under the RECOV-07 entry: append
     `(Phase 16.1: confirmed in [0.05, 0.7] range under _BILINEAR_INIT_SCALE={value}; see 16.1-02-SUMMARY.md.)`.
   - Write `16.1-02-SUMMARY.md` with status `"complete"`. Document:
       - Cluster job ID + start/finish ISO timestamps.
       - The 4-row RECOV gate outcome table (observed vs threshold per gate).
       - RECOV-07 shrinkage_nonnull mean (must be in [0.05, 0.7] per REQUIREMENTS.md line 59).
       - Wall time (RECOV-08).
       - The chosen `_BILINEAR_INIT_SCALE` and rationale.
       - The retry-policy update (halve once on NaN).
       - Any seeds skipped or retried during the run.
       - A concise "milestone-closure" block: "v0.3.0 RECOV-04 acceptance gate
         now passing. /gsd:audit-milestone v0.3.0 may proceed."

   **C.2. RECOV-04 PASSES BUT RECOV-06 (coverage of zero) DROPS BELOW 85%:**

   This is the Open Question 4 escalation path. Per L9 lock + RESEARCH.md
   Open Question 4: RECOV-06 threshold is HARD; this plan MUST NOT relax it.
   - DO NOT flip RECOV-04 to `[x]` (the milestone-blocking flag has just shifted
     from RECOV-04 to RECOV-06; the implementation still doesn't pass all
     gates).
   - Write `16.1-02-SUMMARY.md` with status `"escalation"`. Document:
       - The cluster job + outcome table (RECOV-04 pass, RECOV-06 fail).
       - The L9 / D5 conflict: RECOV-06 degradation under wider init_scale is
         expected (wider init_scale -> wider posterior -> tighter null-element
         CIs -> coverage drops); the documented next step is a guide-family
         discussion (out of scope per D5; would require a milestone-scope
         decision to lift D5 or accept Phase 16.2 with AutoLowRankMVN /
         per-site init).
       - Recommended Phase 16.2: "Guide-family + per-site init_scale
         investigation. Lift D5 or document the trade-off."
   - In REQUIREMENTS.md, leave RECOV-04 at `[ ]` and add a one-line note
     after RECOV-06: `(Phase 16.1: degraded to <observed>% under widened init_scale; see 16.1-02-SUMMARY.md for D5 escalation.)`.

   **C.3. RECOV-04 STILL FAILS (B-RMSE > 0.20):**

   - DO NOT flip RECOV-04 in REQUIREMENTS.md.
   - Write `16.1-02-SUMMARY.md` with status `"failed"`. Document:
       - The cluster outcome (B-RMSE observed value, deviation from local
         single-seed prediction in 16.1-01).
       - This means the local diagnostic on seed 42 was NOT representative of
         the 10-seed cluster distribution (possible seed-specific
         identifiability variance).
       - Recommended next step: Phase 16.2 with the 2D mini-grid Experiment 3
         from RESEARCH.md (init_scale × n_svi_steps cross-product on cluster).
       - Do NOT ship a guess at a third init_scale; the local-diagnostic
         methodology has been falsified at scale and needs revisiting.

D. **Update REQUIREMENTS.md note (in C.1 only).** Per the planning-context
   line "The phase may TIGHTEN or AMEND RECOV-04 / RECOV-07 wording but MUST
   NOT relax the gates": Plan 16.1 does NOT tighten or relax. The thresholds
   stand. The only REQUIREMENTS edit is the citation note (above). If you
   find yourself wanting to change a threshold, STOP and add an "open
   question" to the SUMMARY for user resolution.

E. **DO NOT** update the ROADMAP or STATE.md from this task. Those updates
   happen at /gsd:audit-milestone time after the SUMMARY lands.
  </action>
  <verify>
1. `cat .planning/phases/16.1-recov-04-b-rmse-diagnostic/16.1-02-SUMMARY.md | head -40` shows the standard summary frontmatter with status field set to one of `complete | escalation | failed`.
2. `grep -E "(complete|escalation|failed)" .planning/phases/16.1-recov-04-b-rmse-diagnostic/16.1-02-SUMMARY.md | head -3` shows the status.
3. `grep "Cluster job ID" .planning/phases/16.1-recov-04-b-rmse-diagnostic/16.1-02-SUMMARY.md` returns one line.
4. **(C.1 only)** `grep "^- \[ \] \*\*RECOV-04\*\*" .planning/REQUIREMENTS.md` returns 0 lines AND `grep "^- \[x\] \*\*RECOV-04\*\*" .planning/REQUIREMENTS.md` returns 1 line.
5. **(C.2 / C.3)** `grep "^- \[ \] \*\*RECOV-04\*\*" .planning/REQUIREMENTS.md` returns 1 line (gate not flipped; phase escalates).
6. The cluster job ID and result branch name are recorded so /gsd:audit-milestone can audit the run trail.
  </verify>
  <done>
- Cluster results parsed; the appropriate outcome branch (C.1, C.2, or C.3) executed.
- REQUIREMENTS.md flipped or annotated according to the outcome.
- 16.1-02-SUMMARY.md written with the cluster job ID, RECOV gate observations, retry stats, and milestone-closure or escalation status.
- No threshold relaxations applied to any RECOV gate.
  </done>
</task>

</tasks>

<verification>
End-of-plan checks (run before declaring complete):

1. `grep -n "_BILINEAR_INIT_SCALE = " benchmarks/runners/task_bilinear.py` shows the chosen value AND a docstring referencing the Phase 16.1 diagnostic.
2. `grep -c "_BILINEAR_INIT_SCALE_RETRY" benchmarks/runners/task_bilinear.py` returns 0.
3. `pytest tests/test_task_bilinear_benchmark.py -m "not slow"` passes locally.
4. `ls cluster/submit_phase16_1.sh cluster/run_phase16_1_acceptance.slurm` (both exist).
5. Cluster job dispatched (or clear manual-dispatch instructions in SUMMARY).
6. After cluster completion, RECOV-04 status in REQUIREMENTS.md matches the cluster outcome (flipped to `[x]` only on full pass; left `[ ]` with annotation on RECOV-06 degradation or RECOV-04 still-failing).
7. 16.1-02-SUMMARY.md status field is one of {complete, escalation, failed}, matching the cluster outcome.
8. Scope-creep guard: `git diff --name-only main...HEAD` matches the `files_modified` frontmatter list — with two exceptions:
   (a) under outcome C.3 (RECOV-04 still failing), `.planning/REQUIREMENTS.md` may NOT appear in the diff because no flip happens (no gate passed; no annotation needed beyond the SUMMARY);
   (b) under outcome C.2 (RECOV-04 pass + RECOV-06 degrade), `.planning/REQUIREMENTS.md` may appear with annotation-only changes (the RECOV-06 inline note), and RECOV-04 stays `[ ]`.
   Under outcome C.1, the full `files_modified` set must match the diff (RECOV-01..08 flipped + annotations).
</verification>

<success_criteria>
- Chosen `_BILINEAR_INIT_SCALE` from Plan 16.1-01 landed in versioned source (not in env vars or notebooks).
- Retry policy is now self-consistent under raised init_scale (halve once on NaN at step 0).
- Phase 16.1 cluster sbatch infrastructure exists and is consistent with the Phase 16 conventions (no harness rebuild).
- Cluster 10-seed acceptance run dispatched.
- Outcome documented in REQUIREMENTS.md + 16.1-02-SUMMARY.md per the C.1 / C.2 / C.3 branch table.
- v0.3.0 RECOV-04 milestone-blocker resolved (C.1) OR cleanly escalated to a follow-up phase with an evidence trail (C.2 / C.3).
</success_criteria>

<output>
After completion, /gsd:audit-milestone v0.3.0 may run if the SUMMARY status is
"complete". On "escalation" or "failed", the next step is /gsd:add-phase 16.2
with scope per the SUMMARY recommendation.
</output>
