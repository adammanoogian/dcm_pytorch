---
phase: 08-metrics-benchmarks-and-documentation
plan: 05
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/pyro_dcm/__init__.py
  - src/pyro_dcm/forward_models/neural_state.py
  - src/pyro_dcm/forward_models/balloon_model.py
  - src/pyro_dcm/forward_models/bold_signal.py
  - src/pyro_dcm/forward_models/spectral_transfer.py
  - src/pyro_dcm/forward_models/csd_computation.py
  - src/pyro_dcm/forward_models/rdcm_forward.py
  - src/pyro_dcm/forward_models/rdcm_posterior.py
  - src/pyro_dcm/models/task_dcm_model.py
  - src/pyro_dcm/models/spectral_dcm_model.py
  - src/pyro_dcm/models/rdcm_model.py
  - src/pyro_dcm/models/guides.py
  - src/pyro_dcm/guides/amortized_flow.py
  - src/pyro_dcm/guides/summary_networks.py
  - src/pyro_dcm/guides/parameter_packing.py
  - src/pyro_dcm/simulators/task_simulator.py
  - src/pyro_dcm/simulators/spectral_simulator.py
  - src/pyro_dcm/simulators/rdcm_simulator.py
  - src/pyro_dcm/utils/ode_integrator.py
autonomous: true

must_haves:
  truths:
    - "All public API functions have complete NumPy-style docstrings with Parameters, Returns, and Examples"
    - "ruff check --select D on all source files reports zero docstring violations"
    - "Top-level __init__.py exports include extract_posterior_params and simulator functions"
    - "All existing tests still pass after docstring additions"
  artifacts:
    - path: "src/pyro_dcm/__init__.py"
      provides: "Complete public API exports"
      contains: "extract_posterior_params"
  key_links:
    - from: "src/pyro_dcm/__init__.py"
      to: "src/pyro_dcm/simulators/task_simulator.py"
      via: "simulator exports in __all__"
      pattern: "simulate_task_dcm"
---

<objective>
Audit and complete all public API docstrings, add Examples sections to key functions,
and expand the top-level __init__.py exports for a polished v0.1.0 release.

Purpose: Ensures every public function a researcher would call has complete documentation
including usage examples. The public API in __init__.py becomes the definitive import
surface. This is the final polish before v0.1.0.
Output: Updated docstrings across ~18 source files, expanded __init__.py exports.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-metrics-benchmarks-and-documentation/08-RESEARCH.md

# Current public API surface:
@src/pyro_dcm/__init__.py
@src/pyro_dcm/models/__init__.py
@src/pyro_dcm/guides/__init__.py
@src/pyro_dcm/simulators/__init__.py
@src/pyro_dcm/forward_models/__init__.py

# Key modules to audit:
@src/pyro_dcm/models/guides.py
@src/pyro_dcm/simulators/task_simulator.py
@src/pyro_dcm/simulators/spectral_simulator.py
@src/pyro_dcm/simulators/rdcm_simulator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1A: Audit docstrings in forward_models (~7 files)</name>
  <files>
    src/pyro_dcm/forward_models/neural_state.py
    src/pyro_dcm/forward_models/balloon_model.py
    src/pyro_dcm/forward_models/bold_signal.py
    src/pyro_dcm/forward_models/spectral_transfer.py
    src/pyro_dcm/forward_models/csd_computation.py
    src/pyro_dcm/forward_models/rdcm_forward.py
    src/pyro_dcm/forward_models/rdcm_posterior.py
  </files>
  <action>
Systematic docstring audit for the forward_models subpackage. For EACH file:

1. Read the file.
2. Identify all public functions (not prefixed with _) and classes.
3. Check if the docstring has: Summary line, Parameters, Returns, References (for math),
   and Examples sections.
4. Add missing sections. Do NOT rewrite existing docstrings that are already complete.
   Only ADD what's missing.

For math functions (forward_models), ensure the References section has the
[REF-XXX] Eq. N citation. Most should already have this from prior phases.
Verify, do not assume.

Rules:
- Examples must use the actual function signature. Do NOT invent parameters.
- Keep examples short (3-5 lines of actual code, plus import).
- Every Example should be self-contained enough to understand the pattern.
- Do NOT add examples to private/internal helpers (_, __).
- ONLY touch docstrings. Do NOT modify function logic or signatures.
- DO NOT add docstrings to `__init__` methods unless they have complex parameters.
  The class docstring suffices for classes with simple `__init__`.

Run `ruff check --select D src/pyro_dcm/forward_models/` after all edits.
  </action>
  <verify>
    ruff check --select D src/pyro_dcm/forward_models/ 2>&1 | tail -5
    python -m pytest tests/ -x -q --timeout=120 2>&1 | tail -5
  </verify>
  <done>
    All public functions in forward_models/ have complete NumPy-style docstrings.
    Math functions have [REF-XXX] citations in References sections.
    ruff check --select D reports zero violations on forward_models/.
    All existing tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 1B: Audit docstrings in models, guides, simulators, utils (~11 files)</name>
  <files>
    src/pyro_dcm/models/task_dcm_model.py
    src/pyro_dcm/models/spectral_dcm_model.py
    src/pyro_dcm/models/rdcm_model.py
    src/pyro_dcm/models/guides.py
    src/pyro_dcm/guides/amortized_flow.py
    src/pyro_dcm/guides/summary_networks.py
    src/pyro_dcm/guides/parameter_packing.py
    src/pyro_dcm/simulators/task_simulator.py
    src/pyro_dcm/simulators/spectral_simulator.py
    src/pyro_dcm/simulators/rdcm_simulator.py
    src/pyro_dcm/utils/ode_integrator.py
  </files>
  <action>
Systematic docstring audit for models, guides, simulators, and utils subpackages.
Same protocol as Task 1A.

Priority order for adding Examples sections (most important first):
- `run_svi()` in guides.py -- this is the primary inference entry point
- `create_guide()` in guides.py
- `extract_posterior_params()` in guides.py
- `simulate_task_dcm()` in task_simulator.py
- `simulate_spectral_dcm()` in spectral_simulator.py
- `simulate_rdcm()` in rdcm_simulator.py
- `make_random_stable_A()` in task_simulator.py
- `make_block_stimulus()` in task_simulator.py
- `task_dcm_model()` in task_dcm_model.py
- `spectral_dcm_model()` in spectral_dcm_model.py
- `AmortizedFlowGuide` class in amortized_flow.py (class docstring)

Example format for `run_svi`:
```python
Examples
--------
>>> import pyro
>>> from pyro_dcm.models import task_dcm_model, create_guide, run_svi
>>> guide = create_guide(task_dcm_model, init_scale=0.01)
>>> result = run_svi(
...     task_dcm_model, guide,
...     model_args=(bold, stimulus, a_mask, c_mask, t_eval, TR, dt),
...     num_steps=500, lr=0.005,
... )
>>> print(f"Final loss: {result['final_loss']:.2f}")
```

Rules:
- Examples must use the actual function signature. Do NOT invent parameters.
- Keep examples short (3-5 lines of actual code, plus import).
- ONLY touch docstrings. Do NOT modify function logic or signatures.
- Run `ruff check --select D src/pyro_dcm/` after all edits to catch issues.
  </action>
  <verify>
    ruff check --select D src/pyro_dcm/models/ src/pyro_dcm/guides/ src/pyro_dcm/simulators/ src/pyro_dcm/utils/ 2>&1 | tail -5
    python -m pytest tests/ -x -q --timeout=120 2>&1 | tail -5
  </verify>
  <done>
    All public functions in models/, guides/, simulators/, utils/ have complete docstrings.
    Top ~15 most important functions have Examples sections.
    ruff check --select D reports zero violations.
    All existing tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand top-level __init__.py exports</name>
  <files>
    src/pyro_dcm/__init__.py
  </files>
  <action>
Expand the top-level pyro_dcm/__init__.py to include the full public API surface
that researchers would reasonably import.

Current exports (from Phase 7):
- task_dcm_model, spectral_dcm_model, rdcm_model
- create_guide, run_svi
- AmortizedFlowGuide
- amortized_task_dcm_model, amortized_spectral_dcm_model

Add these additional exports:

From models:
- extract_posterior_params (already in models/__init__.py but NOT in top-level)

From simulators:
- simulate_task_dcm, make_random_stable_A, make_block_stimulus
- simulate_spectral_dcm, make_stable_A_spectral
- simulate_rdcm (if it exists as a public function)

From forward_models:
- parameterize_A (used in posterior analysis to convert A_free -> A)

From guides (already exported):
- BoldSummaryNet, CsdSummaryNet, TaskDCMPacker, SpectralDCMPacker (keep existing)

Organize __all__ into clear sections:
```python
__all__ = [
    "__version__",
    # Generative models
    "task_dcm_model",
    "spectral_dcm_model",
    "rdcm_model",
    # Inference
    "create_guide",
    "run_svi",
    "extract_posterior_params",
    # Amortized inference
    "AmortizedFlowGuide",
    "amortized_task_dcm_model",
    "amortized_spectral_dcm_model",
    "BoldSummaryNet",
    "CsdSummaryNet",
    "TaskDCMPacker",
    "SpectralDCMPacker",
    # Simulators
    "simulate_task_dcm",
    "make_random_stable_A",
    "make_block_stimulus",
    "simulate_spectral_dcm",
    "make_stable_A_spectral",
    # Utilities
    "parameterize_A",
]
```

Add corresponding import statements at the top of the file.

Verify with: `python -c "from pyro_dcm import simulate_task_dcm, extract_posterior_params, parameterize_A; print('All imports OK')"`
  </action>
  <verify>
    python -c "from pyro_dcm import simulate_task_dcm, extract_posterior_params, parameterize_A, make_random_stable_A, make_block_stimulus, simulate_spectral_dcm; print('All new exports OK')"
    python -c "import pyro_dcm; print(f'{len(pyro_dcm.__all__)} exports in __all__')"
    python -m pytest tests/ -x -q --timeout=120 2>&1 | tail -5
  </verify>
  <done>
    Top-level __init__.py exports ~20 public API symbols organized by category.
    All imports resolve cleanly.
    All existing tests still pass.
  </done>
</task>

</tasks>

<verification>
- `ruff check --select D src/pyro_dcm/` reports zero violations (except ignored D100/D104)
- `python -c "from pyro_dcm import simulate_task_dcm, extract_posterior_params"` works
- `python -m pytest tests/ -x -q --timeout=120` all pass
- Key public functions have Examples sections in docstrings
</verification>

<success_criteria>
- All public functions have complete NumPy-style docstrings
- Top ~15 most important functions have Examples sections
- Top-level __init__.py exports the full public API (~20 symbols)
- No ruff docstring violations
- All existing tests pass (docstring changes are additive only)
</success_criteria>

<output>
After completion, create `.planning/phases/08-metrics-benchmarks-and-documentation/08-05-SUMMARY.md`
</output>
