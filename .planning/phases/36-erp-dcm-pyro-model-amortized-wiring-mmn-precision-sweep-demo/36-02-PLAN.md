---
phase: 36-erp-dcm-pyro-model-amortized-wiring-mmn-precision-sweep-demo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pyro_dcm/forward_models/mmn_reference.py
  - src/pyro_dcm/forward_models/__init__.py
  - tests/test_mmn_reference.py
autonomous: true

must_haves:
  truths:
    - "build_mmn_5source_network() returns the canonical 5-source auditory-MMN topology (A1L=0,A1R=1,STGL=2,STGR=3,rIFG=4) reproducing the locked export_to_mat._MS_* edge lists ELEMENT-WISE."
    - "The network has forward (A1L->STGL,A1R->STGR,STGL->rIFG,STGR->rIFG), backward (reverse), lateral reciprocal STGL<->STGR, C into bilateral A1, and a deviant-vs-standard B with diag at {rIFG,A1L,A1R}."
    - "mmn_cmc_params(sp_inhibition_gain,a1_b_gain,rifg_b_gain,fwd_bwd_flag) returns a ready-to-simulate forward-only bundle ({p,a_masks,b_masks,c_mask,x_design,l_full})."
    - "Perturbing sp_inhibition_gain sets P.G[:,0] at {rIFG,A1L,A1R} and that flows to G[:,6] (J_PERM[0]=6), NOT G[:,0] — the Phase-33 permutation guard reused."
    - "NO MNI coordinates are hard-coded (LFP scope; coords are MUST-VERIFY and not load-bearing)."
  artifacts:
    - path: "src/pyro_dcm/forward_models/mmn_reference.py"
      provides: "build_mmn_5source_network() + mmn_cmc_params() — the public 5-source MMN net + the actinf_physics adapter"
      min_lines: 90
      contains: "def build_mmn_5source_network"
    - path: "src/pyro_dcm/forward_models/__init__.py"
      provides: "build_mmn_5source_network, mmn_cmc_params exports (append-only)"
      contains: "build_mmn_5source_network"
    - path: "tests/test_mmn_reference.py"
      provides: "topology-equality vs _MS_* (element-wise) + permutation/precision-node wiring + adapter-bundle shape tests"
  key_links:
    - from: "src/pyro_dcm/forward_models/mmn_reference.py"
      to: "validation.export_to_mat._MS_* topology constants"
      via: "promote the locked _MS_FORWARD/_LATERAL/_BACKWARD/_INPUT/_PRECISION edge lists to a public builder"
      pattern: "STGL|rIFG|A1L"
    - from: "mmn_cmc_params(sp_inhibition_gain=...)"
      to: "G[:,6]"
      via: "P.G[node,0] at {rIFG,A1L,A1R} -> J_PERM[0]=6 -> G[:,6] (precision permutation)"
      pattern: "J_PERM|G\\[:, ?6\\]|G\\[:,0\\]"
---

<objective>
Promote the locked private 5-source auditory-MMN topology to a public, importable network builder and provide the forward-only consumer adapter the `actinf_physics` Phase-133 hand-off imports (ERPDCM-03, ERPDCM-05).

Purpose: One source of truth for the MMN network — the demo (36-03), the adapter, and the downstream consumer all share `build_mmn_5source_network()` instead of re-typing edge lists (pitfall: re-typed topology silently diverges from the parity-gated fixture). `mmn_cmc_params(...)` turns the precision knobs into ready-to-simulate CMC params — the thin map the consumer needs.
Output: NEW `forward_models/mmn_reference.py` (`build_mmn_5source_network`, `mmn_cmc_params`); appended `__init__` exports; one new test file. Forward-only — no fitting, all LAPTOP.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-erp-dcm-pyro-model-amortized-wiring-mmn-precision-sweep-demo/36-RESEARCH.md

# The locked _MS_* topology to promote (edge lists, precision nodes, B_EDGE/B_DIAG, A_LIVE/A_DEAD)
@validation/export_to_mat.py

# The condition-modulation port (diag(B)->G[:,0]) + the permutation guard
@src/pyro_dcm/forward_models/erp_coupled_system.py
@src/pyro_dcm/forward_models/cmc_neural_mass.py

# The LFP lead field the bundle carries
@src/pyro_dcm/forward_models/erp_leadfield.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: build_mmn_5source_network() — the public 5-source MMN net (ERPDCM-03)</name>
  <files>src/pyro_dcm/forward_models/mmn_reference.py, src/pyro_dcm/forward_models/__init__.py</files>
  <action>
Create `forward_models/mmn_reference.py`. Implement `build_mmn_5source_network()` that promotes the
locked `_MS_*` constants from `validation/export_to_mat.py` (lines ~660-701) to a public, importable
builder. 0-indexed sources A1L=0, A1R=1, STGL=2, STGR=3, rIFG=4. Topology (edges as `[to, from]`,
matching `cmc_network_f`/`spm_fx_cmc` A[i]@S routing):
- Forward (`A{1}` sp->ss, `A{2}` sp->dp): A1L->STGL, A1R->STGR, STGL->rIFG, STGR->rIFG
- Lateral RECIPROCAL STGL<->STGR (added to BOTH forward blocks -> triggers the (1+4L) reduction)
- Backward (`A{3}` dp->sp, `A{4}` dp->ii): rIFG->STGL, rIFG->STGR, STGL->A1L, STGR->A1R
- Input C: A1L, A1R only
- B precision nodes (diag(B)->G[:,6]): rIFG, A1L, A1R; `B_EDGE=0.3` on every extrinsic edge,
  `B_DIAG=0.5` on the precision diag
- Design `x_design = [[0],[1]]` (Cnd=2, n_effects=1; row0=standard, row1=deviant)

Build the four `(5,5)` extrinsic masks (live=1/present, the demo will map to free log values via
mmn_cmc_params), the `(5,5)` B value matrix, the `(5,1)` C mask. Return a dict:
```
{"a_masks": [4 x (5,5)], "b_masks": [(5,5)], "c_mask": (5,1), "x_design": (2,1),
 "source_names": ["A1L","A1R","STGL","STGR","rIFG"], "precision_nodes": (4,0,1)}
```
DO NOT include MNI coordinates (LFP scope; coords are MUST-VERIFY per Ranlund 2016 / Garrido 2009 and
NOT load-bearing for the LFP demo — Open Q1). float64. To guarantee the single-source-of-truth
contract, IMPORT and reuse the `_MS_*` constants/edge tuples from `validation.export_to_mat` rather
than re-typing them (the topology-equality test in Task 2 asserts element-wise identity either way).

Append `build_mmn_5source_network` to `forward_models/__init__.py` (append-only).
NumPy docstring citing `spm_fx_cmc.m:68-82,171-198` (extrinsic blocks + lateral reduction) and
Garrido 2009 / Ranlund 2016 by author/year inline (NO `[REF-xxx]` — Zotero rule). `from __future__
import annotations`; line length 88; ruff/format/mypy clean.
  </action>
  <verify>
`python -c "from pyro_dcm.forward_models import build_mmn_5source_network; net=build_mmn_5source_network(); print({k: (v.shape if hasattr(v,'shape') else v) for k,v in net.items()})"` prints the 4 a_masks (5,5), b_masks[(5,5)], c_mask (5,1), x_design (2,1), names, precision_nodes. `ruff check`/`format --check` clean.
  </verify>
  <done>
`build_mmn_5source_network()` returns the locked 5-source topology (forward/backward/lateral/C/B-precision) with NO MNI coords; exported from `forward_models/__init__.py`.
  </done>
</task>

<task type="auto">
  <name>Task 2: mmn_cmc_params() adapter + topology/permutation tests (ERPDCM-05)</name>
  <files>src/pyro_dcm/forward_models/mmn_reference.py, tests/test_mmn_reference.py</files>
  <action>
APPEND `mmn_cmc_params(sp_inhibition_gain, a1_b_gain, rifg_b_gain, fwd_bwd_flag="both")` to
`mmn_reference.py` — the forward-only `actinf_physics` adapter:
- `sp_inhibition_gain` -> set `P.G[node, 0] = sp_inhibition_gain` for `node in {rIFG, A1L, A1R}`
  (the swept precision knob; flows to `G[:,6]` via `J_PERM[0]=6`). This is the FREE `P.G[:,0]` —
  NEVER index `G[:,6]` directly (the permutation trap S1).
- `a1_b_gain` -> `B_DIAG` at A1L, A1R; `rifg_b_gain` -> `B_DIAG` at rIFG.
- `fwd_bwd_flag in {"forward","backward","both"}` -> which extrinsic blocks carry the `B_EDGE`
  modulation (the Garrido/Ranlund model-space toggle; "forward"=A{1}/A{2} edges only,
  "backward"=A{3}/A{4} only, "both"=all).
- Build the free-log-space `p` struct (keys T(5,4)/G(5,4)/C(5,1)/S(5,1)/R(1,2)/A(list[4] (5,5))/
  B(list (5,5))) from `build_mmn_5source_network()` masks using the `_MS_A_LIVE`(0)/`_MS_A_DEAD`(-32)
  convention for A/C and additive B values; compute the LFP lead field
  `l_full = build_lead_field(cmc_default_pj(), lfp_spatial(torch.ones(5), 5))`.
- Return `{"p": p, "a_masks", "b_masks", "c_mask", "x_design", "l_full"}` — ready to feed
  `simulate_erp_dcm(p, x_design, 5, l_full=l_full)`. NO fitting; this is the forward-only contract.

Tests (`tests/test_mmn_reference.py`, all LAPTOP):
- TOPOLOGY EQUALITY: assert `build_mmn_5source_network()` a_masks/b_masks/c_mask reproduce the
  `export_to_mat._MS_*` edge lists ELEMENT-WISE (reconstruct the expected (5,5) masks from
  `_MS_FORWARD_EDGES`/`_MS_LATERAL_EDGES`/`_MS_BACKWARD_EDGES`/`_MS_INPUT_SOURCES`/
  `_MS_PRECISION_NODES` and assert equality).
- PERMUTATION/PRECISION wiring: call `mmn_cmc_params(sp_inhibition_gain=g, ...)`; run
  `apply_condition_modulation`/`parameterize_cmc_network` on the deviant row and assert perturbing
  `P.G[:,0]` at rIFG changes the parameterized `G[:,6]` (NOT `G[:,0]`) — reuse the Phase-33/34
  permutation-guard pattern from `tests/test_cmc_forward.py`/`test_erp_coupled_system.py`.
- ADAPTER BUNDLE: assert `mmn_cmc_params(...)` returns all expected keys with correct shapes and
  that `simulate_erp_dcm(bundle["p"], bundle["x_design"], 5, l_full=bundle["l_full"])` runs finite
  and returns a non-zero `difference_wave_scalp` (forward-only; <30s LAPTOP).
- NEGATIVE control: `fwd_bwd_flag="forward"` vs `"backward"` produce DIFFERENT B-edge placements.

float64; NumPy docstrings (Adams 2013 / Ranlund 2016 by author/year inline; NO `[REF-xxx]`);
ADDITIVE-ONLY (`git diff --stat` only the 3 plan files + the `forward_models/__init__` append);
ruff/format/mypy clean.
  </action>
  <verify>
LAPTOP: `pytest tests/test_mmn_reference.py -x -q` — topology-equality, permutation/precision wiring, adapter-bundle finiteness + non-zero diff wave, and the fwd/bwd negative control PASS (<30s). `ruff check`/`mypy` deltas only the documented baseline. `git diff --stat` lists only the plan files.
  </verify>
  <done>
`mmn_cmc_params(...)` returns a ready-to-simulate forward-only bundle whose sp_inhibition_gain moves the parameterized `G[:,6]` (not `G[:,0]`); the public builder reproduces the `_MS_*` topology element-wise; the fwd/bwd flag toggles B-edge placement; all tests green; additive-only.
  </done>
</task>

</tasks>

<verification>
- `python -c "from pyro_dcm.forward_models import build_mmn_5source_network, mmn_cmc_params"` clean.
- Topology equality vs `export_to_mat._MS_*` element-wise (test green).
- `mmn_cmc_params` sp_inhibition_gain -> P.G[:,0] at {rIFG,A1L,A1R} -> moves G[:,6] (permutation guard reused), not G[:,0].
- Bundle feeds `simulate_erp_dcm` -> finite, non-zero difference wave; no MNI coords present.
- Additive-only; ruff/format clean; mypy baseline-only delta. All forward-only / LAPTOP.
</verification>

<success_criteria>
ERPDCM-03: a 5-source auditory MMN network (A1 L/R, STG L/R, rIFG) with forward/backward/lateral graph, C into bilateral A1, deviant-vs-standard B; MNI coords flagged (not hard-coded).
ERPDCM-05: a consumer-facing adapter `(sp_inhibition_gain, a1_b_gain, rifg_b_gain, fwd_bwd_flag) -> CMC params` (forward-only bundle) for the actinf_physics Phase-133 adapter.
</success_criteria>

<output>
After completion, create `.planning/phases/36-erp-dcm-pyro-model-amortized-wiring-mmn-precision-sweep-demo/36-02-SUMMARY.md` (record: the public builder API; the adapter signature + bundle keys; confirmation that no MNI coords were hard-coded; the precision-permutation guard result).
</output>
