---
phase: 36-erp-dcm-pyro-model-amortized-wiring-mmn-precision-sweep-demo
plan: 03
type: execute
wave: 2
depends_on: ["36-02"]
files_modified:
  - scripts/demo_mmn_precision_sweep.py
  - figures/mmn_precision_sweep.png
  - figures/mmn_precision_sweep.pdf
autonomous: true

must_haves:
  truths:
    - "The demo runs the fixed-reference SPM forward-parity check FIRST and REFUSES to emit any figure unless it is green (scalp + diff wave reproduce the frozen erp_leadfield_fixtures.mat within <=1e-7)."
    - "The precision sweep over superficial-pyramidal self-inhibition gain (P.G[:,0] -> G[:,6]) at {rIFG,A1L,A1R} produces a MONOTONE (non-increasing) gain->|MMN| transfer curve."
    - "The deviant-standard difference wave at the rIFG channel is NEGATIVE-going (diff_wave_scalp[peak, rIFG] < 0)."
    - "The difference wave is FRONTAL-DOMINANT (max|diff_wave_scalp[:,rIFG]| > max|diff_wave_scalp[:,A1L/A1R]|)."
    - "The sweep perturbs the FREE P.G[:,0] (not G[:,6] directly) and the permutation guard confirms it moves G[:,6] (pure-forward sweep; reuses the Phase-33 guard)."
    - "figures/mmn_precision_sweep.{png,pdf} are written ONLY after the gate is green: the gain->|MMN| transfer curve + low/baseline/high difference-wave overlay."
  artifacts:
    - path: "scripts/demo_mmn_precision_sweep.py"
      provides: "self-contained main(): gate -> sweep -> assert (monotone/negative/frontal) -> figure"
      min_lines: 120
      contains: "difference_wave_scalp"
    - path: "figures/mmn_precision_sweep.png"
      provides: "publication-quality gain->|MMN| transfer curve + difference-wave overlay"
    - path: "figures/mmn_precision_sweep.pdf"
      provides: "vector version of the same figure"
  key_links:
    - from: "scripts/demo_mmn_precision_sweep.py (GATE)"
      to: "validation/data/erp_leadfield_fixtures.mat"
      via: "reuse _reference_p/_production_scalp logic; assert scalp & diff_wave <=1e-7 BEFORE any figure"
      pattern: "erp_leadfield_fixtures|1e-7"
    - from: "scripts/demo_mmn_precision_sweep.py (SWEEP)"
      to: "mmn_cmc_params -> simulate_erp_dcm"
      via: "sweep sp_inhibition_gain -> bundle -> simulate_erp_dcm(...,l_full=...) -> difference_wave_scalp"
      pattern: "mmn_cmc_params|simulate_erp_dcm"
    - from: "scripts/demo_mmn_precision_sweep.py (KNOB)"
      to: "G[:,6]"
      via: "P.G[:,0] perturbation -> J_PERM[0]=6 -> G[:,6] (permutation guard reused)"
      pattern: "G\\[:,0\\]|G\\[:, ?6\\]|J_PERM"
---

<objective>
Produce the headline 5-source MMN precision-sweep demo — the `actinf_physics` hand-off artifact — gated behind a green fixed-reference SPM forward-parity check, with a monotone gain->|MMN| transfer curve and a frontal-dominant negative-going difference wave (ERPDCM-04, ERPDCM-06).

Purpose: The capstone deliverable. The figure is only credible once the single fixed-reference forward point is SPM-validated (S3); the transfer curve turns a figure into a *function* the consumer imports (D3). The sweep is PURE-FORWARD over the verified precision knob `P.G[:,0] -> G[:,6]`.
Output: NEW `scripts/demo_mmn_precision_sweep.py` (gate -> sweep -> assert -> figure); `figures/mmn_precision_sweep.{png,pdf}`.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-erp-dcm-pyro-model-amortized-wiring-mmn-precision-sweep-demo/36-RESEARCH.md

# The parity gate to REUSE: _reference_p / _production_scalp / fixture-loading logic, <=1e-7
@tests/test_spm_erp_leadfield_validation.py

# The forward + the (Cnd,ns,Nc) scalp / difference_wave_scalp keys
@src/pyro_dcm/simulators/erp_simulator.py

# The 5-source net + the precision-knob adapter (from plan 36-02)
@src/pyro_dcm/forward_models/mmn_reference.py

# Demo-script idiom (self-contained main(), matplotlib figure)
@scripts/demo_bilinear_consumer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: The fixed-reference SPM parity GATE (ERPDCM-06)</name>
  <files>scripts/demo_mmn_precision_sweep.py</files>
  <action>
Create `scripts/demo_mmn_precision_sweep.py` with a self-contained `main()`. Implement the GATE
FIRST (it runs before anything else writes output):
- Load `validation/data/erp_leadfield_fixtures.mat`. Reuse the Phase-35 logic from
  `tests/test_spm_erp_leadfield_validation.py`: `_reference_p()` (reconstruct the exact free-log P
  from the `_MS_*` topology), `_torch_l_full()` (`build_lead_field(cmc_default_pj(), lfp_spatial(
  ones(5),5))`), and the `_production_scalp` path (per-condition `apply_condition_modulation` ->
  `integrate_local_linearization(cmc_network_f)` -> `project_to_scalp`). Factor the shared logic into
  a small helper imported from the test module OR replicate it faithfully in the script (cite the
  test as the source of truth).
- Assert the production `scalp` and `diff_wave` reproduce the frozen `y_scalp`/`diff_wave` arrays
  `<= 1e-7` (the Phase-35 LEAD-05 tolerance). The reference params ARE byte-identical to the fixture's
  `_reference_p()` (the demo's reference point == the fixture — NO new ECD fixture; ERPDCM-06 reuses
  the LFP fixture).
- If the fixture is ABSENT or the check FAILS: `raise SystemExit`/`RuntimeError` BEFORE any figure is
  written (S3: "demo not gated" pitfall). Print the measured max|diff| for both scalp and diff wave.

COMPUTE ROUTING: the gate is LAPTOP (fixture committed; torch-vs-frozen-array, NO MATLAB). Do not
route the gate to M3.

`from __future__ import annotations`; float64; NumPy docstrings citing `spm_gen_erp.m`/`spm_lx_erp.m`
+ the Phase-35 LEAD-05 gate by author/year (David & Friston 2003) inline (NO `[REF-xxx]`). ruff/format/
mypy clean.
  </action>
  <verify>
LAPTOP: run `python scripts/demo_mmn_precision_sweep.py` with the fixture present — the gate prints the scalp + diff-wave max|diff| (both <=1e-7) and proceeds. Temporarily point the fixture path at a missing file (or simulate failure) and confirm the script RAISES before any figure file is created (no `figures/mmn_precision_sweep.*` written on gate failure). `ruff check`/`format --check` clean.
  </verify>
  <done>
The demo refuses to produce any sweep output unless the fixed-reference SPM forward-parity check (scalp + difference wave vs the frozen LFP fixture) is green within <=1e-7; on missing fixture or failure it raises before writing a figure.
  </done>
</task>

<task type="auto">
  <name>Task 2: The precision SWEEP + acceptance assertions (ERPDCM-04 part A)</name>
  <files>scripts/demo_mmn_precision_sweep.py</files>
  <action>
After the gate passes, implement the pure-forward sweep in `main()`:
- `net = build_mmn_5source_network()`; build `l_full` via the adapter bundle.
- PERMUTATION GUARD (reuse Phase-33): before sweeping, assert perturbing the FREE `P.G[:,0]` changes
  the parameterized `G[:,6]` (not `G[:,0]`) — the sweep hits the right knob (S1).
- Sweep `gain` over a coarse grid (e.g. `np.linspace(...)`, <=12 points): for each gain,
  `bundle = mmn_cmc_params(sp_inhibition_gain=gain, a1_b_gain=0.5, rifg_b_gain=0.5)`;
  `sim = simulate_erp_dcm(bundle["p"], bundle["x_design"], 5, l_full=bundle["l_full"])`;
  `mmn = sim["difference_wave_scalp"]` (ns,Nc); record `|MMN|` peak at the rIFG channel.
- ASSERT, in order (subtraction order pinned `deviant - standard`; `P.J = e_2` sp-voltage idx 2;
  LFP source-space readout — S2):
  1. MONOTONE attenuation: `|MMN|` peak is NON-INCREASING across increasing gain (higher
     sp self-inhibition -> lower sp gain -> smaller PE -> smaller MMN; FEATURES section 4).
  2. NEGATIVE-going: at baseline gain, `diff_wave_scalp[peak, rIFG] < 0`.
  3. FRONTAL-DOMINANT: `max|diff_wave_scalp[:,rIFG]| > max|diff_wave_scalp[:,A1L]|` and `> A1R`.
- Return the `(n_gain,)` gain array + `|MMN|` array (the transfer curve, D3) and the low/baseline/high
  difference waves for the figure.

WATCH ITEM (must measure, do NOT fake — V-discipline): confirm frontal-dominance actually emerges at
the reference params. If A1 dominates rIFG at baseline, tune `rifg_b_gain` vs `a1_b_gain`
(forward-only, allowed) to recover the Ranlund winning-model frontal dominance AND document the tuning
in the SUMMARY; OR document the channel ranking honestly. Never fabricate the sign/dominance.

COMPUTE ROUTING: a COARSE sweep (<=12 gain points) is LAPTOP-feasible (<3 min). If a finer sweep is
desired (>3 min projected), route it to M3 via a demo sbatch (mirror `cluster/sbatch/erp_pytest.sbatch`)
and flush via Mutagen first; confirm `mmn_reference.py` synced. For the headline figure, the coarse
laptop sweep suffices.

ruff/format/mypy clean.
  </action>
  <verify>
LAPTOP: `python scripts/demo_mmn_precision_sweep.py` runs the coarse sweep and the three assertions (monotone non-increasing |MMN|, negative-going rIFG diff, frontal-dominance) PASS, printing the gain->|MMN| array. The permutation-guard assertion passes (P.G[:,0] moves G[:,6]). Runtime <3 min.
  </verify>
  <done>
The pure-forward sweep over the verified precision knob produces a monotone gain->|MMN| transfer curve, a negative-going rIFG difference wave, and frontal-dominance, all machine-asserted; the right knob (G[:,6] via P.G[:,0]) is confirmed by the reused permutation guard.
  </done>
</task>

<task type="auto">
  <name>Task 3: The publication-quality figure + Zotero flag (ERPDCM-04 part B)</name>
  <files>scripts/demo_mmn_precision_sweep.py, figures/mmn_precision_sweep.png, figures/mmn_precision_sweep.pdf</files>
  <action>
Emit the figure ONLY after the gate is green and the assertions pass:
- Panel A: the `gain -> |MMN|` transfer curve (`(n_gain,)`), publication-quality.
- Panel B: low/baseline/high difference-wave overlay at the rIFG channel (peristimulus time on x).
- Save to `figures/mmn_precision_sweep.png` AND `figures/mmn_precision_sweep.pdf` via `matplotlib`
  (already a dependency). Follow the scientific-figure skill conventions (clear axis labels with
  units: gain [a.u.], |MMN| [a.u. or uV-equivalent], time [ms]; legend; readable font sizes;
  tight_layout; vector PDF). Title/caption note the LFP source-space readout + the fixed-reference
  parity gate.

CITATIONS / ZOTERO: the demo claims rest on Adams 2013 (aberrant precision / sp self-inhibition),
Ranlund 2016 (5-source MMN; winning model = intrinsic gain at rIFG + bilateral A1), Garrido 2009 (MMN
predictive-coding review), David & Friston 2003 (neural-mass + lead field). Cite these by AUTHOR/YEAR
inline in the docstring/caption. Do NOT write any `[REF-xxx]` Better-BibTeX key (not yet confirmed in
Zotero — CLAUDE.md .bib rule). In the SUMMARY, FLAG these 6 papers for the user to add to Zotero
before any keyed citation: Adams 2013 (Front Psychiatry 4:47), Ranlund 2016 (Hum Brain Mapp 37:351),
Garrido 2009 (Clin Neurophysiol 120:453), David & Friston 2003 (NeuroImage 20:1743), Bastos 2012
(Neuron 76:695), Kiebel 2006 (NeuroImage 30:1273).

ADDITIVE-ONLY: the script + the two figure files are NEW; no edits to shipped forward/leadfield/
simulator/ERPDCMForward or Phase 33/34/35 modules. ruff/format/mypy clean on the script.
  </action>
  <verify>
After a green run, `figures/mmn_precision_sweep.png` and `.pdf` exist and are non-empty; opening the PNG shows the two panels (transfer curve + difference-wave overlay) with labelled axes. `git diff --stat` lists only the script + the two figures. `ruff check`/`format --check` clean on the script.
  </verify>
  <done>
A publication-quality `figures/mmn_precision_sweep.{png,pdf}` (gain->|MMN| transfer curve + low/baseline/high difference-wave overlay) is written only after the gate + assertions pass; the 6 source papers are flagged for Zotero; no `[REF-xxx]` keys fabricated; additive-only.
  </done>
</task>

</tasks>

<verification>
- The gate runs FIRST and refuses any figure unless scalp + diff wave reproduce the frozen LFP fixture <=1e-7 (reuses `_reference_p`/`_production_scalp`; LAPTOP, no MATLAB).
- The sweep is PURE-FORWARD over the verified `P.G[:,0] -> G[:,6]` knob (permutation guard reused), via `mmn_cmc_params` + `simulate_erp_dcm`.
- Machine-asserted: monotone gain->|MMN| attenuation, negative-going rIFG diff wave, frontal-dominance (or honestly documented + forward-only tuned).
- `figures/mmn_precision_sweep.{png,pdf}` written only after green gate + assertions; scientific-figure conventions.
- Additive-only; ruff/format clean; 6 papers flagged for Zotero, NO fabricated `[REF-xxx]`.
</verification>

<success_criteria>
ERPDCM-04: `scripts/demo_mmn_precision_sweep.py` sweeps sp self-inhibition gain (P.G[:,0]->G[:,6]) at rIFG + bilateral A1 and emits a gain->|MMN| transfer curve; asserts monotone attenuation + a frontal-dominant negative-going difference wave (the Adams/Ranlund artifact), reusing the Phase-33 permutation guard.
ERPDCM-06: the MMN sweep figure is gated behind a GREEN fixed-reference SPM forward-parity check (LFP + difference wave reproduced from the frozen `spm_gen_erp`+`spm_lx_erp` fixture) BEFORE any sweep output is produced.
</success_criteria>

<output>
After completion, create `.planning/phases/36-erp-dcm-pyro-model-amortized-wiring-mmn-precision-sweep-demo/36-03-SUMMARY.md` (record: the gate's measured scalp + diff-wave max|diff|; the gain->|MMN| array; whether frontal-dominance held at baseline or required forward-only tuning; the figure paths; the 6 Zotero-flagged papers). This SUMMARY closes the v0.8.0 CAPSTONE.
</output>
