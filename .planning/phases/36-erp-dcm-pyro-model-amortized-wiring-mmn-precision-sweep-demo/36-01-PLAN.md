---
phase: 36-erp-dcm-pyro-model-amortized-wiring-mmn-precision-sweep-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pyro_dcm/models/erp_dcm_model.py
  - src/pyro_dcm/models/__init__.py
  - src/pyro_dcm/guides/parameter_packing.py
  - src/pyro_dcm/guides/summary_networks.py
  - src/pyro_dcm/guides/__init__.py
  - src/pyro_dcm/models/amortized_wrappers.py
  - tests/test_erp_dcm_model.py
  - tests/test_amortized_erp.py
  - cluster/sbatch/erp_pytest.sbatch
autonomous: true

must_haves:
  truths:
    - "A user can construct erp_dcm_model(...) and run SVI: it samples A/B/C/T/G/S/R + a noise scale and conditions a Gaussian likelihood on the flattened (Cnd,ns,Nc) scalp residual."
    - "erp_dcm_model uses simulate_erp_dcm(..., l_full=...) as its ONLY forward (the parity-gated pipeline; no second integrate->project loop)."
    - "ERPDCMPacker.unpack(pack(x)) == x bit-for-bit (identity reshape; no .exp() — CMC free params are already unconstrained), with pack order IDENTICAL to ERPDCMForward.pack_params."
    - "The amortized flow guide trains on a handful of erp_simulator draws WITHOUT error (B held FIXED, mirroring ERPDCMForward + v0.3.0-D5)."
    - "create_guide(...) auto-discovers the new sample sites (AutoNormal trace succeeds) with ZERO factory edits (MODEL-06 precedent)."
  artifacts:
    - path: "src/pyro_dcm/models/erp_dcm_model.py"
      provides: "erp_dcm_model Pyro generative model (samples 7 named sites + scalp_noise_scale; reuses simulate_erp_dcm; Gaussian obs_erp likelihood)"
      min_lines: 80
      contains: "simulate_erp_dcm"
    - path: "src/pyro_dcm/guides/parameter_packing.py"
      provides: "class ERPDCMPacker appended (pack/unpack/fit_standardization/standardize/unstandardize/n_features)"
      contains: "class ERPDCMPacker"
    - path: "src/pyro_dcm/models/amortized_wrappers.py"
      provides: "amortized_erp_dcm_model + _run_erp_forward_model appended (B fixed; reuses ERPDCMForward.predict)"
      contains: "def amortized_erp_dcm_model"
    - path: "src/pyro_dcm/guides/summary_networks.py"
      provides: "class ErpSummaryNet appended (MLP over flattened (Cnd*ns*Nc,) -> embed_dim, float64)"
      contains: "class ErpSummaryNet"
    - path: "tests/test_erp_dcm_model.py"
      provides: "structural trace test (sites discovered) + SVI-smoke (few steps) + packer round-trip"
    - path: "tests/test_amortized_erp.py"
      provides: "ERPDCMPacker round-trip + amortized flow-trains-without-error smoke"
  key_links:
    - from: "src/pyro_dcm/models/erp_dcm_model.py"
      to: "simulate_erp_dcm"
      via: "deterministic forward reuse (l_full supplied -> sim['scalp'])"
      pattern: "simulate_erp_dcm\\("
    - from: "src/pyro_dcm/models/amortized_wrappers.py"
      to: "ERPDCMForward.predict"
      via: "_run_erp_forward_model (B fixed inside forward)"
      pattern: "\\.predict\\("
    - from: "src/pyro_dcm/guides/parameter_packing.py"
      to: "ERPDCMForward.pack_params order"
      via: "ERPDCMPacker mirrors A_free(4NN)|C_free(NM)|T(4N)|G(4N)|S(N)|R(2M)"
      pattern: "A_free"
---

<objective>
Wire the Pyro generative ERP-DCM model and the amortized inference path for DCM-for-evoked-responses, reusing the parity-verified Phase 33/34/35 forward stack verbatim (ERPDCM-01, ERPDCM-02).

Purpose: Makes the CMC scalp-ERP forward a *DCM* (priors + likelihood -> SVI/VL inference) and an *amortized* model (flow guide trained on simulator draws), completing the inference-reuse story for the v0.8.0 capstone. The full SVI model samples B; the amortized path holds B fixed (matches ERPDCMForward + the v0.3.0-D5 "amortized defers B" precedent).
Output: NEW `models/erp_dcm_model.py`; APPENDED `ERPDCMPacker`, `ErpSummaryNet`, `amortized_erp_dcm_model` + `_run_erp_forward_model`; two new test files; the M3 pytest sbatch target updated.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-erp-dcm-pyro-model-amortized-wiring-mmn-precision-sweep-demo/36-RESEARCH.md

# Pyro-model idiom to mirror (B_free_{j} per-effect loop, NO pyro.plate; HalfCauchy noise; Gaussian obs)
@src/pyro_dcm/models/spectral_dcm_model.py
@src/pyro_dcm/models/task_dcm_model.py

# The forward to REUSE (never re-assemble) + the FROZEN pack order to mirror
@src/pyro_dcm/simulators/erp_simulator.py
@src/pyro_dcm/inference/forward_models.py

# Amortized + packer + summary-net siblings to mirror additively
@src/pyro_dcm/models/amortized_wrappers.py
@src/pyro_dcm/guides/parameter_packing.py
@src/pyro_dcm/guides/summary_networks.py

# Prior means/variances (transcribe scales + SPM line refs)
@src/pyro_dcm/forward_models/cmc_priors.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: erp_dcm_model.py — the Pyro generative ERP-DCM model (ERPDCM-01)</name>
  <files>src/pyro_dcm/models/erp_dcm_model.py, src/pyro_dcm/models/__init__.py</files>
  <action>
Create `models/erp_dcm_model.py` mirroring `spectral_dcm_model.py`. Signature (float64 throughout):

```python
def erp_dcm_model(
    observed_scalp,     # (Cnd, ns, Nc) float64
    a_masks,            # list of 4 x (N,N) extrinsic routing masks
    b_masks,            # list of (N,N) between-trial B masks (live positions)
    c_mask,             # (N, M)
    x_design,           # (Cnd, n_effects)
    l_full,             # (Nc, 8N) precomputed LFP lead field
    N=None,
    *, dt=0.004, ns=128, ons_ms=60.0, dur_ms=16.0, sus=0.0,
) -> None
```

Sample sites + prior scales — transcribe EXACTLY from `cmc_prior_moments`/`spm_cmc_priors.m`
(use `pyro.sample(site, dist.Normal(mean, sqrt(var)).to_event(k))`, mean=0):

| Site | shape | to_event | sqrt(var) | SPM ref | mask-DEAD value |
|------|-------|----------|-----------|---------|-----------------|
| `A_free` | (4,N,N) | 3 | (1/16)**0.5 | spm_cmc_priors.m:80-81 | **-32** (CMC dead = exp(-32)*E0~0; NOT 0) |
| `B_free_{j}` (per-effect loop, NO pyro.plate) | (N,N) | 2 | **VERIFY pC.B** — provisional (1/8)**0.5 | spm_dcm_erp B prior | **0** (B is an additive log-offset; 0 = no modulation) |
| `C_free` | (N,M) | 2 | (1/32)**0.5 | spm_cmc_priors.m:114-116 | **-32** |
| `T` | (N,4) | 2 | (1/32)**0.5 | :121 | — |
| `G` | (N,4) | 2 | (1/32)**0.5 | :122 | — |
| `S` | (N,1) | 2 | (1/64)**0.5 | :124 | — |
| `R` | (M,2) | 2 | (1/16)**0.5 | :133 | — |

LOAD-BEARING mask asymmetry: apply `a_masks`/`c_mask` by sending DEAD entries to free `-32`
(mirror `ERPDCMForward._masked_free` — a 0 free value would be a LIVE edge under `exp(P)*E0`);
B DEAD value is `0` (additive-in-log). Reuse the `task_dcm_model.py` per-effect `B_free_{j}` loop
VERBATIM (no `pyro.plate` — enables AutoGuide auto-discovery, MODEL-06).

Body after sampling + masking:
```python
p = {"T": T, "G": G, "C": c_masked, "S": S, "R": R,
     "A": [a_free_list[i] for i in range(4)], "B": B_free_list}
sim = simulate_erp_dcm(p, x_design, N, ns=ns, dt=dt, ons_ms=ons_ms,
                       dur_ms=dur_ms, sus=sus, l_full=l_full)
pred = torch.nan_to_num(sim["scalp"])          # (Cnd, ns, Nc); NaN-guard (amortized idiom)
pyro.deterministic("predicted_scalp", pred)
noise_scale = pyro.sample("scalp_noise_scale", dist.HalfCauchy(torch.tensor(1.0, dtype=torch.float64)))
pyro.sample("obs_erp",
            dist.Normal(pred.reshape(-1), noise_scale).to_event(1),
            obs=observed_scalp.reshape(-1))
```
Flatten with `.reshape(-1)` (C-order) to match the locked `(Cnd,ns,Nc)` layout used downstream.
B IS sampled here (objective requirement) — unlike `ERPDCMForward` where B is fixed.
NEVER re-assemble the forward inside the model (pitfall V1); `simulate_erp_dcm` is the only forward.

NumPy-style docstring: cite `spm_cmc_priors.m` (line refs above), `spm_gen_erp.m`, `spm_gen_Q.m`
by SPM source + author/year (David & Friston 2003; Bastos 2012) inline. Do NOT write any `[REF-xxx]`
key (not yet in Zotero — CLAUDE.md .bib rule). Flag the B-prior variance as MUST-VERIFY in the docstring
(provisional 1/8; low-stakes for the fixed-B headline demo, matters for SVI recovery).
Append `erp_dcm_model` to `models/__init__.py` exports (append-only).
`from __future__ import annotations`; line length 88; ruff/mypy clean.
  </action>
  <verify>
`python -c "from pyro_dcm.models import erp_dcm_model"` imports clean. `ruff check src/pyro_dcm/models/erp_dcm_model.py` and `ruff format --check` clean. Grep confirms `simulate_erp_dcm(` is called and there is NO second `integrate_local_linearization`/`project_to_scalp` loop in the file.
  </verify>
  <done>
`erp_dcm_model` samples A_free/B_free_{j}/C_free/T/G/S/R + scalp_noise_scale, masks A/C to -32 and B to 0, delegates the forward to `simulate_erp_dcm(..., l_full=...)`, and conditions `obs_erp ~ Normal(pred.reshape(-1), noise_scale)` on the flattened observed scalp. Exported from `models/__init__.py`.
  </done>
</task>

<task type="auto">
  <name>Task 2: ERPDCMPacker + ErpSummaryNet (ERPDCM-02 part A)</name>
  <files>src/pyro_dcm/guides/parameter_packing.py, src/pyro_dcm/guides/summary_networks.py, src/pyro_dcm/guides/__init__.py</files>
  <action>
APPEND `class ERPDCMPacker` to `parameter_packing.py`, mirroring `TaskDCMPacker`/`SpectralDCMPacker`
but for the FROZEN ERPDCMForward order (B EXCLUDED — fixed in the amortized path):
```
n_features = 4*N*N + N*M + 4*N + 4*N + N + 2*M
order      = A_free(4NN) | C_free(NM) | T(4N) | G(4N) | S(N) | R(2M)
unpack shapes: A_free (4,N,N) | C_free (N,M) | T (N,4) | G (N,4) | S (N,1) | R (M,2)
```
LOG-SPACE CONTRACT (state explicitly in the docstring): unlike `TaskDCMPacker` (which `log()`s
`noise_prec`), EVERY CMC free param is already unconstrained (`P.*` free log-params) — pack/unpack
are IDENTITY reshapes, NO `.exp()` at unpack, no positive-constrained scalar. Mirror `pack_params`/
`unpack_params` of `ERPDCMForward` EXACTLY so a packed latent <-> VL theta are interchangeable.
Provide `fit_standardization(dataset)`, `standardize`, `unstandardize`, `n_features` identical in
surface to the sibling packers (NSF spline needs ~zero-mean/unit-var input).

APPEND `class ErpSummaryNet` to `summary_networks.py`: a small MLP over the flattened scalp
`(Cnd*ns*Nc,) -> embed_dim` (default 128, float64), matching the `BoldSummaryNet` surface
(`embed_dim` attribute, `forward(x) -> (..., embed_dim)`). The flow guide
`AmortizedFlowGuide(ErpSummaryNet(...), packer.n_features, packer=packer)` then needs ZERO changes.

Append `ERPDCMPacker`, `ErpSummaryNet` to `guides/__init__.py` exports (append-only).
NumPy docstrings; `from __future__ import annotations`; ruff/mypy clean.
  </action>
  <verify>
`python -c "from pyro_dcm.guides import ERPDCMPacker, ErpSummaryNet"` clean. `ruff check`/`format --check` clean on both files. A quick `python` snippet: build `ERPDCMPacker(N=2, M=1)`, assert `n_features == 4*4 + 2 + 8 + 8 + 2 + 2`.
  </verify>
  <done>
`ERPDCMPacker` round-trips a random theta (`unpack(pack(x)) == x`) as identity reshapes with no exp/log; `n_features` matches `ERPDCMForward.param_count`; `ErpSummaryNet` maps a flattened scalp vector to `embed_dim` floats. Both exported.
  </done>
</task>

<task type="auto">
  <name>Task 3: amortized_erp_dcm_model + tests + M3 sync-confirm (ERPDCM-02 part B)</name>
  <files>src/pyro_dcm/models/amortized_wrappers.py, tests/test_erp_dcm_model.py, tests/test_amortized_erp.py, cluster/sbatch/erp_pytest.sbatch</files>
  <action>
APPEND `amortized_erp_dcm_model` + `_run_erp_forward_model` to `amortized_wrappers.py`, mirroring
`amortized_task_dcm_model` + `_run_task_forward_model`:
```python
def amortized_erp_dcm_model(observed_scalp, a_masks, b_masks, c_mask, x_design, l_full,
                            forward: ERPDCMForward, packer: ERPDCMPacker,
                            *, dt=0.004, ns=128, ...):
    params = _sample_latent_and_unpack(packer)   # A_free,C_free,T,G,S,R (unconstrained)
    theta = forward.pack_params(**params)        # reuse the frozen pack order
    _run_erp_forward_model(forward, theta, observed_scalp)
```
`_run_erp_forward_model` calls `forward.predict(theta, observed_scalp, N)` (the parity-gated forward;
B FIXED inside `forward`), NaN-guards (`torch.nan_to_num`), then
`pyro.sample("obs_erp", dist.Normal(pred_flat, noise_std).to_event(1), obs=observed_scalp.reshape(-1))`.
Construct `forward = ERPDCMForward(l_full, x_design, a_masks, b_masks, c_mask, ...)` ONCE OUTSIDE the
model so `b_masks` stay fixed. Do NOT edit `ERPDCMForward.predict` or `variational_laplace.py`
(B stays fixed in amortized — pitfall "amortized B-packing scope creep").

Tests (`tests/test_erp_dcm_model.py`):
- structural trace: `pyro.poutine.trace(erp_dcm_model).get_trace(...)` on a small 2-source net
  discovers sites `A_free`, `B_free_0`, `C_free`, `T`, `G`, `S`, `R`, `scalp_noise_scale`, `obs_erp`.
- `create_guide` auto-discovery: build `AutoNormal(erp_dcm_model)` and confirm it traces without a
  factory edit (MODEL-06).
- SVI-smoke: a FEW steps (<=5) on a tiny 1-source/2-source synthetic `erp_simulator` draw; assert
  the ELBO is finite and decreases or stays finite (LAPTOP, <30s — keep it tiny).

Tests (`tests/test_amortized_erp.py`):
- `ERPDCMPacker` round-trip `unpack(pack(x)) == x` (LAPTOP).
- amortized flow-trains-without-error: build `AmortizedFlowGuide(ErpSummaryNet(...), packer.n_features,
  packer=packer)` + `amortized_erp_dcm_model`, run a handful of SVI steps on a few `erp_simulator`
  draws; assert no exception and finite loss. This is the >30s jacrev path -> mark for M3
  (see COMPUTE ROUTING below); on laptop keep it a 1-source minimal smoke if collected.

COMPUTE ROUTING (per the cluster rule the user reaffirmed): packer round-trip + structural trace are
LAPTOP (<30s). Any REAL SVI/amortized fit (jacrev x 128 steps x 2 conditions) routes to M3 via
`cluster/sbatch/erp_pytest.sbatch` with `TEST_TARGET=tests/test_amortized_erp.py` (and add the two
new Phase-36 test files to the default `TEST_TARGET` line in the sbatch — append-only).

MUTAGEN models/ FOOTGUN (load-bearing): `src/pyro_dcm/models/erp_dcm_model.py` lives under
`src/pyro_dcm/models/` and the unanchored `models/` mutagen ignore can SILENTLY exclude it from M3
sync. BEFORE any M3 pytest/fit run: confirm the new file landed on M3
(`ssh m3 'ls <repo>/src/pyro_dcm/models/erp_dcm_model.py'`) OR `scp` it as a stopgap; anchor the ignore
(`/models/` not `models/`) if it is missing. Record the confirmation in the SUMMARY.

`from __future__ import annotations`; ruff/format/mypy clean; ADDITIVE-ONLY (verify
`git diff --stat` touches only the listed files + the named appends; Phase 33/34/35 modules + the
`ForwardModel` protocol BYTE-UNTOUCHED).
  </action>
  <verify>
LAPTOP: `pytest tests/test_erp_dcm_model.py tests/test_amortized_erp.py -x -q` — structural trace, AutoNormal auto-discovery, packer round-trip, and the tiny SVI-smoke PASS (<30s slice). M3 (the >30s amortized-fit slice): `sbatch cluster/sbatch/erp_pytest.sbatch` with `TEST_TARGET=tests/test_amortized_erp.py` returns exit 0 AFTER confirming `erp_dcm_model.py` is present on M3. `git diff --stat` lists only the plan's files. `ruff check` + `mypy` deltas only the documented `pyro_dcm.*` import-untyped + numpy-stub baseline.
  </verify>
  <done>
The amortized flow guide trains on `erp_simulator` draws without error (B fixed, via `ERPDCMForward.predict`); the SVI Pyro model traces + runs a few steps with all sites discovered by `create_guide`; the packer round-trips; the M3 sync-confirm step is executed and recorded; everything is additive-only.
  </done>
</task>

</tasks>

<verification>
- `python -c "from pyro_dcm.models import erp_dcm_model; from pyro_dcm.guides import ERPDCMPacker, ErpSummaryNet; from pyro_dcm.models.amortized_wrappers import amortized_erp_dcm_model"` imports clean.
- `erp_dcm_model` reuses `simulate_erp_dcm` (grep) with NO re-assembled forward; amortized path reuses `ERPDCMForward.predict`.
- ERPDCMPacker pack order == ERPDCMForward.pack_params (A_free|C_free|T|G|S|R); identity reshapes (no exp).
- LAPTOP suite green (<30s); M3 amortized-fit slice exit 0 after the models/-sync-confirm.
- Additive-only: Phase 33/34/35 modules + ForwardModel protocol byte-untouched; their suites still green.
- ruff/format clean; mypy delta only the documented baseline.
</verification>

<success_criteria>
ERPDCM-01: `models/erp_dcm_model.py` Pyro generative model with log-normal priors (A,B,C,T,G,S,R), Gaussian scalp-residual likelihood, B modulation — consistent with the `spectral_dcm_model` idiom and reusing the parity-gated forward.
ERPDCM-02: `ERPDCMPacker` + `amortized_erp_dcm_model` appended additively; the amortized flow guide trains on `erp_simulator` draws without error; B held fixed in the amortized path.
</success_criteria>

<output>
After completion, create `.planning/phases/36-erp-dcm-pyro-model-amortized-wiring-mmn-precision-sweep-demo/36-01-SUMMARY.md` (record: the verified B-prior variance or that it stayed provisional 1/8; the M3 models/-sync-confirm result; any flagged Zotero papers).
</output>
