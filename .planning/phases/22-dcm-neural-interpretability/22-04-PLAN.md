---
phase: 22-dcm-neural-interpretability
plan: 04
type: execute
wave: 2
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - src/pyro_dcm/neural_data_models/latent_extraction.py
  - tests/test_neural_data_latent_extraction.py
  - tests/test_synthetic_pipeline.py
autonomous: true

must_haves:
  truths:
    - "extract_latent_trajectories passes trained LSTM-AE data through encoder to get (n_samples, T, N_latent) trajectories"
    - "compute_latent_csd computes CSD from latent trajectories at MEG frequencies (1-45 Hz)"
    - "Synthetic end-to-end test: generate from known A -> train LSTM-AE -> extract latents -> compute CSD -> fit spectral DCM -> get posterior A"
    - "CSD from latent trajectories has correct shape (F, N_latent, N_latent) and is Hermitian"
  artifacts:
    - path: "src/pyro_dcm/neural_data_models/latent_extraction.py"
      provides: "extract_latent_trajectories and compute_latent_csd functions"
      contains: "def extract_latent_trajectories"
    - path: "tests/test_neural_data_latent_extraction.py"
      provides: "Unit tests for latent extraction and CSD computation"
      min_lines: 60
    - path: "tests/test_synthetic_pipeline.py"
      provides: "End-to-end synthetic pipeline integration test"
      min_lines: 80
  key_links:
    - from: "src/pyro_dcm/neural_data_models/latent_extraction.py"
      to: "src/pyro_dcm/neural_data_models/lstm_autoencoder.py"
      via: "extract_latent_trajectories calls model.encode()"
      pattern: "model\\.encode"
    - from: "src/pyro_dcm/neural_data_models/latent_extraction.py"
      to: "src/pyro_dcm/forward_models/csd_computation.py"
      via: "compute_latent_csd calls compute_empirical_csd"
      pattern: "compute_empirical_csd"
    - from: "tests/test_synthetic_pipeline.py"
      to: "src/pyro_dcm/models/spectral_dcm_model.py"
      via: "Fits spectral_dcm_model to latent CSD"
      pattern: "spectral_dcm_model"
---

<objective>
Create latent dynamics extraction from trained LSTM-AE and validate the complete
synthetic pipeline from simulated timeseries through DCM fitting.

Purpose: After the LSTM autoencoder is trained (Plan 02), we need to extract latent
trajectories and compute their cross-spectral density for spectral DCM fitting. This
plan also creates the critical synthetic end-to-end integration test that proves the
entire pipeline works on data with known ground truth. This test is the primary risk
reduction for the phase -- if it passes, we know the pipeline can recover connectivity
from an autoencoder's latent space.

Output: latent_extraction.py with extraction + CSD utilities, unit tests, and a
synthetic integration test that runs the full pipeline on simulated data from a known A.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-dcm-neural-interpretability/22-RESEARCH.md

@src/pyro_dcm/neural_data_models/lstm_autoencoder.py (from Plan 22-02)
@src/pyro_dcm/simulators/meg_simulator.py (from Plan 22-03)
@src/pyro_dcm/forward_models/csd_computation.py
@src/pyro_dcm/forward_models/spectral_transfer.py
@src/pyro_dcm/models/spectral_dcm_model.py
@src/pyro_dcm/models/guides.py
@.planning/phases/22-dcm-neural-interpretability/22-01-SUMMARY.md
@.planning/phases/22-dcm-neural-interpretability/22-02-SUMMARY.md
@.planning/phases/22-dcm-neural-interpretability/22-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create latent extraction and CSD computation utilities</name>
  <files>src/pyro_dcm/neural_data_models/latent_extraction.py, src/pyro_dcm/neural_data_models/__init__.py, tests/test_neural_data_latent_extraction.py</files>
  <action>
1. Create `src/pyro_dcm/neural_data_models/latent_extraction.py`:

   a. `extract_latent_trajectories(model: MEGAutoencoder, data: Tensor | DataLoader, *,
      device: str = "cpu", batch_size: int = 64) -> np.ndarray`:
      - If data is a Tensor, wraps it in a DataLoader with batch_size
      - Sets model.eval(), uses torch.no_grad()
      - Iterates over batches, calls model.encode(batch.to(device))
      - Concatenates all latent outputs: shape (n_samples, T, N_latent)
      - Returns as numpy array (detached from graph)

   b. `compute_latent_csd(latent_trajectories: np.ndarray, *, sfreq: float = 250.0,
      fmin: float = 1.0, fmax: float = 45.0, n_freqs: int = 64,
      average_over_samples: bool = True) -> dict[str, np.ndarray]`:
      - latent_trajectories shape: (n_samples, T, N_latent) or (T, N_latent)
      - If 3D and average_over_samples=True: average CSD across samples
      - If 3D and average_over_samples=False: compute CSD per sample
      - If 2D: single trajectory, compute CSD directly
      - For each sample, call compute_empirical_csd(sample, fs=sfreq, freqs=freqs)
      - Returns dict: {"csd": np.ndarray, "freqs": np.ndarray, "sfreq": float, "n_latent": int}

   c. `prepare_for_spectral_dcm(csd_result: dict) -> dict[str, torch.Tensor]`:
      - Converts numpy CSD dict to torch tensors for spectral DCM
      - Returns {"csd": torch.tensor(complex128), "freqs": torch.tensor(float64),
        "a_mask": torch.ones(N, N, float64)}

2. Update __init__.py to export all three functions.

3. Create tests/test_neural_data_latent_extraction.py:
   a. test_extract_latent_trajectories_shape: (10, 100, 6) input, n_latent=12 -> (10, 100, 12)
   b. test_extract_latent_trajectories_from_dataloader: Same but with DataLoader input
   c. test_extract_latent_trajectories_eval_mode: Model is in eval mode during extraction
   d. test_compute_latent_csd_2d_input: (T, N) -> (F, N, N)
   e. test_compute_latent_csd_3d_averaged: (n, T, N) with average=True -> (F, N, N)
   f. test_compute_latent_csd_3d_per_sample: (n, T, N) with average=False -> (n, F, N, N)
   g. test_compute_latent_csd_hermitian: CSD[f, i, j] == conj(CSD[f, j, i])
   h. test_prepare_for_spectral_dcm_dtypes: csd is complex128, freqs is float64, a_mask is float64
  </action>
  <verify>
- `pytest tests/test_neural_data_latent_extraction.py -v` passes all 8 tests
- `ruff check src/pyro_dcm/neural_data_models/latent_extraction.py tests/test_neural_data_latent_extraction.py`
  </verify>
  <done>
- extract_latent_trajectories produces numpy array of latent dynamics
- compute_latent_csd produces CSD at MEG frequencies from latent trajectories
- prepare_for_spectral_dcm converts to torch tensors for spectral_dcm_model
- 8 unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Synthetic end-to-end integration test</name>
  <files>tests/test_synthetic_pipeline.py</files>
  <action>
Create tests/test_synthetic_pipeline.py -- a comprehensive integration test that
proves the FULL pipeline works on synthetic data with known ground truth.

Test structure:

1. `test_synthetic_pipeline_end_to_end`:
   - Generate synthetic timeseries from known A:
     * A = make_sensorimotor_A(seed=42) -- 10 regions
     * data = generate_meg_dataset(A, n_train=100, n_val=25, duration=4.0, sfreq=250.0)
     * This gives us ground-truth A and timeseries shaped (100, 1000, 10)

   - Train MEGAutoencoder: n_roi=10, n_latent=20, hidden_size=64, 30 epochs
     Verify: final loss < initial loss (training converges)

   - Extract latent trajectories: shape (100, 1000, 20)

   - Compute latent CSD: freqs 1-45 Hz, 64 bins, averaged over samples
     Verify: shape (64, 20, 20), Hermitian, auto-spectra non-negative

   - Prepare for spectral DCM: convert to torch tensors

   - Fit spectral DCM with MEG-adapted parameters:
     * Use spectral_dcm_model with prior_a_var=1/16, eig_clamp=-1.0
     * N_latent = 20 regions in the DCM (the latent space IS the DCM state space)
     * Use create_guide with init_scale=0.01
     * Run run_svi for 200 steps (smoke test, not full convergence)
     * Verify: ELBO decreases (loss goes down), no NaN in posterior A

   This test should take < 3 minutes on laptop. Mark with @pytest.mark.slow.

2. `test_raw_vs_latent_csd_shapes`:
   - Compute CSD directly from the raw synthetic timeseries (10-channel)
   - Compute CSD from latent trajectories (20-channel)
   - Verify: raw CSD is (64, 10, 10), latent CSD is (64, 20, 20)
   - Both are Hermitian with non-negative auto-spectra

Use `from __future__ import annotations`, NumPy docstrings, full imports.
  </action>
  <verify>
- `pytest tests/test_synthetic_pipeline.py -v` passes both tests
- `ruff check tests/test_synthetic_pipeline.py`
- No regressions: `pytest tests/test_spectral.py tests/test_meg_spectral_dcm.py tests/test_lstm_autoencoder.py tests/test_meg_simulator.py -v`
  </verify>
  <done>
- Synthetic pipeline test proves end-to-end flow: generate from known A -> train LSTM-AE ->
  extract latents -> compute CSD -> fit spectral DCM
- Spectral DCM converges on latent CSD (ELBO decreases, no NaN)
- Raw vs latent CSD shape comparison validates dimension expansion
  </done>
</task>

</tasks>

<verification>
- All 10 tests pass (8 unit + 2 integration)
- Synthetic pipeline test confirms full flow works with known ground truth
- No regressions in existing tests
- ruff clean on all new files
</verification>

<success_criteria>
- Latent extraction produces correctly-shaped trajectories from trained LSTM-AE
- CSD computation on latent trajectories produces valid Hermitian CSD at MEG frequencies
- End-to-end synthetic test proves: known A -> timeseries -> LSTM-AE -> latents -> CSD -> spectral DCM -> posterior A
- The pipeline recovers a posterior A matrix (convergence verified, no NaN)
</success_criteria>

<output>
After completion, create `.planning/phases/22-dcm-neural-interpretability/22-04-SUMMARY.md`
</output>
