---
plan_id: "13-04"
phase: 13-bilinear-neural-state
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true

must_haves:
  truths:
    - "CLAUDE.md directory-tree no longer claims a 'generative_models/' folder — updated to the actual 'models/' layout"
    - "CLAUDE.md tree lists the real files (task_dcm_model.py, spectral_dcm_model.py, rdcm_model.py, guides.py, amortized_wrappers.py)"
    - ".planning/PROJECT.md Validated line 23 no longer calls the A+Cu form 'bilinear' — it is linear"
    - "Grep for 'generative_models/' in CLAUDE.md returns zero hits after the rewrite"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Directory-tree block at line 101 replaced with actual models/ layout"
      contains: "models/"
    - path: ".planning/PROJECT.md"
      provides: "Line 23 rewritten from 'Bilinear' to 'Linear' for the A+Cu form"
      contains: "**Linear** neural state equation"
  key_links:
    - from: "CLAUDE.md"
      to: "src/pyro_dcm/models/"
      via: "directory-tree documentation now matches actual filesystem"
      pattern: "models/"
    - from: ".planning/PROJECT.md"
      to: "REQUIREMENTS.md v0.3.0 milestone"
      via: "v0.1.0 Validated line corrected so v0.3.0 bilinear entry can be added cleanly on ship"
      pattern: "\\*\\*Linear\\*\\*"
---

<objective>
Correct two documentation-drift sites that mislabel linear behavior as "bilinear"
or describe a stale directory layout. Scope is intentionally narrow and
non-overlapping with Plans 13-01/13-02/13-03 so this plan can execute in parallel
in Wave 1.

**Source-code `neural_state.py` docstring rename lives in 13-01 (Task 1).** This
plan covers the two non-source sites only: `CLAUDE.md` directory tree (line ~101)
and `PROJECT.md` v0.1.0 Validated line (line 23). Together, 13-01's source-
docstring rewrite + this plan's non-source edits close BILIN-07 / ROADMAP Phase 13
success criterion 5.

Drift sites handled here (exactly two files):

1. **`CLAUDE.md` line 101** — the directory-tree block under `src/pyro_dcm/` still
   shows `generative_models/` with files `task_dcm.py`, `spectral_dcm.py`,
   `regression_dcm.py`. The actual repo has `models/` with
   `task_dcm_model.py`, `spectral_dcm_model.py`, `rdcm_model.py`, `guides.py`,
   and `amortized_wrappers.py`. Rewrite the block to match the filesystem and
   note that v0.3.0 extends `task_dcm_model.py` with the bilinear B path.

2. **`.planning/PROJECT.md` line 23** — the v0.1.0 Validated bullet says
   "Bilinear neural state equation (dx/dt = Ax + Cu) with explicit A matrix —
   v0.1.0". That is factually incorrect: `A + Cu` is LINEAR. Rewrite to
   "**Linear** neural state equation ... — v0.1.0". The true bilinear form is
   the v0.3.0 deliverable of this phase and will be added to Validated by the
   v0.3.0 release phase (out of scope here).

This plan is docs-only — no test changes, no source edits. The source-code half of
BILIN-07 (module docstring + `NeuralStateEquation` class summary line in
`src/pyro_dcm/forward_models/neural_state.py`) is Plan 13-01's Task 1, handled
alongside the `parameterize_B` / `compute_effective_A` additions so all neural-state
edits land in one commit.

Purpose: Correct stale documentation so readers of CLAUDE.md and PROJECT.md
get an accurate model of the v0.1.0 shipping state. Unblocks the v0.3.0 release
phase from doing this cleanup later under release pressure.

Output: Two file edits (CLAUDE.md tree block, PROJECT.md Validated line). No
test runs needed (docs-only). Git commit `docs(13-04): ...`.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-bilinear-neural-state/13-CONTEXT.md
@.planning/phases/13-bilinear-neural-state/13-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md directory-tree generative_models/ block to actual models/ layout</name>
  <files>CLAUDE.md</files>
  <action>
Edit `CLAUDE.md`. Locate the `generative_models/` block at line 101 (verify with a
grep first — if line numbers have shifted, use the literal content match).

### Current content (lines 101-105 approximately)

```
│       ├── generative_models/
│       │   ├── __init__.py
│       │   ├── task_dcm.py          # Pyro model for task-based DCM
│       │   ├── spectral_dcm.py      # Pyro model for spectral DCM
│       │   └── regression_dcm.py    # Pyro model for regression DCM
```

### Replacement content

```
│       ├── models/
│       │   ├── __init__.py
│       │   ├── task_dcm_model.py       # Pyro model for task-based DCM [v0.3.0: + bilinear B path]
│       │   ├── spectral_dcm_model.py   # Pyro model for spectral DCM
│       │   ├── rdcm_model.py           # Pyro model for regression DCM
│       │   ├── guides.py               # SVI guide factory (AutoNormal/AutoLowRankMVN/AutoIAF/...)
│       │   └── amortized_wrappers.py   # Amortized task/spectral DCM wrappers
```

### Sub-tasks

1. Replace the `generative_models/` block with the `models/` block above. Preserve
   the tree-drawing characters (`│`, `├──`, `└──`) exactly as they appear in
   CLAUDE.md (they are box-drawing Unicode chars; copy the indentation pattern
   from adjacent blocks like `forward_models/`).
2. Do NOT touch the `guides/`, `connectivity/`, `simulators/`, `inference/`, or
   `utils/` blocks in this plan. CONTEXT-locked scope is the `generative_models/`
   block only. (The `inference/` block has separately drifted — code has moved
   into `models/` — but CONTEXT defers that to Phase 15 review.)
3. Do NOT touch the `# shape: (n_regions, n_timepoints)` tensor conventions table
   later in the file. CONTEXT defers that to Phase 15 when B becomes a Pyro
   sample site.

### Verification grep pattern

After the edit:
```bash
grep -n "generative_models" CLAUDE.md
```
Expected: zero hits.

```bash
grep -nE "^\│       ├── models/$" CLAUDE.md
```
Expected: one hit on the replacement block line.
  </action>
  <verify>
```bash
grep -n "generative_models" CLAUDE.md
```

Expected output: (no matches) — exit code 1 / zero lines printed.

```bash
grep -n "task_dcm_model.py" CLAUDE.md
grep -n "rdcm_model.py" CLAUDE.md
grep -n "amortized_wrappers.py" CLAUDE.md
```

Expected: each grep returns at least one match (the new tree block).

Manually read the edited block to confirm tree-drawing characters are preserved
(open `CLAUDE.md` around line 101 and visually confirm).
  </verify>
  <done>
`CLAUDE.md` directory-tree no longer mentions `generative_models/` and accurately
reflects the `src/pyro_dcm/models/` layout including `task_dcm_model.py`,
`spectral_dcm_model.py`, `rdcm_model.py`, `guides.py`, `amortized_wrappers.py`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite .planning/PROJECT.md line 23 from "Bilinear" to "**Linear**"</name>
  <files>.planning/PROJECT.md</files>
  <action>
Edit `.planning/PROJECT.md`. Locate line 23 in the "Validated" requirements list
under the "Requirements" heading. Verify with a grep if line numbers have shifted.

### Current content (line 23)

```
- Bilinear neural state equation (dx/dt = Ax + Cu) with explicit A matrix — v0.1.0
```

### Replacement content

```
- **Linear** neural state equation (dx/dt = Ax + Cu) with explicit A matrix — v0.1.0
```

### Sub-tasks

1. Replace the single line via the Edit tool. Bold formatting on `**Linear**`
   (asterisk markdown) emphasizes the correction and mirrors the style of other
   lines in the Validated list that highlight key technical words.
2. Do NOT add a new v0.3.0 line for the true bilinear form. That entry is the
   v0.3.0 release phase's responsibility (after Phase 16 acceptance passes). The
   v0.3.0 line will read:
   `- **Bilinear** neural state equation (dx/dt = (A + Σ u_j B_j)·x + Cu) with stability monitor — v0.3.0`
   Creating it now would be premature and misleading (the requirement ships when
   Phase 16 passes, not now).
3. Do NOT touch any other line in PROJECT.md. Scope is line 23 only.
  </action>
  <verify>
```bash
grep -nE "^- Bilinear neural state equation \(dx/dt = Ax \+ Cu\)" .planning/PROJECT.md
```

Expected: zero hits (the old text is gone).

```bash
grep -nE "\*\*Linear\*\* neural state equation \(dx/dt = Ax \+ Cu\)" .planning/PROJECT.md
```

Expected: exactly one hit on the new line.
  </verify>
  <done>
`.planning/PROJECT.md` line 23 now reads "**Linear** neural state equation
(dx/dt = Ax + Cu) with explicit A matrix — v0.1.0" and no line still calls the
`A + Cu` form "bilinear".
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit plan 13-04 work</name>
  <files>(git commit)</files>
  <action>
Stage the two edited docs and commit.

```bash
git add CLAUDE.md .planning/PROJECT.md
git commit -m "docs(13-04): correct stale doc drift (BILIN-07 non-source sites)

Two non-source doc-rename sites per CONTEXT.md §Doc-rename scope
(the source-code half landed in Plan 13-01):

1. CLAUDE.md directory-tree (~line 101): replace the stale
   'generative_models/' block with the actual 'models/' layout.
   Lists the real files: task_dcm_model.py, spectral_dcm_model.py,
   rdcm_model.py, guides.py, amortized_wrappers.py. Annotates
   task_dcm_model.py as the v0.3.0 bilinear-B entrypoint for
   future readers.

2. .planning/PROJECT.md line 23: rewrite the v0.1.0 Validated
   line from 'Bilinear neural state equation (dx/dt = Ax + Cu)'
   to '**Linear** neural state equation (dx/dt = Ax + Cu)'.
   The A+Cu form is LINEAR, not bilinear; the true bilinear form
   is the v0.3.0 Phase 13 deliverable and will be added to
   Validated when Phase 16 ships.

Docs-only. No source or test changes. No overlap with plans
13-01/13-02/13-03 (different files), enabling Wave 1 parallelism."
```
  </action>
  <verify>
```bash
git log -1 --stat
git status
```

Expected: one new commit with exactly two files touched (`CLAUDE.md` and
`.planning/PROJECT.md`). Working tree clean.
  </verify>
  <done>
Commit `docs(13-04): ...` exists; `git status` is clean.
  </done>
</task>

</tasks>

<verification>
Final phase-level verification for Plan 13-04:

```bash
# Confirm both drift sites are gone
grep -c "generative_models" CLAUDE.md  # expect 0
grep -cE "^- Bilinear neural state equation" .planning/PROJECT.md  # expect 0

# Confirm new content is present
grep -c "models/" CLAUDE.md  # expect >= 1
grep -cE "\*\*Linear\*\* neural state equation" .planning/PROJECT.md  # expect 1

# Sanity: this plan has no test impact, but confirm no accidental source edits
git log -1 --stat | grep -E "\.(py|txt|toml)$"  # expect no matches; only .md files
```

Expected: all grep counts match the stated expectations; no `.py`/`.toml`/`.txt`
files in the last commit's stat.
</verification>

<success_criteria>
- [x] `CLAUDE.md` no longer contains the word `generative_models` and shows the
      actual `models/` directory layout.
- [x] `.planning/PROJECT.md` line 23 reads `**Linear** neural state equation ...`
      and no line calls the `A + Cu` form "bilinear".
- [x] No source files edited (confirmed by `git log -1 --stat`).
- [x] Commit `docs(13-04): ...` recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/13-bilinear-neural-state/13-04-SUMMARY.md`.
Record: two docs edits, both drift sites resolved, zero source/test changes, Wave
1 parallel execution with 13-01 was possible (no file conflicts).
</output>
