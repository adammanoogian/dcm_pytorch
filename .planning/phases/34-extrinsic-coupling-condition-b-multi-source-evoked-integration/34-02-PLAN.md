---
phase: 34-extrinsic-coupling-condition-b-multi-source-evoked-integration
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - validation/export_to_mat.py
  - validation/matlab_scripts/run_spm_erp_dcm_multisource.m
  - cluster/scripts/erp_cross_validation.py
  - cluster/sbatch/erp_cross_validation_multisource.sbatch
autonomous: false

must_haves:
  truths:
    - "The exact 5-source MMN reference A/B/C masks + per-condition design X are LOCKED in export_erp_dcm_multisource / DCM.meta BEFORE the M3 run (fixture regeneration is a reviewed change, V4); MNI coords are NOT hard-coded here (Phase 36)"
    - "export_erp_dcm_multisource writes P.A{1..4} (N,N) and P.B{..} (N,N) as MATLAB cell arrays, U.X (Cnd, n_effects) as double, M.x=zeros(N,8), M.n=8N (double), M.f='spm_fx_cmc_nodelay' (int64->double spm_Ce footgun avoided, Phase 32 a27828b)"
    - "run_spm_erp_dcm_multisource.m forces D=1 via M.f='spm_fx_cmc_nodelay' and ASSERTS nargout(M.f)==2 AND isequal(M.x, zeros(N,8)) before generating fixtures (EVOK-06)"
    - "The fixture exports per-condition spm_gen_Q outputs QA{c} (cell of 4 (N,N)) and QG{c} ((N,) = Q.G(:,1)), per-condition frozen J0{c} (spm_diff FD, (8N,8N)) and Qupd{c} (spm_expm right-division), and the spm_gen_erp multi-source trajectory y{c} ((ns,8N))"
    - "A provenance meta header records SPM $Id, dt, ns, ons, dur, D=1, nargout_Mf==2, x0==zeros(N,8), N, the exact edge list, and X (V4)"
    - "The orchestrator submits the sbatch to M3 and harvests validation/data/erp_multisource_fixtures.mat (MATLAB license unreachable locally; ssh unlocked at orchestrator level)"
  artifacts:
    - path: "validation/export_to_mat.py"
      provides: "export_erp_dcm_multisource (the 5-source DCM input .mat writer)"
      contains: "def export_erp_dcm_multisource"
    - path: "validation/matlab_scripts/run_spm_erp_dcm_multisource.m"
      provides: "spm_gen_Q QA/QG + per-condition J0/Qupd + spm_gen_erp trajectory fixtures"
      contains: "spm_gen_Q"
    - path: "cluster/scripts/erp_cross_validation.py"
      provides: "multisource mode (export -> matlab -batch -> round-trip check -> JSON record)"
      contains: "multisource"
    - path: "cluster/sbatch/erp_cross_validation_multisource.sbatch"
      provides: "M3 entrypoint (comp/16G/1h, no pip) for the multi-source fixtures"
      contains: "spm_fx_cmc_nodelay"
    - path: "validation/data/erp_multisource_fixtures.mat"
      provides: "the frozen 5-source MATLAB ground truth (QA, QG, J0, Qupd, y, meta)"
  key_links:
    - from: "export_erp_dcm_multisource"
      to: "run_spm_erp_dcm_multisource.m"
      via: "DCM .mat with P.A/P.B cells + U.X + M.f='spm_fx_cmc_nodelay'"
      pattern: "spm_fx_cmc_nodelay"
    - from: "run_spm_erp_dcm_multisource.m"
      to: "erp_multisource_fixtures.mat"
      via: "save QA QG J0 Qupd y meta"
      pattern: "save"
    - from: "cluster/scripts/erp_cross_validation.py (multisource)"
      to: "matlab -batch run_spm_erp_dcm_multisource"
      via: "subprocess record-don't-crash"
      pattern: "subprocess"
---

<objective>
Generate the frozen multi-source MATLAB ground-truth fixtures that the Wave-3 parity ladder
asserts the pure-torch network forward against — the ONLY MATLAB/M3 piece of Phase 34.
Lock the canonical 5-source auditory-MMN reference (bilateral A1, bilateral STG, rIFG)
edge list — A/B/C masks + the per-condition between-trial design X — into the exporter and
`DCM.meta` BEFORE the run (V4), extend `export_to_mat.py` with a multi-source path, write
the MATLAB fixture generator (reusing the `spm_fx_cmc_nodelay.m` D=1 wrapper UNCHANGED), and
submit on M3.

Purpose: Parity is vs-SPM, never vs-torch (pitfall V1). This produces the per-condition
`spm_gen_Q` Q.A/Q.G arrays (the B-wiring guard), the per-condition frozen Jacobian + update
operator (the scheme-rung anchor), and the `spm_gen_erp` multi-source trajectory — all with
delays forced off (D=1) and x0==zeros(N,8) asserted in the script.
Output: `export_erp_dcm_multisource`, `run_spm_erp_dcm_multisource.m`, the cluster
multisource mode + sbatch, and the harvested `erp_multisource_fixtures.mat`.

NOTE TO EXECUTOR: any compute that touches MATLAB runs on M3 (local FlexLM unreachable).
Tasks 1-2 are laptop authoring (Python + MATLAB source, no execution). Task 3 is the M3
submit+harvest checkpoint — the ORCHESTRATOR submits the sbatch and harvests the .mat (ssh
is unlocked at the orchestrator level); this is NOT a user-action gate.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-extrinsic-coupling-condition-b-multi-source-evoked-integration/34-RESEARCH.md
@.planning/phases/34-extrinsic-coupling-condition-b-multi-source-evoked-integration/34-01-SUMMARY.md

# The single-source bridge this MIRRORS additively (do not edit the single-source paths):
@validation/export_to_mat.py
@validation/matlab_scripts/run_spm_erp_dcm.m
@validation/matlab_scripts/spm_fx_cmc_nodelay.m
@cluster/scripts/erp_cross_validation.py
@cluster/sbatch/erp_cross_validation.sbatch
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the 5-source reference + extend export_to_mat.py with export_erp_dcm_multisource</name>
  <files>validation/export_to_mat.py</files>
  <action>
    APPEND `export_erp_dcm_multisource(...)` to `export_to_mat.py` (the existing
    `export_erp_dcm` and all other exporters byte-untouched). Mirror its savemat
    conventions: scalars `np.array([[v]])`, strings `np.array([[...]], dtype=object)`,
    ALL dimensions cast to float64 (the int64->double `spm_Ce` footgun, Phase 32 a27828b /
    decision 32-03). Use the SAME P/A/B/X schema 34-01-SUMMARY locked (keys T,G,C,S,R; A as
    length-4 list of (N,N); B as list of (N,N); X as (Cnd, n_effects)).

    **LOCK the canonical 5-source auditory-MMN reference (topology only — NO MNI coords,
    those are Phase 36).** Sources, index 0..4: A1L, A1R, STGL, STGR, rIFG. Encode as
    explicit (5,5) boolean/strength masks (34-RESEARCH.md Open Q3):
      - Forward A{1} (sp->ss) & A{2} (sp->dp): A1L->STGL, A1R->STGR, STGL->rIFG,
        STGR->rIFG.
      - Backward A{3} (dp->sp) & A{4} (dp->ii): the reverse — rIFG->STGL, rIFG->STGR,
        STGL->A1L, STGR->A1R.
      - Lateral reciprocal (triggers 1+4L): STGL<->STGR (a clean reciprocal test pair).
      - Input C: drives A1L and A1R only (bilateral auditory recipients); the C mask is
        (5, n_inp) with non-zero rows at A1L/A1R. Sources with no input get `P.C ~
        mask*32-32` so `exp(P.C) ~ 0`.
      - Condition B: deviant X=[1] vs standard X=[0]. A single (5,5) `B{1}` on the
        forward/backward edges, with NON-ZERO diag(B) at rIFG + bilateral A1 (the precision
        nodes). X = `[[0.0],[1.0]]` (Cnd=2, n_effects=1).
    Build the free log-param `P` from these masks: `P.A` = length-4 list of (5,5) (zeros
    where the mask is off; small finite log-values where on); `P.B` = `[B1]` (5,5);
    `P.C` = (5, n_inp); `P.T/P.G/P.S/P.R` per the prior-mean shapes. Keep the exact numeric
    values modest and deterministic; document each edge in a comment.

    Encode `P.A{1..4}` and `P.B{..}` as MATLAB CELL arrays:
    `np.empty((1,4), dtype=object)` filled with the four (N,N) float64 blocks (mirror how
    the task exporter encodes B cells). `U.X` = (Cnd, n_effects) float64. `M.x` =
    zeros(N,8); `M.n` = 8N (double); `M.m` = n_inp; `M.f` = 'spm_fx_cmc_nodelay';
    `M.ons/M.dur/M.sus` = 60/16/0 (explicit, pitfalls N3/N4). Integration grid ns=128,
    dt=0.004. Store the locked edge list + X + the exact P in `DCM.meta` (provenance, V4)
    plus `meta.N=5`, `meta.D=1`. Default `output_path =
    "validation/data/erp_multisource_input.mat"`. Return the meta dict for logging.

    Add a tiny laptop round-trip assertion (in a `if __name__` block or a small pytest in
    an existing validation test, executor's choice — must run on laptop): load the written
    .mat back with `scipy.io.loadmat` and assert P.A is a 1x4 cell, each block (5,5);
    U.X is (2,1); M.f decodes to 'spm_fx_cmc_nodelay'; M.x is zeros(5,8).
  </action>
  <verify>`python -c "from validation.export_to_mat import export_erp_dcm_multisource as e;
  m = e(); print(m)"` writes `validation/data/erp_multisource_input.mat` and prints the
  meta. `scipy.io.loadmat` round-trip confirms the cell shapes (P.A 1x4 of (5,5), P.B cell
  of (5,5)), U.X (2,1), M.x zeros(5,8), M.f=='spm_fx_cmc_nodelay'. `ruff check` clean; the
  single-source `export_erp_dcm` test (if any) still green.</verify>
  <done>The 5-source reference A/B/C + X is locked in code + DCM.meta; the multisource DCM
  input .mat is written with correct cell/double encodings; the single-source exporter is
  byte-untouched (git diff shows append-only).</done>
</task>

<task type="auto">
  <name>Task 2: Write run_spm_erp_dcm_multisource.m + the cluster multisource mode + sbatch</name>
  <files>validation/matlab_scripts/run_spm_erp_dcm_multisource.m, cluster/scripts/erp_cross_validation.py, cluster/sbatch/erp_cross_validation_multisource.sbatch</files>
  <action>
    **MATLAB (`run_spm_erp_dcm_multisource.m`):** Mirror `run_spm_erp_dcm.m` scaffolding
    (SPM12 path setup via `getenv('SPM12_PATH')` + fallback, `spm('defaults','EEG')`,
    addpath `toolbox/dcm_meeg` + `Neural_Models`, loud try/catch with `fprintf ERROR` +
    `return`, `getenv('DCM_INPUT_PATH')`/`DCM_OUTPUT_PATH` IO). Load `DCM`, set
    `M.f='spm_fx_cmc_nodelay'`, `M.x=zeros(N,8)`, `M.n=8*N`, `M.m=size(DCM.U.u,2)`. ASSERT
    `nargout(M.f)==2` (the D=1 routing proof) AND `isequal(M.x, zeros(N,8))` (M1), exactly
    as `run_spm_erp_dcm.m:83-89`. Then (34-RESEARCH.md per-file spec):

      % (1) per-condition spm_gen_Q (the B-wiring guard, EVOK-05 part 1)
      for c = 1:size(DCM.U.X,1)
          Qc = spm_gen_Q(P, DCM.U.X(c,:));
          QA{c} = Qc.A;        % cell of 4 (N,N) free-log-space blocks (spm_gen_Q:47)
          QG{c} = Qc.G(:,1);   % (N,) free precision column           (spm_gen_Q:66)
      end
      % (2) per-condition frozen Jacobian + update operator (SCHEME-rung anchor, Fact 5)
      for c = 1:size(DCM.U.X,1)
          Qc   = spm_gen_Q(P, DCM.U.X(c,:));
          x0   = spm_vec(M.x); u0 = sparse(M.m,1);
          J0c  = full(spm_cat(spm_diff(@spm_fx_cmc, x0, u0, Qc, M, 1)));  % (8N,8N) FD
          dfdx = J0c - eye(8*N)*exp(-16);
          Qupd{c} = (spm_expm(DCM.U.dt*dfdx) - eye(8*N)) / dfdx;          % right-division
          J0{c} = J0c;
      end
      % (3) the multi-source evoked trajectory (EVOK-05 part 2)
      y = spm_gen_erp(P, M, DCM.U);     % cell {Cnd} of (ns, 8N)
      save(output_path,'QA','QG','J0','Qupd','y','meta');

    Build the provenance `meta` exactly like the single-source script: `spm('Ver')`, the
    `$Id` of spm_int_L/spm_fx_cmc/spm_gen_Q/spm_gen_erp/spm_cmc_priors/spm_erp_u,
    `meta.D=1`, `meta.nargout_Mf=nargout(M.f)`, `meta.N`, `meta.edges` (the locked edge
    list from DCM.meta), `meta.X=DCM.U.X`, `meta.dt/ns/ons/dur`, `meta.x0=zeros(N,8)`.
    Default output `validation/data/erp_multisource_fixtures.mat`.

    **Cluster Python (`erp_cross_validation.py`):** APPEND a multisource mode mirroring the
    existing single-source `main()` (export -> `matlab -batch
    run_spm_erp_dcm_multisource` -> round-trip shape/meta check -> JSON record;
    record-don't-crash, exit 0 on soft miss, non-zero only on unexpected MATLAB/IO failure,
    per 31-03-D3). Select via an `argparse --mode {single,multisource}` flag (default
    single, so the existing entrypoint behaviour is unchanged) OR an env var; call
    `export_erp_dcm_multisource` and point `DCM_INPUT_PATH`/`DCM_OUTPUT_PATH` at the
    multisource paths. The fixture round-trip check should assert QA is a 1xCnd cell, QG
    present, J0/Qupd (8N,8N), y a 1xCnd cell of (ns,8N), and `meta.D==1`,
    `meta.nargout_Mf==2`.

    **sbatch (`erp_cross_validation_multisource.sbatch`):** Copy
    `erp_cross_validation.sbatch` (comp / 16G / 1h / 4 cpus, `source cluster/lib/
    cluster_env.sh`, `crlf_guard`, `activate_env`, NO pip — concurrent resolvers OOM the
    shared env), export the verified `MATLAB_PATH=/usr/local/matlab/r2022a/bin/matlab` +
    `SPM12_PATH=/home/aman0087/fc37/Carrick/spm12`, `mkdir -p` the dirs, and invoke
    `python cluster/scripts/erp_cross_validation.py --mode multisource`. Header comment
    documents D=1 via the 2-output wrapper + record-don't-crash.
  </action>
  <verify>Laptop static checks only (no MATLAB locally): `ruff check
  cluster/scripts/erp_cross_validation.py` clean; `python cluster/scripts/
  erp_cross_validation.py --help` shows the `--mode` flag; `octave --version` not required.
  MATLAB syntax sanity by inspection against `run_spm_erp_dcm.m`. The single-source sbatch +
  default cluster mode are byte-unchanged (git diff append-only on the Python; new sbatch is
  a new file).</verify>
  <done>The MATLAB generator, the cluster multisource mode, and the new sbatch exist and are
  internally consistent with the locked .mat; D=1 is asserted (`nargout(M.f)==2`) and x0==0
  in the script; the single-source path is untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The multi-source fixture toolchain: `export_erp_dcm_multisource` (locked
  5-source A/B/C/X), `run_spm_erp_dcm_multisource.m` (spm_gen_Q QA/QG + per-condition
  J0/Qupd + spm_gen_erp trajectory, D=1 + x0==0 asserted), and
  `erp_cross_validation_multisource.sbatch`. Everything is staged for the M3 run; nothing
  MATLAB has executed yet (local FlexLM unreachable).</what-built>
  <how-to-verify>
  The ORCHESTRATOR runs this on M3 (ssh unlocked at orchestrator level — this is an
  orchestrator submit+harvest, NOT a user action):
  1. Sync the repo to M3 (Mutagen, per project workflow — never git push/pull for code).
  2. `sbatch cluster/sbatch/erp_cross_validation_multisource.sbatch`; wait for completion
     (minutes; well under the 1h wall).
  3. Confirm the SLURM job exited 0 and `cluster/results/erp_cross_validation_*.json`
     records `checks_pass=true`, `meta.D==1`, `meta.nargout_Mf==2`, `N==5`, `x0==zeros`.
  4. Harvest `validation/data/erp_multisource_fixtures.mat` back to the laptop (Mutagen),
     and COMMIT it to git (validation/data/ is mutagen-ignored, so the byte-frozen .mat
     must live in the repo for the Wave-3 laptop gate — same pattern as the single-source
     fixture in 33-02).
  5. Spot-check with `scipy.io.loadmat`: QA is 1x2 cell (each a 1x4 cell of (5,5)), QG
     1x2, J0/Qupd (40,40), y 1x2 of (128,40), and `meta` carries the SPM $Id strings +
     the locked edge list.
  If the MATLAB run soft-fails (shape/meta mismatch recorded, exit 0): inspect the JSON +
  SLURM .err, fix the exporter/script, re-run. A non-zero exit means an unexpected MATLAB/
  IO failure — read `cluster/logs/erp_xval_*.err`.
  </how-to-verify>
  <resume-signal>Type "harvested" once `erp_multisource_fixtures.mat` is committed to git
  and the loadmat spot-check passes, or describe the mismatch to fix.</resume-signal>
</task>

</tasks>

<verification>
- `export_erp_dcm_multisource()` writes a DCM input .mat whose loadmat round-trip shows
  P.A 1x4 cell of (5,5), P.B cell of (5,5), U.X (2,1) double, M.x zeros(5,8),
  M.f=='spm_fx_cmc_nodelay', and a DCM.meta with the locked edge list + X + N=5 + D=1.
- The M3 job produces `validation/data/erp_multisource_fixtures.mat` with QA{c} (cell of 4
  (5,5)), QG{c} ((5,)), J0{c}/Qupd{c} ((40,40)), y{c} ((128,40)), and a provenance meta
  (SPM $Id, dt, ns, ons, dur, D=1, nargout_Mf==2, x0==zeros(5,8), N=5, edges, X).
- The fixture is committed to git (Wave-3 asserts against it on the laptop).
- The single-source `export_erp_dcm` / `run_spm_erp_dcm.m` / default cluster mode / single
  sbatch are byte-unchanged (git diff append-only on shared files; new files otherwise).
</verification>

<success_criteria>
- EVOK-05 (fixture half): the frozen 5-source ground truth exists — per-condition
  `spm_gen_Q` QA/QG, per-condition frozen J0/Qupd, and the `spm_gen_erp` multi-source
  trajectory y — generated under MATLAB R2022a + Carrick spm12 on M3.
- EVOK-06: delays forced off — `M.f='spm_fx_cmc_nodelay'` makes `nargout(M.f)==2`, ASSERTED
  in the MATLAB script alongside `isequal(M.x, zeros(N,8))`; `meta.D==1` recorded.
- V4 provenance frozen; the reference A/B/C/X locked before the run; MNI coords deferred to
  Phase 36.
</success_criteria>

<output>
After completion, create `34-02-SUMMARY.md`. Record: the exact locked edge list (the 5x5
A/B/C masks + X), the SPM `$Id` strings captured in `meta`, the harvested fixture array
shapes (QA, QG, J0, Qupd, y), the committed .mat path, and confirmation that
`nargout(M.f)==2` + `x0==zeros(5,8)` asserted green on M3 — so Wave 3 asserts against a
provenance-pinned fixture.
</output>
