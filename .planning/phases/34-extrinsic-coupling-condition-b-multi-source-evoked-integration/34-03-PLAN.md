---
phase: 34-extrinsic-coupling-condition-b-multi-source-evoked-integration
plan: 03
type: execute
wave: 3
depends_on: ["34-01", "34-02"]
files_modified:
  - tests/test_spm_erp_multisource_validation.py
autonomous: true

must_haves:
  truths:
    - "apply_condition_modulation reproduces SPM's per-condition spm_gen_Q QA{c} (all four (N,N) blocks) AND QG{c} (the diag(B)->Q.G[:,0] precision column) element-wise <=1e-12 — the single most important B-wiring guard, EVOK-05 part 1 / C4"
    - "A NEGATIVE assertion: an apply_condition_modulation variant that omits the diag(B)->G[:,0] line produces a Q.G[:,0] that does NOT match QG{c} (proves the precision path is load-bearing, EVOK-02)"
    - "Network frozen J0 — built with a spm_diff-matched forward-difference Jacobian (dx=exp(-8)) of cmc_network_f at x0=0 — matches J0{c} <=1e-10, proving cmc_network_f IS spm_fx_cmc at N>1 with the Jacobian method held to SPM's (inherited gate split, Fact 5)"
    - "Network Q_update via _update_operator(J0{c}, dt, 1, None) matches Qupd{c} <=1e-9 (right-division at network scale, C2)"
    - "The multi-source trajectory splits into 3 rungs: SCHEME rung (drive the exp-Euler loop with SPM's OWN Qupd{c}) ~1e-13; FD-Jacobian rung (operator from the spm_diff-matched J0) <=1e-8; shipped-jacrev rung (full integrate_local_linearization on cmc_network_f) MEASURED + recorded, NOT gated at 1e-8 (expected >4.7e-8, exact-AD is more accurate than SPM — Fact 5, not a bug)"
    - "Pre-asserts read meta.D==1, meta.nargout_Mf==2, x0==zeros(8N), float64 — the gate is element-wise forward agreement only (no absolute-F, no Cp; V2)"
  artifacts:
    - path: "tests/test_spm_erp_multisource_validation.py"
      provides: "the V5 staged multi-source parity ladder vs the frozen erp_multisource_fixtures.mat (EVOK-05)"
      contains: "def test_"
  key_links:
    - from: "test_spm_erp_multisource_validation.py"
      to: "validation/data/erp_multisource_fixtures.mat"
      via: "scipy.io.loadmat (asserts torch-vs-frozen-MATLAB, never torch-vs-torch; V1)"
      pattern: "loadmat"
    - from: "the network J0 rung"
      to: "_spm_diff_jacobian(lambda v: cmc_network_f(v,u0,Q_c,N), x0, dx=exp(-8))"
      via: "reuse the Phase-33 helper to hold the Jacobian method to SPM's"
      pattern: "_spm_diff_jacobian"
    - from: "the scheme rung"
      to: "fixture Qupd{c}"
      via: "exp-Euler loop driven by SPM's own update operator (isolates loop ordering)"
      pattern: "Qupd"
---

<objective>
The Phase-34 SPM12 parity GATE (EVOK-05): a V5 staged ladder that proves the pure-torch
network forward + condition-B mechanism are bit-close to SPM at N>1, asserting torch against
the frozen multi-source MATLAB fixtures (`erp_multisource_fixtures.mat`) — never torch
vs torch (V1). The assertions are deterministic float64 against committed arrays, so the
suite RUNS AND PASSES on the laptop (the laptop run IS authoritative, exactly as Phase
33-03 decision D1), with `@pytest.mark.spm`/`slow` retained for an optional M3 re-run.

Purpose: localise any network-coupling divergence to ONE stage (V5). The ladder rungs are
asserted IN ORDER: `spm_gen_Q` algebra (the B-wiring guard + the diag->G negative test) ->
network J0 (FD-matched) -> network Q_update -> multi-source trajectory (scheme + FD-Jacobian
rungs + a measured shipped-jacrev floor). The trajectory gate inherits the settled Phase
33-03 split: do NOT gate the shipped exact-AD `jacrev` integrator at <=1e-8 against an SPM
`spm_diff`-FD-Jacobian fixture — that is a documented numerical-method divergence, not a bug.
Output: `tests/test_spm_erp_multisource_validation.py`.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-extrinsic-coupling-condition-b-multi-source-evoked-integration/34-RESEARCH.md
@.planning/phases/34-extrinsic-coupling-condition-b-multi-source-evoked-integration/34-01-SUMMARY.md
@.planning/phases/34-extrinsic-coupling-condition-b-multi-source-evoked-integration/34-02-SUMMARY.md

# REUSE the Phase-33 ladder structure + the FD-Jacobian helper + the integrator:
@tests/test_spm_erp_dcm_validation.py
@src/pyro_dcm/utils/local_linearization.py
@src/pyro_dcm/forward_models/erp_coupled_system.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: spm_gen_Q algebra + diag->G negative + network J0 + Q_update rungs</name>
  <files>tests/test_spm_erp_multisource_validation.py</files>
  <action>
    Create the test mirroring `tests/test_spm_erp_dcm_validation.py` structure: a
    `_FIXTURE_PATH` pointing at `validation/data/erp_multisource_fixtures.mat`, a
    `pytestmark` of `[pytest.mark.spm, pytest.mark.slow,
    pytest.mark.skipif(not _FIXTURE_PATH.exists(), ...)]` (gate on FIXTURE availability,
    not MATLAB — decision 33-03-D1), a `fx` fixture that `scipy.io.loadmat`s the arrays, and
    a `_reference_p()` that reconstructs the EXACT free-param `P` (T,G,C,S,R,A,B) the
    Wave-2 exporter locked (read it from `meta` / 34-02-SUMMARY so torch and MATLAB feed the
    identical P). REUSE the Phase-33 helpers by importing or copying `_spm_diff_jacobian(f,
    x0, dx=exp(-8))` and `_update_operator`. Unpack MATLAB cells carefully (loadmat returns
    nested object arrays: `QA[c]` is a 1x4 cell of (5,5); `y` is 1xCnd of (128,40)).

    **Pre-asserts** (one test): `meta.D==1`, `meta.nargout_Mf==2`, `N==5`,
    `x0==zeros(8N)`, fixture arrays are float64.

    **Rung 1 — spm_gen_Q algebra (the B-wiring guard, C4 / EVOK-05 part 1).** For each
    condition c, `Q = apply_condition_modulation(P, X[c])`:
      - Assert `Q["A"][j]` matches `QA[c][j]` element-wise (max|diff| <=1e-12) for all four
        j (free log-space blocks, spm_gen_Q:47).
      - Assert `Q["G"][:,0]` matches `QG[c]` element-wise (max|diff| <=1e-12) (the
        diag(B)->Q.G[:,0] precision column, spm_gen_Q:66).

    **Rung 1b — diag->G NEGATIVE assertion (EVOK-02).** Define a local
    `_apply_no_diag(P, X_c)` that folds B into A{1..4} but SKIPS the diag(B)->G[:,0] line;
    assert its `Q.G[:,0]` does NOT match `QG[c]` for the deviant condition (X=[1]) — the
    difference is exactly `X*diag(B0)`, proving the precision path is load-bearing and
    cannot be omitted.

    **Rung 2 — network J0 (FD-matched), <=1e-10.** For each c: `Q_c =
    apply_condition_modulation(P, X[c])`; `x0 = zeros(8N)`; `u0 = zeros(n_inp)`;
    `J_fd = _spm_diff_jacobian(lambda v: cmc_network_f(v, u0, Q_c, N), x0, dx=exp(-8))`;
    assert `max|J_fd - J0[c]| <= 1e-10`. This proves `cmc_network_f` IS `spm_fx_cmc` at N>1
    with the Jacobian-construction method held to SPM's (Fact 5; do NOT compare exact
    `jacrev` to the FD fixture — that floor is ~5.6e-4 and is the inherited, documented
    divergence).

    **Rung 3 — network Q_update, <=1e-9.** For each c: `_, q =
    _update_operator(torch.as_tensor(J0[c]), dt, 1, None)`; assert `max|q - Qupd[c]| <=1e-9`
    (right-division at network scale, C2; reuses the production operator builder).

    Record each rung's measured max|diff| in the test output (print or assert message), as
    Phase 33-03 did.
  </action>
  <verify>`pytest tests/test_spm_erp_multisource_validation.py -q -k "pre_asserts or
  gen_q or j0 or q_update"` — green on the laptop against the committed fixture (or SKIPPED
  with a clear reason if the .mat is absent). The diag->G negative rung passes (asserts a
  MISMATCH). `ruff check` clean.</verify>
  <done>The spm_gen_Q algebra rungs (QA all-four-blocks + QG, <=1e-12), the diag->G negative
  assertion, the network J0 (FD-matched, <=1e-10), and the network Q_update (<=1e-9) all
  pass against the frozen MATLAB fixture; pre-asserts confirm D==1, nargout_Mf==2,
  x0==zeros, float64.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-source trajectory ladder (scheme + FD-Jacobian + measured jacrev floor)</name>
  <files>tests/test_spm_erp_multisource_validation.py</files>
  <action>
    Append the trajectory rungs (EVOK-05 part 2). Build the shared evoked drive once from
    P.R / meta (the Gaussian grid is condition-independent), and `x0_flat = zeros(8N)`.
    For each condition c, with `Q_c = apply_condition_modulation(P, X[c])`:

    **Rung 4 — SCHEME rung (bit-exact, isolates C1 loop ordering), ~1e-13.** Drive the
    exp-Euler loop with SPM's OWN per-condition update operator: `v = x0_flat.clone()`; for
    each time-step i, `v = v + torch.as_tensor(Qupd[c]) @ cmc_network_f(v, inputs[i], Q_c,
    N)`; collect `v` -> traj (ns, 8N). Assert `max|traj - y[c]| <= 1e-12` (target ~1e-13;
    anchor to the Phase-33 scheme floor 6.6e-14 as a small multiple). This isolates the
    loop ordering / scheme from the Jacobian method.

    **Rung 5 — FD-Jacobian rung, <=1e-8.** Build the operator from the spm_diff-matched J0:
    `J_fd = _spm_diff_jacobian(lambda v: cmc_network_f(v, u0, Q_c, N), x0_flat,
    dx=exp(-8))`; `_, q = _update_operator(J_fd, dt, 1, None)`; integrate with that frozen
    `q` (same loop as rung 4 but with `q` instead of the fixture Qupd); assert `max|traj -
    y[c]| <= 1e-8`. This is the end-to-end integrator with the Jacobian method matched to
    SPM's.

    **Rung 6 — shipped-jacrev rung (MEASURED, recorded, NOT gated at 1e-8).** Run the full
    production integrator: `traj = integrate_local_linearization(lambda v, u:
    cmc_network_f(v, u, Q_c, N), x0_flat, inputs, dt)`; MEASURE `max|traj - y[c]|` and
    record it (print + assert against a LOOSE ceiling only, e.g. `<= 1e-5`, with a comment
    that the expected floor is >4.7e-8 because exact `jacrev` is MORE accurate than SPM's
    `spm_diff` FD Jacobian — Fact 5 / decision 33-03-D2/D3; this is documented, not a bug).
    Do NOT gate this rung at 1e-8.

    Keep all assertions element-wise forward agreement (V2 — no absolute-F, no Cp). Assert
    the rungs IN ORDER so a failure localises (V5). Add a short module docstring summarising
    the 3-way trajectory gate split + the inherited Phase-33 Jacobian-method carry-forward,
    citing 34-RESEARCH.md Fact 5 and 33-03-SUMMARY decisions D2/D3.
  </action>
  <verify>`pytest tests/test_spm_erp_multisource_validation.py -q` — the full ladder green
  on the laptop (scheme rung ~1e-13, FD-Jacobian rung <=1e-8, shipped-jacrev rung recorded
  under the loose ceiling). The measured jacrev floor is printed. `ruff check` +
  `ruff format --check` clean. The whole suite runs in seconds (5 sources x 2 conditions x
  128 steps x 40x40 matrix_exp is sub-second).</verify>
  <done>The multi-source evoked trajectory matches `spm_gen_erp` on the scheme rung
  (~1e-13) and the FD-Jacobian rung (<=1e-8); the shipped exact-AD integrator floor is
  MEASURED and recorded (not gated), transparently documenting the inherited
  jacrev-vs-spm_diff divergence. EVOK-05 is fully gated.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_spm_erp_multisource_validation.py -q` — all rungs green on the laptop
  against the committed `erp_multisource_fixtures.mat` (or cleanly SKIPPED if absent),
  asserted in order: pre-asserts -> spm_gen_Q algebra (QA + QG <=1e-12) -> diag->G negative
  -> network J0 (FD-matched <=1e-10) -> network Q_update (<=1e-9) -> trajectory scheme
  (~1e-13) -> trajectory FD-Jacobian (<=1e-8) -> trajectory shipped-jacrev (measured,
  recorded).
- Every parity assertion loads a frozen MATLAB array (V1 — no torch-vs-torch masquerading
  as a gate). Element-wise forward agreement only (V2 — no absolute-F, no Cp).
- The Phase-33 single-source ladder + the new Wave-1 structural suite remain green
  (`pytest tests/test_spm_erp_dcm_validation.py tests/test_erp_coupled_system.py -q`).
- `git diff --stat` shows ONLY the one new test file (no source edits).
</verification>

<success_criteria>
- EVOK-05 (PARITY GATE): torch `Q.A{1..4}` and `Q.G[:,0]` match exported MATLAB
  element-wise (<=1e-12), AND the multi-source evoked trajectory matches `spm_gen_erp` for
  the 5-source MMN reference (delays off, D=1) within <=1e-8 on the FD-Jacobian rung and
  ~1e-13 on the scheme rung; the shipped-jacrev floor is recorded.
- EVOK-02 negative test: the diag->G omission is asserted to break the QG match (the
  precision path is load-bearing).
- EVOK-06: pre-asserts confirm `meta.D==1` and `meta.nargout_Mf==2` (delays forced off) and
  `x0==zeros(8N)`.
- The inherited Phase 33-03 Jacobian-method gate split is honoured (scheme + FD-Jacobian
  gated; exact-AD jacrev measured, not gated).
</success_criteria>

<output>
After completion, create `34-03-SUMMARY.md`. Record the measured max|diff| per rung (the
deliverable numbers, as 33-03 did): QA, QG, J0 (FD), Q_update, trajectory scheme,
trajectory FD-Jacobian, and the MEASURED shipped-jacrev trajectory floor — plus confirmation
the diag->G negative rung fails-as-designed when omitted. This pins the Phase-34 network
forward as bit-close to SPM and clears Phase 35 (lead field) to build on it.
</output>
