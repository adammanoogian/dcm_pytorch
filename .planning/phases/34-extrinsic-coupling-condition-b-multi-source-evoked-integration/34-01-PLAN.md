---
phase: 34-extrinsic-coupling-condition-b-multi-source-evoked-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pyro_dcm/forward_models/erp_coupled_system.py
  - src/pyro_dcm/simulators/erp_simulator.py
  - tests/test_erp_coupled_system.py
  - src/pyro_dcm/forward_models/__init__.py
  - src/pyro_dcm/simulators/__init__.py
autonomous: true

must_haves:
  truths:
    - "apply_condition_modulation folds B additively in log-space into all four A{1..4} AND adds diag(B) to free G column 0 (the precision path), porting spm_gen_Q.m:41-67 (EVOK-02)"
    - "Omitting the diag(B)->G[:,0] line is a load-bearing change: a guard test perturbs diag(B) and asserts G[:,6] (sp self-inhibition) moves, AND an omit-diag variant does NOT move it (EVOK-02 negative test)"
    - "cmc_network_f(n=1) reproduces the frozen Phase-33 cmc_f bit-exactly (A=zeros -> the 4 extrinsic terms vanish); the proven single-source forward is untouched"
    - "Forward coupling reads superficial-pyramidal voltage firing S[:,2] (into ss + dp conductance), backward reads deep-pyramidal voltage firing S[:,6] (into sp + ii conductance), with structural signs + (fwd) / - (bwd) (EVOK-01)"
    - "Input C drives spiny-stellate only; lateral (1+4L) reciprocal reduction triggers on a 2-source reciprocal pair and no-ops on a one-way pair (EVOK-01)"
    - "simulate_erp_dcm loops conditions via the spm_gen_erp pattern, re-freezing the Jacobian per condition through the Phase-33 integrator, and returns a per-condition source-LFP dict + deviant-minus-standard difference-wave hook (EVOK-03/04)"
    - "The network forward is float64-guarded (TypeError on non-f64), mirroring cmc_f"
  artifacts:
    - path: "src/pyro_dcm/forward_models/erp_coupled_system.py"
      provides: "parameterize_cmc_network, apply_condition_modulation, cmc_network_f"
      contains: "def cmc_network_f"
    - path: "src/pyro_dcm/simulators/erp_simulator.py"
      provides: "simulate_erp_dcm (per-condition evoked loop + difference-wave hook)"
      contains: "def simulate_erp_dcm"
    - path: "tests/test_erp_coupled_system.py"
      provides: "C4 diag->G guard + omit-diag negative test + structural guards (n=1 bit-exact, lateral 1+4L, C->ss-only, fwd S[:,2]/bwd S[:,6] adjacency, float64)"
      contains: "def test_"
  key_links:
    - from: "apply_condition_modulation"
      to: "parameterize_cmc_network -> G[:,6]"
      via: "diag(P.B[i]) added to free G col 0, then exp via J_PERM[0]==6"
      pattern: "diag"
    - from: "cmc_network_f"
      to: "cmc_neural_mass.cmc_f intrinsic body"
      via: "duplicated EOM + 4 extrinsic A@S terms; bit-exact at n=1"
      pattern: "cmc_sigmoid|cmc_flatten|cmc_unflatten"
    - from: "simulate_erp_dcm"
      to: "integrate_local_linearization"
      via: "per-condition closure f_c = lambda v,u: cmc_network_f(v,u,Q_c,n)"
      pattern: "integrate_local_linearization"
---

<objective>
Build the pure-torch hierarchical-CMC network core that lifts the Phase-33 single-source
forward to an N-source network, ENTIRELY on the laptop with sub-second tests. Three new
symbols in one new module: `parameterize_cmc_network` (extrinsic A-block `exp*E` + lateral
`(1+4L)` reduction), `apply_condition_modulation` (the `spm_gen_Q` port: B folded
additively in log-space into all four A blocks AND into the `diag(B)->G[:,6]` precision
path), and `cmc_network_f` (the Phase-33 intrinsic EOM plus the four extrinsic `A@S`
terms). Plus `simulate_erp_dcm` (the `spm_gen_erp` condition loop) and the structural guard
battery — with the C4 `diag(B)->G[:,6]` guard + omit-diag negative test WRITTEN FIRST,
because it IS the MMN precision mechanism the whole milestone exists to demonstrate.

Purpose: This is the network forward + the entire condition-B mechanism, isolated from the
integrator and from MATLAB so the B-wiring / coupling logic is caught in isolation before
it compounds through `spm_int_L` (pitfall V5). Phase-33 proved `cmc_f` == `spm_fx_cmc` and
the exp-Euler scheme == `spm_int_L` bit-exact; this plan composes those frozen artifacts
additively, so any later divergence localises to the new coupling code.
Output: `erp_coupled_system.py`, `erp_simulator.py`, `test_erp_coupled_system.py`, and the
two `__init__.py` re-exports.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-extrinsic-coupling-condition-b-multi-source-evoked-integration/34-RESEARCH.md

# The frozen Phase-33 forward + integrator + input this plan COMPOSES (do not edit):
@src/pyro_dcm/forward_models/cmc_neural_mass.py
@src/pyro_dcm/forward_models/cmc_priors.py
@src/pyro_dcm/forward_models/erp_input.py
@src/pyro_dcm/utils/local_linearization.py
@src/pyro_dcm/simulators/spectral_simulator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the C4 guard tests FIRST (RED) + the structural guard battery</name>
  <files>tests/test_erp_coupled_system.py</files>
  <action>
    Author the laptop-only (no MATLAB) structural guard suite BEFORE implementing the
    module. These reference functions that do not yet exist, so the suite is RED on first
    run — that is the point (the C4 guard is THE precision mechanism; it must drive the
    implementation). Import from `pyro_dcm.forward_models.erp_coupled_system`
    (`parameterize_cmc_network`, `apply_condition_modulation`, `cmc_network_f`) and from the
    frozen `cmc_neural_mass` (`cmc_f`, `cmc_flatten`, `cmc_unflatten`, `J_PERM`, `E0`).

    Write these tests (all float64):

    1. **C4 PRECISION GUARD (the headline).** Build a free-param dict `P` for n=2 with
       `B=[B0]` where `B0` is a (2,2) matrix with a NON-ZERO diagonal (e.g. diag [0.3, 0.5])
       and some off-diagonal. With design row `X=[1.0]`:
       - `Q = apply_condition_modulation(P, tensor([1.0]))`.
       - Assert `Q["G"][:,0]` equals `P["G"][:,0] + 1.0 * diag(B0)` element-wise
         (spm_gen_Q.m:65-67).
       - Parameterize: `params = parameterize_cmc_network(Q, 2)`; `params_base =
         parameterize_cmc_network(P_without_X, 2)`. Assert `params["G"][:,6]` (sp
         self-inhibition, the J_PERM[0]==6 column) CHANGED vs the unmodulated baseline
         (the precision knob moved).
    2. **C4 NEGATIVE TEST (omit-diag is load-bearing, EVOK-02).** Define a local
       `_apply_condition_modulation_no_diag` that folds B into A{1..4} but SKIPS the
       `diag(B)->G[:,0]` line. Assert its `Q.G[:,0]` does NOT equal the real
       `apply_condition_modulation`'s `Q.G[:,0]` (they differ exactly by `X*diag(B0)`), and
       that the resulting `params["G"][:,6]` is UNCHANGED from baseline — proving the
       precision path is destroyed if omitted.
    3. **B->all-four-A folding (spm_gen_Q.m:41-47).** Assert `Q["A"][j]` equals
       `P["A"][j] + X*B0` for every j in 0..3 (the SAME B added to all four blocks).
    4. **cmc_network_f(n=1) == cmc_f bit-exact.** With a non-zero test state
       `x = 0.1*ones(1,8)` flattened, `u=tensor([32.0])`, and a zero free-param dict
       `{T,G,C,S}` (no A/B), assert `torch.equal(cmc_network_f(x_flat,u,p,1),
       cmc_f(x_flat,u,p,1))` (bit-exact, max|diff|==0.0). This pins single-source regression.
    5. **Lateral (1+4L) reduction (spm_fx_cmc.m:79-82).** For n=2: a RECIPROCAL pair
       (both `A[i][r,c]` and `A[i][c,r]` > exp(-8) after exp*E) is divided by 5
       (`1+4*1`); a ONE-WAY pair (only one direction non-zero) is NOT divided. Construct
       free `P.A` so block 0 has a clean reciprocal pair and assert the post-reduction
       magnitude == `exp(P.A)*E0[0] / 5` on the reciprocal entries and `exp(P.A)*E0[0]` on
       the one-way entry.
    6. **C -> spiny-stellate only.** Perturb `u` (or `P.C`) and assert only the ss
       conductance derivative row `f[ss_I=1]` changes among the four conductance rows
       (the exogenous drive enters f1/the ss numerator only).
    7. **Forward/backward adjacency (spm_fx_cmc.m:171,177,183,189).** With a known A and a
       state whose sp-voltage firing `S[:,2]` and dp-voltage firing `S[:,6]` are
       distinguishable, assert: forward terms move ss (f[1]) and dp (f[7]) conductance rows
       and are driven by `S[:,2]`; backward terms move sp (f[3]) and ii (f[5]) and are
       driven by `S[:,6]`; signs are + (fwd) and - (bwd).
    8. **float64 guard.** `cmc_network_f` raises `TypeError` on a float32 `x_flat`.

    Cite the SPM line refs inline in test docstrings (spm_fx_cmc.m:68-82,171-198;
    spm_gen_Q.m:41-67). 0-indexed map: ss_I=1, sp_I=3, ii_I=5, dp_I=7; firing cols
    sp-V=2, dp-V=6.
  </action>
  <verify>`pytest tests/test_erp_coupled_system.py -q` runs and FAILS with ImportError /
  AttributeError (module/functions not yet implemented) — confirming the tests are RED and
  reference the intended API. `ruff check tests/test_erp_coupled_system.py` clean.</verify>
  <done>The guard suite exists, is syntactically valid, imports the intended API surface,
  and fails only because the implementation is absent (RED). The C4 precision guard + the
  omit-diag negative test are both present.</done>
</task>

<task type="auto">
  <name>Task 2: Implement erp_coupled_system.py (turn the guards GREEN)</name>
  <files>src/pyro_dcm/forward_models/erp_coupled_system.py, src/pyro_dcm/forward_models/__init__.py</files>
  <action>
    Create `erp_coupled_system.py` composing the frozen `cmc_neural_mass` (import
    `parameterize_cmc`, `cmc_sigmoid`, `cmc_flatten`, `cmc_unflatten`, `J_PERM`, `E0`).
    Module docstring cites `spm_fx_cmc.m:68-82,171-198` and `spm_gen_Q.m:41-67` (CLAUDE.md
    rule 2). `from __future__ import annotations`; float64 (`_F64 = torch.float64`); NumPy
    docstrings. Do NOT import `parameterize_A`. Do NOT edit `cmc_neural_mass.py`.

    Three functions:

    1. `parameterize_cmc_network(p, n) -> dict` — reuse `parameterize_cmc(p, n)` for
       T/G/C/S, then OVERRIDE the A path: for i in 0..3,
       `A[i] = exp(p["A"][i]) * E0[i]` (E0 is the frozen [200,100,200,100]); then the
       lateral reduction (spm_fx_cmc.m:79-82): `L = (A[i] > exp(-8)) & (A[i].T > exp(-8))`
       (an (n,n) boolean), `A[i] = A[i] / (1 + 4*L.to(_F64))`. `p["A"]` is a length-4
       list/stack of (n,n) free log-params (default `zeros(4,n,n)` -> at n=1 the scalars are
       0, `0>exp(-8)` is False, reduction is a no-op, A stays effectively
       `exp(0)*E0=E0`... NOTE: for the n=1 bit-exact guard, A is multiplied into the
       extrinsic terms which are `A@S`; the GUARD passes because at n=1 with default
       `p["A"]` ABSENT the function must return `A = zeros(4,1,1)` exactly as `cmc_f`
       expects — so when `"A"` key is MISSING from p, return `zeros(4,n,n)` like
       `parameterize_cmc` does; only build the `exp*E` blocks when `p["A"]` is PRESENT).
       Returns `{"T":(n,4),"G":(n,10),"C":(n,n_inp),"A":(4,n,n),"S":(n,1)}`.
    2. `apply_condition_modulation(p, x_design) -> dict` — port `spm_gen_Q.m:24-67` in
       free/log space, PRE-exp. Copy `p` (deep-copy the tensors), drop any `"B"` key from
       the returned Q. For each `i, Xi` in enumerate(x_design): for j in 0..3,
       `Q["A"][j] = Q["A"][j] + Xi * p["B"][i]` (spm_gen_Q.m:47, SAME B{i} to all four);
       and `Q["G"][:,0] = Q["G"][:,0] + Xi * diag(p["B"][i])` (spm_gen_Q.m:65-67, the
       precision path; `diag` is the (n,) self-connection vector). C-effect
       (spm_gen_Q.m:35-37) ONLY if `p["C"].ndim == 3 and p["C"].shape[-1] >= 2` else skip
       (the reference net uses single-page C). Do NOT implement the NMDA AN/BN or M/int
       branches (absent in the plain CMC reference). `p["A"]` must be present as a length-4
       list of (n,n) free log-params for B-folding; if absent, treat as `zeros(4,n,n)`.
    3. `cmc_network_f(x_flat, u, p, n) -> Tensor` — DUPLICATE the Phase-33 `cmc_f` intrinsic
       body verbatim (the `uu_g/uu_sp/uu_ii/uu_dp` numerators, the `(uu - 2x - x/T)/T`
       kernel, the voltage integrals f0/f2/f4/f6), calling `parameterize_cmc_network(p, n)`
       instead of `parameterize_cmc`. Then ADD the four extrinsic terms INTO THE CONDUCTANCE
       NUMERATORS (inside the `1/T` scaling, exactly where spm_fx_cmc.m:171,177,183,189
       places `+A{i}*S` alongside the intrinsic G-terms and the exogenous U):
       `uu_g  += A[0] @ S[:,2]`   (fwd sp->ss, +),
       `uu_dp += A[1] @ S[:,2]`   (fwd sp->dp, +),
       `uu_sp -= A[2] @ S[:,6]`   (bwd dp->sp, -),
       `uu_ii -= A[3] @ S[:,6]`   (bwd dp->ii, -),
       where `S = cmc_sigmoid(x, p_s)` is (n,8), `A[i]` is (n,n), `A[i] @ S[:,col]` is (n,).
       Float64-guard `x_flat` with a `TypeError` mirroring `cmc_f`. At n=1 with default A
       (zeros) the four terms vanish -> bit-exact with `cmc_f` (the Task-1 guard pins this).
       Return `cmc_flatten(f)`.

    Append all three names to `forward_models/__init__.py` imports + `__all__` (re-export
    only; existing exports byte-untouched).
  </action>
  <verify>`pytest tests/test_erp_coupled_system.py -q` — guard suite GREEN (the n=1
  bit-exact guard reports max|diff| 0.0). `ruff check` + `ruff format --check` clean on the
  new module + __init__. `mypy src/pyro_dcm/forward_models/erp_coupled_system.py` clean
  (modulo the accepted pyro_dcm [import-untyped] baseline). Existing Phase-33 suite still
  green: `pytest tests/test_cmc_forward.py tests/test_local_linearization.py -q`.</verify>
  <done>All structural guards pass; the C4 precision guard + omit-diag negative test pass;
  `cmc_network_f(n=1)` is bit-exact to `cmc_f`; Phase-33 single-source tests remain green;
  only NEW symbols added to `__init__.py` (verifiable by `git diff`).</done>
</task>

<task type="auto">
  <name>Task 3: Implement erp_simulator.py (the spm_gen_erp condition loop + difference-wave hook)</name>
  <files>src/pyro_dcm/simulators/erp_simulator.py, src/pyro_dcm/simulators/__init__.py, tests/test_erp_coupled_system.py</files>
  <action>
    Create `erp_simulator.py` mirroring the `spectral_simulator.simulate_spectral_dcm`
    return-dict idiom. `from __future__ import annotations`; float64; NumPy docstrings;
    cite `spm_gen_erp.m:35,69-86` inline.

    `simulate_erp_dcm(p, x_design, n, ns=128, dt=0.004, ons_ms=60.0, dur_ms=16.0, sus=0.0)
    -> dict`:
    - `p`: free-param dict with keys T,G,C,S,R, and A (length-4 list of (n,n) free
      log-params) and B (list of (n,n)); `x_design`: (Cnd, n_effects) between-trial design.
    - Peristimulus time grid: `pst = arange(1, ns+1)*dt - ons_ms/1000` (spm_gen_erp.m:35).
    - Shared evoked drive (condition-INDEPENDENT, B does not touch P.R): `inputs =
      erp_gaussian_input(pst, p["R"], ons_ms, dur_ms, sus)` -> (ns, n_inp). Compute ONCE.
    - `x0 = cmc_steady_state(n)` -> zeros(n,8); flatten via `cmc_flatten` -> (8n,).
    - For each condition row `X_c` in `x_design`:
        `Q = apply_condition_modulation(p, X_c)`  (free-space B folding, per condition),
        `f_c = lambda v, u, Q=Q: cmc_network_f(v, u, Q, n)`  (bind Q; Jacobian re-frozen
        per condition by the integrator),
        `traj = integrate_local_linearization(f_c, x0_flat, inputs, dt)`  -> (ns, 8n),
        reshape each row back to (n,8) via `cmc_unflatten` (column-major inverse) ->
        accumulate states (Cnd, ns, n, 8).
    - Return `{"states": (Cnd, ns, n, 8), "pst": (ns,), "inputs": (ns, n_inp),
      "difference_wave": states[dev] - states[std]}`. The difference-wave hook differences
      SOURCE states (sp voltage, col 2 is the readout of interest); the scalp lead-field
      projection and the TRUE MMN difference wave are Phase 35 (LEAD-03). State this scope
      boundary explicitly in the docstring so downstream does not over-scope: Phase 34 only
      needs a non-trivial source-level difference between conditions (which exists iff B is
      wired). Treat row 0 of x_design as standard and row 1 as deviant unless Cnd<2 (then
      omit / null the difference_wave key).

    Append `simulate_erp_dcm` to `simulators/__init__.py` (re-export only).

    Add ONE smoke test to `tests/test_erp_coupled_system.py`:
    `test_simulate_erp_dcm_smoke` — run a 2-source, 2-condition reference with a non-zero B
    (wired on at least one forward edge + a non-zero diag) and assert: output shapes
    (states (2,ns,2,8), pst (ns,), inputs (ns,n_inp)); all-finite (no NaN over the full
    peristimulus window, CLAUDE.md numerical-stability rule); and the `difference_wave`
    on the sp-voltage column (col 2) is NON-zero (B is load-bearing) — but with B set to
    all-zeros the difference_wave is exactly zero (control). Keep ns small (e.g. 64) for
    sub-second runtime.
  </action>
  <verify>`pytest tests/test_erp_coupled_system.py -q` fully green incl. the new smoke test.
  `ruff check` + `ruff format --check` clean. `python -c "from pyro_dcm.simulators import
  simulate_erp_dcm"` succeeds. Full new-suite wall time < a few seconds.</verify>
  <done>`simulate_erp_dcm` produces a per-condition source-LFP dict + difference-wave hook
  over the peristimulus window via the Phase-33 integrator (one re-frozen Jacobian per
  condition); the smoke test confirms finite trajectories and that the difference wave is
  non-zero iff B is wired; re-export added.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_erp_coupled_system.py -q` — all guards green, incl. the C4 precision
  guard, the omit-diag NEGATIVE test, the `cmc_network_f(n=1)==cmc_f` bit-exact guard
  (max|diff|==0.0), lateral (1+4L), C->ss-only, fwd S[:,2]/bwd S[:,6] adjacency, float64,
  and the simulator smoke test.
- Phase-33 regression intact: `pytest tests/test_cmc_forward.py
  tests/test_local_linearization.py tests/test_spm_erp_dcm_validation.py -q` still green
  (the frozen single-source forward + integrator are untouched).
- `git diff --stat` shows ONLY: 2 new source files, 1 new test file, and append-only
  insertions to the two `__init__.py` (no edits to `cmc_neural_mass.py`,
  `local_linearization.py`, `parameterize_A`, or any existing forward/model).
- `ruff check` + `ruff format --check` + `mypy` clean (modulo the accepted pyro_dcm
  [import-untyped] baseline).
</verification>

<success_criteria>
- EVOK-01: `erp_coupled_system.cmc_network_f` wires extrinsic A — forward (sp->ss/dp, +),
  backward (dp->sp/ii, -), lateral (reciprocal `1/(1+4L)` reduction) — citing
  `spm_fx_cmc.m`; n=1 stays bit-exact to the proven `cmc_f`.
- EVOK-02: `apply_condition_modulation` folds B additively in log-space into all `A{1..4}`
  AND via `diag(B)->G[:,6]`, porting `spm_gen_Q.m`; the omit-diag path is explicitly tested
  against (negative test) and shown to destroy the precision modulation.
- EVOK-03/04: C drives spiny-stellate only; `simulate_erp_dcm` integrates the evoked window
  via the Phase-33 integrator and returns a per-condition source-LFP dict + the
  deviant-minus-standard difference-wave hook.
- All additive: existing fMRI/spectral/rDCM/CMC-single-source paths byte-identical;
  float64 throughout.
</success_criteria>

<output>
After completion, create
`.planning/phases/34-extrinsic-coupling-condition-b-multi-source-evoked-integration/34-01-SUMMARY.md`.
Record: the locked function signatures, the measured `cmc_network_f(n=1)==cmc_f` max|diff|
(must be 0.0), confirmation the C4 negative test fails-as-designed when diag->G is omitted,
and the exact `p` dict schema (keys T,G,C,S,R,A,B; A as length-4 list of (n,n); B as list of
(n,n)) so Wave 2/3 encode the same shapes.
</output>
