---
phase: 33-cmc-core-dynamics-spm-int-l-integrator-single-source-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pyro_dcm/utils/local_linearization.py
  - src/pyro_dcm/forward_models/cmc_neural_mass.py
  - src/pyro_dcm/forward_models/cmc_priors.py
  - src/pyro_dcm/forward_models/erp_input.py
  - src/pyro_dcm/forward_models/__init__.py
  - tests/test_cmc_forward.py
  - tests/test_local_linearization.py
autonomous: true

must_haves:
  truths:
    - "Perturbing P_G[:,0] changes G[:,6] (sp self-inhibition / precision) and does NOT change G[:,0] (permutation guard)"
    - "cmc_sigmoid applies S = 1/(1+exp(-R*x)) - 1/2 with R = (2/3)*exp(P.S); the -1/2 baseline subtraction is present"
    - "CMC steady state is exactly zeros(1,8); f(0,0) == 0 (no Newton solve)"
    - "The integrator computes Q via right-division torch.linalg.solve(J.T,(E-I).T).T, never torch.inverse"
    - "The exp(-16) Jacobian regulariser is applied BEFORE forming both E and Q"
    - "All CMC / integrator tensors are float64; no eigenvalue-clip is applied to the CMC Jacobian"
    - "Existing fMRI/spectral/rDCM/latent forward paths stay bit-exact (parameterize_A untouched; only NEW files + an __init__ append)"
  artifacts:
    - path: "src/pyro_dcm/utils/local_linearization.py"
      provides: "integrate_local_linearization: spm_int_L exp-Euler port (frozen Jacobian, exp(-16) shift, right-division Q, D=identity)"
      contains: "torch.linalg.solve"
    - path: "src/pyro_dcm/forward_models/cmc_neural_mass.py"
      provides: "cmc_f, cmc_sigmoid, parameterize_cmc with J_PERM permutation"
      contains: "J_PERM"
    - path: "src/pyro_dcm/forward_models/cmc_priors.py"
      provides: "cmc_prior_moments + cmc_steady_state (x0==0 assertion)"
      contains: "zeros"
    - path: "src/pyro_dcm/forward_models/erp_input.py"
      provides: "erp_gaussian_input: spm_erp_u Gaussian bump, ms timebase, 32-scaling"
      contains: "1000"
    - path: "tests/test_cmc_forward.py"
      provides: "permutation guard + sigmoid + units + float64 + no-eig-clip + x0==0 + flatten round-trip"
      contains: "G[:, 6]"
    - path: "tests/test_local_linearization.py"
      provides: "right-division orientation, regulariser-before-Q, dtype guards (non-MATLAB tier)"
      contains: "float64"
  key_links:
    - from: "src/pyro_dcm/utils/local_linearization.py"
      to: "f(x,u) callable"
      via: "v = v + Q @ f(v, U[i]) loop"
      pattern: "Q @ f"
    - from: "src/pyro_dcm/forward_models/cmc_neural_mass.py"
      to: "G[:,6] (sp self-inhibition)"
      via: "G[:, J_PERM[i]] *= exp(P_G[:,i]) with J_PERM[0]==6"
      pattern: "J_PERM"
---

<objective>
Port the single-source SPM12 canonical-microcircuit forward (`spm_fx_cmc.m`) and the SPM12
evoked integrator (`spm_int_L.m`) into pure torch (float64, zero new deps), plus the CMC
priors and the Gaussian evoked drive — all as NEW files. Write the C1-isolation
(integrator) and permutation-guard tests FIRST.

Purpose: This is the integration-scheme + intrinsic-dynamics foundation for the whole
v0.8.0 ERP milestone. SPM integrates ERPs with a frozen-Jacobian exponential-Euler scheme
(Ozaki 1992), NOT Runge-Kutta — the milestone headline risk (pitfall C1). Getting the
integrator and the intrinsic-parameter permutation right, in isolation, before any
extrinsic coupling can compound the error, is the entire point of Phase 33.

Output: 4 NEW source modules (`local_linearization.py`, `cmc_neural_mass.py`,
`cmc_priors.py`, `erp_input.py`), an append to `forward_models/__init__.py`, and 2 NEW
laptop test files. All unit tests green; existing paths bit-exact.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# THE implementation-ready spec — exact equations with spm_fx_cmc.m line refs (Facts 1-6),
# per-file specs, and the Open Questions (x_test, M.dur, jacrev) the planner resolved.
@.planning/phases/33-cmc-core-dynamics-spm-int-l-integrator-single-source-parity/33-RESEARCH.md

# Pitfalls C1, C2, C3, M1, N1, N2, B1 — lift the prevention items into the tests.
@.planning/research/v0.8.0/PITFALLS.md

# Existing patterns to MIRROR (CMC must NOT reuse parameterize_A's -exp/2 convention).
@src/pyro_dcm/forward_models/neural_state.py
@src/pyro_dcm/utils/ode_integrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port spm_int_L as utils/local_linearization.py (C1-isolation, test-first)</name>
  <files>src/pyro_dcm/utils/local_linearization.py, tests/test_local_linearization.py</files>
  <action>
  Write the test FIRST (C1 isolation), then implement.

  CREATE `tests/test_local_linearization.py` (laptop, sub-second, NO MATLAB yet):
  - `test_right_division_orientation`: build a deliberately ASYMMETRIC 4x4 J (float64) and a
    known E; assert the integrator's internal Q equals `(E - I) @ torch.linalg.inverse(J)`
    (reference computed once via inverse FOR THE TEST ONLY) and is NOT equal to
    `torch.inverse(J) @ (E - I)` — proves right-division orientation (pitfall C2).
  - `test_regulariser_before_Q`: assert the implementation shifts `J -> J - I*exp(-16)`
    BEFORE forming E and Q (e.g. expose/compute the shifted operator and check both E and
    the inv(J) inside Q use the shifted J).
  - `test_float64_enforced`: passing a float32 `x0` raises (CMC-07 / N1).
  - `test_no_eig_clip`: feed an f whose Jacobian has a positive real eigenvalue and assert
    the integrator does NOT clip/modify eigenvalues (only the exp(-16) shift) — pitfall N2.
  - `test_identity_delay_default`: with `delay_operator=None`, D == identity is used.
  - `test_linear_ode_matches_closed_form`: for f(v,u)=J@v (constant J, u ignored), one step
    `v1 = v0 + Q@f(v0)` reproduces the exp-Euler update; assert finite, float64, shape (n,).

  CREATE `src/pyro_dcm/utils/local_linearization.py` with the EXACT signature from
  33-RESEARCH.md "Per-file implementation spec":
    `integrate_local_linearization(f, x0, inputs, dt, n_substeps=1, delay_operator=None, g=None) -> Tensor`
  Body = Fact 3 verbatim:
    - Hard-assert `x0.dtype == torch.float64` and `torch.isfinite(f(x0, u0)).all()` where
      `u0 = torch.zeros_like(inputs[0])` (Jacobian taken at u=0 per Fact 5).
    - Frozen Jacobian: `J = torch.func.jacrev(lambda v: f(v, u0))(x0)` (Open Question 3 →
      jacrev recommended, exact + differentiable for Phase 35).
    - `J = J - torch.eye(n, dtype=float64) * math.exp(-16.0)`  (regulariser BEFORE Q).
    - `D = delay_operator if not None else eye(n)`.
    - `E = torch.matrix_exp(dt * D @ J / n_substeps)`.
    - `Q = torch.linalg.solve(J.T, (E - I).T).T`  (right-division; NEVER torch.inverse).
    - Loop: `v = x0.clone(); for i in range(ns): for _ in range(n_substeps): v = v + Q @ f(v, inputs[i]); y[i] = g(v, inputs[i]) if g else v`.
    - Return `(ns, n)` float64 (real part).
  Keep the module CMC-AGNOSTIC (takes `f`, not CMC internals); it lives next to
  `ode_integrator.py` and MUST NOT touch it (NEW sibling, not an edit — pitfall C1
  prevention). Docstring cites `spm_int_L.m:112-169` (regulariser+Q `:126-127`, loop
  `:132-147`, N default `:70`) and Ozaki (1992) by name. Do NOT fabricate a [REF-xxx]
  Better-BibTeX key (interim-citation decision: SPM source file + line + author/year only,
  per CLAUDE.md .bib rule — Zotero keys wait on the user).
  </action>
  <verify>`python -m pytest tests/test_local_linearization.py -q` all green; `ruff check src/pyro_dcm/utils/local_linearization.py tests/test_local_linearization.py` clean; `mypy src/pyro_dcm/utils/local_linearization.py` no NEW error categories.</verify>
  <done>Integrator reproduces the exp-Euler update with right-division Q via torch.linalg.solve, regulariser applied before Q, float64 enforced, no eig-clip; all laptop tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: CMC forward cmc_neural_mass.py + permutation guard (test-first)</name>
  <files>src/pyro_dcm/forward_models/cmc_neural_mass.py, tests/test_cmc_forward.py</files>
  <action>
  Write the permutation guard FIRST, then implement.

  CREATE `tests/test_cmc_forward.py` (laptop, NO MATLAB):
  - `test_permutation_guard` (MANDATORY, CMC-02): build a baseline `P` with `P_G = zeros(1,4)`;
    compute `G_base = parameterize_cmc(P, n=1)["G"]`. Perturb `P_G[:,0] += 0.5`; compute
    `G_pert`. Assert `G_pert[:,6] != G_base[:,6]` (sp self-inhibition changed) AND
    `torch.allclose(G_pert[:,0], G_base[:,0])` (G col 0 unchanged). This is the precision-knob
    guard — `P.G col 1 → G(:,7) MATLAB → G[:,6] Python` (Fact 2).
  - `test_sigmoid_baseline`: `cmc_sigmoid(zeros, P_S=0)` returns 0 exactly (the -1/2
    subtraction); `R == 2/3` when `P_S=0` (check via a known nonzero x: S = 1/(1+exp(-(2/3)x)) - 1/2).
  - `test_time_constant_units`: `parameterize_cmc` returns `T` in SECONDS (T0_MS/1000 then
    *exp(P.T)); assert `T[:,0]` ≈ 0.002 when `P_T=0`.
  - `test_extrinsic_convention`: parameterize_cmc uses `+exp(P.A)*E0` (NOT the fMRI
    `-exp(x)/2`); assert it does NOT import or call `parameterize_A` (pitfall B1 / C3f). At
    n=1 the A{1..4} blocks are identically zero (spm_fx_cmc.m:73-75) — assert they are zeros.
  - `test_steady_state_zero` (M1): `cmc_f(zeros(8), u=0, P)` returns zeros(8) — zero IS the
    fixed point (S=F-1/2=0, U=0).
  - `test_float64` (CMC-07/N1): cmc_f returns float64; a float32 input is rejected/up-cast-guarded.
  - `test_column_major_flatten`: round-trip `x_flat = x.T.reshape(-1)` ↔ `x = x_flat.reshape(8,n).T`
    is identity at n=1 and preserves state-blocked order (pitfall C3h / Fact 1 flatten note).

  CREATE `src/pyro_dcm/forward_models/cmc_neural_mass.py` per 33-RESEARCH.md spec:
    - Module constants (Fact 1): `J_PERM = (6,1,2,3,0,4,5,7,8,9)`,
      `G0 = tensor([4,4,8,4,4,2,4,4,2,1], float64)*200`, `T0_MS = tensor([2,2,16,28], float64)`,
      `E0 = tensor([1,0.5,1,0.5], float64)*200`. (`from __future__ import annotations` at top.)
    - `cmc_sigmoid(x, P_S)`: `R=(2/3)*exp(P_S); return 1/(1+exp(-R*x)) - 0.5`  (spm_fx_cmc.m:90-94).
    - `parameterize_cmc(P, n)`: T = (ones(n,1)*T0_MS)/1000 then `T[:,i]*=exp(P_T[:,i])` for i in 0..3
      (spm_fx_cmc.m:114,148-150); G = ones(n,1)*G0 then `G[:,J_PERM[i]]*=exp(P_G[:,i])` for i in 0..3
      (spm_fx_cmc.m:151-154); C = exp(P_C); A = zeros at n=1 (spm_fx_cmc.m:73-75 else-branch).
      Return dict {T, G, C, A, S=P_S}. NOT a generalisation of parameterize_A.
    - `cmc_f(x_flat, u, P, n=1) -> (8n,)`: reshape via column-major (Fact 1 flatten); compute
      S = cmc_sigmoid(x, P_S); U = C*u*32 (exogenous branch, spm_fx_cmc.m:86,107); the 4
      equations of motion verbatim from Fact 1 (granular f[:,1], supragranular sp f[:,3], ii
      f[:,5], infragranular dp f[:,7]); voltage integrals f[:,0]=x[:,1], f[:,2]=x[:,3],
      f[:,4]=x[:,5], f[:,6]=x[:,7]; flatten column-major and return. G-index map: MATLAB G(:,k)
      → Python G[:,k-1].
  Pin a comment citing the `spm_fx_cmc.m` line number next to EACH transform (CLAUDE.md rule 2).
  Interim-citation: SPM source line + David & Friston 2003 by author/year in prose ONLY; do
  NOT add a \cite{} key or REFERENCES.md entry (Zotero unconfirmed — Open Question 6).
  </action>
  <verify>`python -m pytest tests/test_cmc_forward.py -q` all green (permutation guard passes); `ruff check` clean on both files; confirm `parameterize_A` is NOT imported (`grep -n parameterize_A src/pyro_dcm/forward_models/cmc_neural_mass.py` returns nothing).</verify>
  <done>cmc_f, cmc_sigmoid, parameterize_cmc implemented with the J_PERM permutation; perturbing P_G[:,0] moves G[:,6] not G[:,0]; sigmoid has the -1/2; T in seconds; x0=0 is the fixed point; float64; column-major flatten round-trips.</done>
</task>

<task type="auto">
  <name>Task 3: CMC priors + Gaussian evoked input + __init__ exports</name>
  <files>src/pyro_dcm/forward_models/cmc_priors.py, src/pyro_dcm/forward_models/erp_input.py, src/pyro_dcm/forward_models/__init__.py, tests/test_cmc_forward.py</files>
  <action>
  CREATE `src/pyro_dcm/forward_models/cmc_priors.py` (CMC-04) transcribing Fact 6 as data
  tables (dicts name → (mean, var)). Provide:
    - `cmc_prior_moments(a_mask, c_mask, n)` → (E, V) dicts for the single-source diagonal:
      4×T (mean 0, var 1/32), 4×G (mean 0, var 1/32), 1×S (mean 0, var 1/64),
      C (mean mask*32-32, var mask/32), R (mean zeros(u,2), var [1/16,1/16]).
      Omit/zero P.D for Phase-33 fixtures (spm_cmc_priors.m:123 — D deferred). Cite
      `spm_cmc_priors.m` line numbers (:114-133).
    - `cmc_steady_state(n)` → `torch.zeros(n, 8, dtype=float64)` WITH an `assert` that it is
      all-zeros and a docstring citing the `spm_dcm_neural_x.m:70-72` otherwise→zeros branch
      (M1 — no Newton solve).

  CREATE `src/pyro_dcm/forward_models/erp_input.py` (CMC-05) per Fact 5:
    `erp_gaussian_input(t_s, P_R, ons_ms=60.0, dur_ms=16.0, sus=0.0) -> (ns, n_inp)`:
    - `t_ms = t_s * 1000` (spm_erp_u.m:46 — ms timebase internally).
    - `delay = ons_ms + 128 * P_R[i,0]`; `scale = dur_ms * exp(P_R[i,1])`.
    - `Ug = exp(-(t_ms - delay)**2 / (2*scale**2))`.
    - `prop = sus * exp(P_R[i,2])` (default sus=0 → prop 0); KEEP the sustained-mix term even
      though default is 0 (pitfall N4): `Ug = prop*cumsum(Ug)/sum(Ug) + Ug*(1-prop)`.
    - `u[:,i] = 32 * Ug` (the 32-scaling lives HERE; spm_erp_u.m:63).
    - Use M.dur = 16 ms EXPLICITLY (Open Question 2 / N3 — do not rely on the 32 fallback).
    Cite `spm_erp_u.m:42-64`.

  APPEND to `tests/test_cmc_forward.py`:
    - `test_erp_input_peak`: peak of `erp_gaussian_input` lands at t_ms ≈ ons_ms (P_R=0) and
      the peak value is 32 (the 32-scaling); output is float64, shape (ns, n_inp).
    - `test_steady_state_assert`: `cmc_steady_state(1)` is zeros(1,8) float64.

  APPEND to `src/pyro_dcm/forward_models/__init__.py` ONLY new exports (cmc_f, cmc_sigmoid,
  parameterize_cmc, cmc_prior_moments, cmc_steady_state, erp_gaussian_input). Do NOT remove or
  reorder existing exports — additive-only (B4); existing fMRI/spectral/rDCM/latent paths
  must stay importable and bit-exact.
  </action>
  <verify>`python -m pytest tests/test_cmc_forward.py -q` all green; `python -c "from pyro_dcm.forward_models import cmc_f, parameterize_cmc, erp_gaussian_input, cmc_steady_state"` succeeds; `ruff check` clean; `python -m pytest tests/ -q -k "spectral or task or rdcm" -x` still green (additive-only regression spot-check).</verify>
  <done>cmc_priors + erp_input implemented and exported; Gaussian bump peaks at onset with 32-scaling; steady state asserts zeros; existing forward-model imports unbroken.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_cmc_forward.py tests/test_local_linearization.py -q` → all green on laptop (sub-second to a few seconds; <3 min so laptop is correct per compute routing).
- Permutation guard passes: perturbing `P_G[:,0]` changes `G[:,6]`, not `G[:,0]`.
- `ruff check` clean and `mypy` introduces no new error categories on the 4 new modules.
- `git diff --name-only` shows ONLY the 4 new source files + 2 new test files + the
  `forward_models/__init__.py` append. No edits to `parameterize_A`, `ode_integrator.py`, or
  any existing forward model. `pyproject.toml` unchanged (zero new deps, B4).
</verification>

<success_criteria>
- ROADMAP Phase 33 success criteria 2, 3, 4, 5 satisfied at the unit-test level (the parity
  gate, criterion 1, is Waves 2-3).
- Integrator (CMC-03), cmc_neural_mass (CMC-01/02), cmc_priors (CMC-04), erp_input (CMC-05)
  exist as NEW files; float64 + no-eig-clip guards (CMC-07) green; x0==0 asserted (M1).
- The C1-isolation integrator test and the permutation guard were written first and pass.
- Existing paths bit-exact (additive-only).
</success_criteria>

<output>
After completion, create
`.planning/phases/33-cmc-core-dynamics-spm-int-l-integrator-single-source-parity/33-01-SUMMARY.md`.
Record the frozen single-source reference `P` struct shape (Wave 2 needs it locked to build
the MATLAB fixture input) and the recommended frozen `x_test`/`u_test` (Open Question 1:
`x_test = 0.1*ones(1,8)`, `u_test` = peak Gaussian value) so Plan 33-02 freezes the SAME
point in the export.
</output>
