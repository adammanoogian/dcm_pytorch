---
phase: 33-cmc-core-dynamics-spm-int-l-integrator-single-source-parity
plan: 03
type: execute
wave: 3
depends_on: ["33-01", "33-02"]
files_modified:
  - tests/test_spm_erp_dcm_validation.py
  - tests/test_local_linearization.py
  - cluster/sbatch/erp_parity_test.sbatch
autonomous: false

must_haves:
  truths:
    - "f_field parity: torch cmc_f at the frozen (x_test,u_test) matches the MATLAB f_field array <= 1e-10"
    - "J0 parity: the torch frozen Jacobian at x0=0,u0=0 matches the MATLAB J0 array <= 1e-10"
    - "matrix_exp floor is MEASURED: max|torch.matrix_exp(dtJ) - Eexp| is recorded (not assumed); Q_update / y_states thresholds are a small multiple of it"
    - "Q_update parity: torch right-division Q matches the MATLAB Q_update array <= 1e-9"
    - "y_states parity: the torch single-source trajectory (D=1, x0=0) matches the MATLAB y_states <= 1e-8"
    - "The ladder asserts IN ORDER (f_field -> J0 -> matrix_exp -> Q_update -> y_states) so a failure localises to one stage (V5)"
    - "The test reads meta.D==1 and asserts torch x0 is exactly zeros(8) before integrating; tolerances are element-wise forward agreement (no absolute-F, no Cp — V2)"
  artifacts:
    - path: "tests/test_spm_erp_dcm_validation.py"
      provides: "the V5 staged parity ladder vs the frozen .mat (CMC-06)"
      contains: "erp_single_source_fixtures"
    - path: "tests/test_local_linearization.py"
      provides: "MEASURED matrix_exp vs spm_expm floor on exported dtJ (extends Wave-1 file)"
      contains: "matrix_exp"
    - path: "cluster/sbatch/erp_parity_test.sbatch"
      provides: "M3 entrypoint running the SPM-gated parity suite (real assertion run)"
      contains: "pytest"
  key_links:
    - from: "tests/test_spm_erp_dcm_validation.py"
      to: "validation/data/erp_single_source_fixtures.mat"
      via: "scipy.io.loadmat in the test body"
      pattern: "loadmat"
    - from: "tests/test_spm_erp_dcm_validation.py"
      to: "integrate_local_linearization + cmc_f"
      via: "torch forward re-run compared element-wise to the fixtures"
      pattern: "integrate_local_linearization"
---

<objective>
Assert the single-source SPM12 parity gate (CMC-06): the V5 staged ladder
(`f_field` → `J0` → `matrix_exp(dtJ)` vs `Eexp` MEASURED → `Q_update` → `y_states`) against
the frozen MATLAB fixtures from Wave 2, plus the MEASURED `matrix_exp`↔`spm_expm` floor.

Purpose: This is the phase deliverable — proving the torch CMC forward + `spm_int_L`
integrator are element-wise equivalent to SPM12 in isolation, BEFORE any extrinsic coupling
(Phase 34) can compound a divergence. Staged ordering means any failure localises to one
stage (pitfall V5); forward parity is element-wise, never absolute-F (pitfall V2 / the
Phase-32 270-nat lesson).

Output: `tests/test_spm_erp_dcm_validation.py` (the ladder), an extension to
`tests/test_local_linearization.py` (measured floor), and the M3 sbatch that runs the
SPM-gated suite for real (it auto-skips on the laptop).
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The parity-gate test spec (fixture table, tolerance tiers, the staged ladder, mandatory asserts).
@.planning/phases/33-cmc-core-dynamics-spm-int-l-integrator-single-source-parity/33-RESEARCH.md

# Pitfalls C1, C2 (loop-ordering localisation, right-division), V1/V2/V3/V5 (vs-SPM, element-wise,
# measured tolerance, staged ladder).
@.planning/research/v0.8.0/PITFALLS.md

# Fixture provenance to assert against (shapes, dtypes, frozen x_test/u_test/dt/ns/M.dur, $Id).
@.planning/phases/33-cmc-core-dynamics-spm-int-l-integrator-single-source-parity/33-02-SUMMARY.md

# The EXACT @pytest.mark.spm/slow + skipif(not check_matlab_available()) pattern to mirror.
@tests/test_vl_spm_cross_validation.py
@cluster/sbatch/spm_cross_validation.sbatch
</context>

<tasks>

<task type="auto">
  <name>Task 1: The V5 staged parity ladder — tests/test_spm_erp_dcm_validation.py</name>
  <files>tests/test_spm_erp_dcm_validation.py</files>
  <action>
  CREATE `tests/test_spm_erp_dcm_validation.py`. Module-level markers EXACTLY like
  `tests/test_vl_spm_cross_validation.py:33-40`:
  `pytestmark = [pytest.mark.spm, pytest.mark.slow,
   pytest.mark.skipif(not check_matlab_available(), reason="MATLAB+SPM12 required")]`
  Additionally skip if `validation/data/erp_single_source_fixtures.mat` is absent (so the
  laptop skips cleanly; the real run is on M3 where both MATLAB + the fixtures are present).

  Load the fixtures once (`scipy.io.loadmat`), read `meta`. Re-run the torch forward using the
  frozen reference `P`/`M` from `meta` (the SAME P struct 33-01-SUMMARY locked) via
  `parameterize_cmc`, `cmc_f`, and `integrate_local_linearization`.

  MANDATORY pre-asserts (baked into the gate):
  - `assert int(meta["D"]) == 1`  (delays off).
  - `assert torch.equal(x0, torch.zeros(8, dtype=torch.float64))` BEFORE integrating (x0==0, M1).
  - float64 at the integrator boundary (CMC-07).

  Assert the ladder IN THIS ORDER (V5 — a failure localises to one stage):
  1. `test_f_field_parity`: torch `cmc_f(x_test, u_test, P)` vs `f_field` — `max|.| <= 1e-10`.
     (Isolates every transform/sigmoid/perm/units BEFORE the integrator.)
  2. `test_j0_parity`: torch frozen Jacobian (`torch.func.jacrev` of `cmc_f` at x0=0,u0=0) vs
     `J0` — `<= 1e-10`.
  3. `test_matrix_exp_measured`: compute `max|torch.matrix_exp(dtJ) - Eexp|` on the EXPORTED
     `dtJ`; RECORD it (print + assert it is below a measured ceiling, e.g. `< 1e-9`, the floor
     pinned in Task 2). This is the matrix_exp↔spm_expm measurement, not an assumption (V3).
  4. `test_q_update_parity`: torch `Q = torch.linalg.solve(dfdx.T,(Eexp-I).T).T` (right-division
     on the SAME exported `dfdx = J0 - I*exp(-16)`) vs `Q_update` — `<= 1e-9`.
  5. `test_y_states_parity`: torch `integrate_local_linearization(cmc_f, x0=0, inputs, dt, D=I)`
     vs `y_states` — `<= 1e-8`. Docstring notes: if J0 + Q_update match but this fails, the bug
     is loop ordering (`v += Q@f` vs `v = Q@(v+f)`), not algebra (C1 prevention).

  Tolerances are ELEMENT-WISE forward agreement only — NO absolute free energy, NO element-wise
  Cp (V2 / the Phase-32 S3 rule). Each test prints the measured `max|diff|` so the actual floor
  is recorded, not just the threshold.
  </action>
  <verify>On laptop: `python -m pytest tests/test_spm_erp_dcm_validation.py -q` → collects + SKIPS cleanly (no MATLAB license / and/or fixtures present-but-marker-gated), 0 errors. `ruff check tests/test_spm_erp_dcm_validation.py` clean. The real green run is Task 3 on M3.</verify>
  <done>The 5-rung ladder is authored with the mandatory D==1 + x0==0 + float64 pre-asserts, element-wise tolerances (1e-10/1e-10/measured/1e-9/1e-8), and clean laptop skip.</done>
</task>

<task type="auto">
  <name>Task 2: Measured matrix_exp floor + M3 parity-test sbatch</name>
  <files>tests/test_local_linearization.py, cluster/sbatch/erp_parity_test.sbatch</files>
  <action>
  EXTEND `tests/test_local_linearization.py` (the Wave-1 file) with the MEASURED tier — guard
  it with the same `@pytest.mark.spm + skipif(not check_matlab_available() or fixtures absent)`
  so the Wave-1 non-MATLAB tests stay laptop-green and this one rides M3:
  - `test_matrix_exp_vs_spm_expm_floor`: load `dtJ` and `Eexp` from
    `erp_single_source_fixtures.mat`; compute `floor = max|torch.matrix_exp(dtJ) - Eexp|`;
    print it; assert `floor < 1e-9` (the recorded measured ceiling — V3, MEDIUM-confidence
    1e-12 is asserted/measured, never assumed). This value sets the small-multiple thresholds
    used by the Q_update/y_states tiers in Task 1.

  CREATE `cluster/sbatch/erp_parity_test.sbatch` mirroring `cluster/sbatch/spm_cross_validation.sbatch`
  (and the existing `vl_protocol_tests.sbatch` pattern): `--partition=comp`, `--mem=16G`,
  `--time=01:00:00`, `export MATLAB_PATH=/usr/local/matlab/r2022a/bin/matlab`,
  `export SPM12_PATH=/home/aman0087/fc37/Carrick/spm12`, then
  `python -m pytest tests/test_spm_erp_dcm_validation.py tests/test_local_linearization.py -m "spm and slow" -v`
  so the SPM-gated parity suite runs for real on M3 (where MATLAB is available AND the synced
  `.mat` is present). NO pip install (project rule).
  </action>
  <verify>`python -m pytest tests/test_local_linearization.py -q` on laptop → Wave-1 tests still green, the new measured-floor test SKIPS cleanly (no MATLAB/fixtures). `bash -n cluster/sbatch/erp_parity_test.sbatch`. `ruff check tests/test_local_linearization.py` clean.</verify>
  <done>The measured matrix_exp↔spm_expm floor test is added (skips on laptop), and the M3 sbatch runs the SPM-gated parity suite. Wave-1 non-MATLAB tests remain green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The full V5 parity ladder + the measured matrix_exp floor test + an M3 sbatch (`erp_parity_test.sbatch`) that runs the SPM-gated suite for real. The tests auto-skip on the laptop (no MATLAB license — FlexLM -15); the real assertion run rides M3, mirroring Phase 32.</what-built>
  <how-to-verify>
    1. Unlock the ssh-agent (`ssh-add` if locked) and confirm `ssh m3` reaches the cluster.
    2. The executor deploys via Mutagen (NOT git push/pull), ensures `validation/data/erp_single_source_fixtures.mat` (from Plan 33-02) is on M3, and submits `sbatch cluster/sbatch/erp_parity_test.sbatch`.
    3. Wait for the job (minutes). Harvest the pytest output / JSON back via Mutagen.
    4. Confirm the ladder is GREEN in order: f_field <= 1e-10, J0 <= 1e-10, matrix_exp floor recorded (< 1e-9), Q_update <= 1e-9, y_states <= 1e-8 — and that D==1 + x0==0 pre-asserts passed. Record the MEASURED max|diff| for each rung.
    5. If any rung fails, note WHICH rung (the ladder localises the bug: f_field→transforms; J0→Jacobian; matrix_exp→Pade floor; Q_update→right-division; y_states→loop ordering) and ESCALATE (a real parity miss is a finding, not something to silently relax — the Phase-32 discipline).
  </how-to-verify>
  <resume-signal>Type "parity green" with the 5 measured max|diff| values once the M3 run passes the full ladder, or paste the failing rung + its measured diff to diagnose.</resume-signal>
</task>

</tasks>

<verification>
- On M3 (real run): `pytest -m "spm and slow"` passes the full ladder — f_field ≤1e-10,
  J0 ≤1e-10, matrix_exp floor recorded, Q_update ≤1e-9, y_states ≤1e-8 — with D==1 + x0==0
  pre-asserts green.
- On laptop: the SPM-gated tests collect and SKIP cleanly (0 errors); Wave-1 non-MATLAB tests
  stay green.
- Tolerances are element-wise forward agreement; no absolute-F / no Cp anywhere (V2).
- The matrix_exp↔spm_expm floor is MEASURED and recorded, not assumed (V3).
</verification>

<success_criteria>
- ROADMAP Phase 33 criterion 1 (the SPM12 PARITY GATE) is GREEN on M3: the single-source
  `spm_fx_cmc` + `spm_int_L` torch port is element-wise equivalent to SPM12 (CMC-06).
- The staged ladder localises any divergence to one stage; the integration-scheme mismatch
  (C1) is proven absent in isolation before Phase 34.
- The real assertion run executed on M3 (compute routing); the laptop auto-skips.
</success_criteria>

<output>
After completion, create
`.planning/phases/33-cmc-core-dynamics-spm-int-l-integrator-single-source-parity/33-03-SUMMARY.md`.
Record the M3 job id and the 5 MEASURED max|diff| values (one per ladder rung) + the measured
matrix_exp↔spm_expm floor, so Phase 34 inherits the proven single-source baseline and the
tolerance floors.
</output>
