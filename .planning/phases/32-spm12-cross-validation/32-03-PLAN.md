---
phase: 32-spm12-cross-validation
plan: 03
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - validation/run_vl_validation.py
  - tests/test_vl_spm_cross_validation.py
autonomous: true

must_haves:
  truths:
    - "VL output (theta_post['A_free']) is cross-validated vs SPM spm_nlsi_GN Ep.A in FREE-PARAMETER space (not the parameterized 'A') on a prior-matched reciprocal-edge N=2 spectral problem, agreeing within ~10% (S1+S2)."
    - "The VL fit uses SPM-matched priors: hyperprior_mean=8.0, hyperprior_precision=128.0, prior_mean_a_offset=a_mask/128.0 (S2)."
    - "Both engines fit the IDENTICAL Python-computed observed_csd via the same-CSD injection (Plan 32-01), so VL free_energy[-1] vs SPM DCM.F is a like-for-like 5% HARD gate on the matched problem (user decision)."
    - "Cross-model ranking agreement uses RELATIVE delta-F across >=3 reduced-model masks via compare_model_ranking (agreement_rate >= 0.80), NEVER absolute F across models and NEVER element-wise Cp (S3)."
    - "The matched problem uses reciprocal edges with ASYMMETRIC strengths (e.g. (0,1)=0.15, (1,0)=0.10) so the S4 layout check has teeth; feed-forward/lone-edge A is forbidden (Phase 31 identifiability finding)."
    - "The SPM-gated test auto-skips gracefully when MATLAB is unavailable (skipif(not check_matlab_available())) and is marked @pytest.mark.spm + slow."
  artifacts:
    - path: "validation/run_vl_validation.py"
      provides: "run_vl_spectral_dcm_validation() — VL-path twin of run_spectral_dcm_validation: builds reciprocal-asymmetric N=2 problem, VL fit with SPM-matched priors, injects same CSD to SPM, returns Ep comparison + matched-F comparison + ranking"
      contains: "def run_vl_spectral_dcm_validation"
      min_lines: 80
    - path: "tests/test_vl_spm_cross_validation.py"
      provides: "@pytest.mark.spm + slow SPM-gated test asserting Ep ~10% AND ranking agreement >= 0.80 as HARD gates; matched-F 5% recorded + asserted per user decision; auto-skips without MATLAB"
      contains: "check_matlab_available"
  key_links:
    - from: "validation/run_vl_validation.py"
      to: "run_variational_laplace(..., hyperprior_mean=8.0, hyperprior_precision=128.0, prior_mean_a_offset=a_mask/128.0)"
      via: "SPM-matched prior kwargs on the VL fit"
      pattern: "hyperprior_mean|prior_mean_a_offset"
    - from: "validation/run_vl_validation.py"
      to: "validation/export_to_mat.py::export_spectral_dcm_csd_for_spm + run_spm_spectral_dcm_csd_injected.m"
      via: "inject the SAME observed_csd into SPM, subprocess([MATLAB_PATH, '-batch', ...])"
      pattern: "export_spectral_dcm_csd_for_spm|run_spm_spectral_dcm_csd_injected"
    - from: "validation/run_vl_validation.py"
      to: "result.theta_post['A_free'] vs spm['Ep_A'] via compute_free_param_comparison(tolerance=0.10)"
      via: "free-parameter-space Ep comparison (S1)"
      pattern: "theta_post\\[.A_free.\\]|compute_free_param_comparison"
    - from: "validation/run_vl_validation.py"
      to: "compare_free_energies(vl_F, spm_F, rel_tolerance=0.05) and compare_model_ranking(...)"
      via: "matched-F gate (single problem) + relative cross-model ranking (S3-safe)"
      pattern: "compare_free_energies|compare_model_ranking"
---

<objective>
Build `run_vl_validation.py` — the VL-path twin of the existing SVI orchestrator
`run_spectral_dcm_validation` — and the SPM-gated test that runs the actual cross-validation: fit
the Phase 28 VL engine on a prior-matched reciprocal-edge N=2 spectral DCM problem, inject the
identical CSD into SPM via the Plan 32-01 bridge, run `spm_nlsi_GN`, and compare in free-parameter
space (Ep ~10%, S1/S2), matched free energy (5% HARD gate on identical CSD, user decision), and
cross-model ranking (relative delta-F >= 0.80, S3-safe).

Purpose: This is the deliverable of Phase 32 (VLSPM-01/02/03). It wires the Phase 28 VL engine to
MATLAB SPM12 through the existing `validation/` bridge plus the two new pieces (the same-CSD export
from 32-01 and `compare_free_energies` from 32-02), proving the VL port agrees with the reference
implementation it was built to match.

Output: `run_vl_spectral_dcm_validation()` in `validation/run_vl_validation.py` (new file per
VLSPM-03) + `tests/test_vl_spm_cross_validation.py` (new SPM-gated test).
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-spm12-cross-validation/32-RESEARCH.md

# What this plan depends on (built in Wave 1):
@.planning/phases/32-spm12-cross-validation/32-01-SUMMARY.md
@.planning/phases/32-spm12-cross-validation/32-02-SUMMARY.md

# The SVI orchestrator to MIRROR (do NOT retrofit VL into it — VLSPM-03 mandates a new file).
# Reuse check_matlab_available (L67), MATLAB_SCRIPTS_DIR/DEFAULT_OUTPUT_DIR (L59-64), the
# subprocess([MATLAB_PATH, "-batch", ...]) pattern (L466-485).
@validation/run_validation.py
# Comparators to reuse: load_spm_results, compute_free_param_comparison, compare_model_ranking.
@validation/compare_results.py
# The VL engine under test: run_variational_laplace (L401), signature L402-415 (three SPM-match
# kwargs), VariationalLaplaceResult.theta_post["A_free"] + .free_energy[-1].
@src/pyro_dcm/inference/variational_laplace.py
# Matched ground truth: make_sparse_ground_truth_A (reciprocal, asymmetric strengths) + stability check.
@benchmarks/bmr_recovery.py
# Canonical reciprocal-edge spectral VL fit pattern.
@tests/test_bmr_vlbmr01_recovery.py
# SPM-gated pytest skip pattern (module-level pytestmark).
@tests/test_spm_spectral_dcm_validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write run_vl_validation.py (VL twin orchestrator, same-CSD path)</name>
  <files>validation/run_vl_validation.py</files>
  <action>
  Create `validation/run_vl_validation.py` (NEW FILE — do NOT edit `run_validation.py`; VLSPM-03
  mandates separation). Module docstring: VL-path cross-validation against SPM12 `spm_nlsi_GN`, same-
  CSD injection, prior-matched, reciprocal-asymmetric N=2; cite VLSPM-01/02/03 and S1-S4.
  `from __future__ import annotations`. Import `MATLAB_SCRIPTS_DIR`, `DEFAULT_OUTPUT_DIR`,
  `check_matlab_available` from `validation.run_validation`; prefer `from config import MATLAB_PATH`
  (env-overridable; cast to `str(MATLAB_PATH)` for subprocess) per research Open Q5.

  Define `run_vl_spectral_dcm_validation(seed: int = 42, n_regions: int = 2, max_iter: int = 64,
  output_dir: str | None = None) -> dict`:

  1. BUILD THE MATCHED PROBLEM (reciprocal, asymmetric):
     `A_true = make_sparse_ground_truth_A(n_regions, present_edges=[(0,1),(1,0)])` — BUT with
     asymmetric strengths. Since `make_sparse_ground_truth_A` sets a single `strength`, set the two
     edges asymmetric AFTER the call: `A_true[0,1] = 0.15; A_true[1,0] = 0.10`, then re-assert
     stability (`max real eig < 0`) and raise with expected-vs-actual if not. (Comment: feed-forward/
     lone-edge A is CSD-indistinguishable from empty — Phase 31 finding — so reciprocal-asymmetric is
     mandatory; asymmetry gives the S4 layout check teeth.)
  2. `sim = simulate_spectral_dcm(A_true, TR=2.0, n_freqs=32, seed=seed)`;
     `observed_csd = sim["csd"].to(torch.complex128)` (F,N,N); `freqs = sim["freqs"].double()`.
     `a_mask = torch.ones(N, N, dtype=torch.float64)`.
  3. VL FIT (SPM-matched priors, S2):
     ```python
     result = run_variational_laplace(
         observed_csd=observed_csd, freqs=freqs, a_mask=a_mask, N=N, max_iter=max_iter,
         hyperprior_mean=8.0, hyperprior_precision=128.0,
         prior_mean_a_offset=a_mask / 128.0,
     )
     vl_A_free = result.theta_post["A_free"].detach().cpu().numpy()   # S1: free space, NOT "A"
     vl_F = float(result.free_energy[-1])
     ```
  4. SPM SIDE via SAME-CSD injection (Plan 32-01): export with
     `export_spectral_dcm_csd_for_spm(observed_csd=observed_csd.cpu().numpy(),
     freqs=freqs.cpu().numpy(), a_mask=a_mask.cpu().numpy(), c_mask=np.ones((N,1)), TR=2.0,
     output_path=input_path)`. Then subprocess MATLAB exactly like `run_validation.py:466-485` but
     calling `run_spm_spectral_dcm_csd_injected` (the injected-CSD script). Use
     `setenv('DCM_INPUT_PATH', ...)` + `setenv('DCM_OUTPUT_PATH', ...)`. On non-zero returncode raise
     `RuntimeError` with `stdout[-500:]`/`stderr[-500:]` (mirror the existing error handling).
  5. LOAD + COMPARE:
     `spm = load_spm_results(results_path)`;
     `ep_cmp = compute_free_param_comparison(vl_A_free, spm["Ep_A"], tolerance=0.10)` (S1/S2);
     `f_cmp = compare_free_energies(vl_F, float(spm["F"]), rel_tolerance=0.05)` (matched-F gate);
     record `spm["Ep_A"][0,1]` vs `spm["Ep_A"][1,0]` as `ep_asymmetry = (float(spm["Ep_A"][0,1]),
     float(spm["Ep_A"][1,0]))` for the S4 sanity readout.
  6. CROSS-MODEL RANKING (>=3 masks, relative delta-F, S3-safe): re-fit VL on the SAME observed_csd
     under >=3 a_masks — full reciprocal (correct), single-direction (only [1,0]), diagonal-only —
     and run SPM on each (reuse the injection + subprocess; OR, to bound MATLAB cost, fit SPM under
     each a_mask by re-exporting with that a_mask). Collect scenarios
     `[{"spm_F": spm_F_k, "pyro_elbo": vl_F_k}, ...]` (note: `compare_model_ranking` key is literally
     `"pyro_elbo"`; VL `free_energy[-1]` substitutes — higher = better, same convention). Call
     `ranking = compare_model_ranking(scenarios)`. (Comment: single-direction may rank near diagonal-
     only — itself a valid agreement signal per Phase 31; NEVER compare absolute F across these
     masks, only relative ordering.)
  7. RETURN a flat dict: `{"ep_comparison": ep_cmp, "matched_f_comparison": f_cmp, "ranking":
     ranking, "vl_A_free": vl_A_free, "spm_Ep_A": spm["Ep_A"], "vl_F": vl_F, "spm_F": float(spm["F"]),
     "ep_asymmetry": ep_asymmetry, "A_true": A_true.cpu().numpy(), "seed": seed,
     "n_regions": n_regions}`. Do NOT put `Cp` element-wise comparison anywhere (S3); you may carry
     `spm.get("Cp")` UNUSED only if needed for debugging, but never compare it element-wise.

  Keep the function focused; factor the "fit one mask on both engines" into a private helper
  `_fit_both_engines(observed_csd, freqs, a_mask, ...)` if it reduces duplication. NumPy docstrings,
  native type hints, no wildcard imports, ruff line-length 88. Math symbols stay internal;
  descriptive names at the API boundary.
  </action>
  <verify>
  `python -c "import validation.run_vl_validation as m; print(hasattr(m, 'run_vl_spectral_dcm_validation'))"`
  prints True (import + symbol presence; does NOT run MATLAB). `ruff check validation/run_vl_validation.py`
  clean; `mypy validation/run_vl_validation.py` no new errors. Confirm via grep that the file contains
  `hyperprior_mean=8.0`, `prior_mean_a_offset`, `theta_post["A_free"]`, `compare_free_energies`,
  `compare_model_ranking`, and `run_spm_spectral_dcm_csd_injected`, and does NOT contain any
  element-wise `Cp` comparison.
  </verify>
  <done>
  `run_vl_spectral_dcm_validation` exists in a new file, builds the reciprocal-asymmetric N=2 matched
  problem, fits VL with the three SPM-matched priors, injects the SAME CSD into SPM, and returns Ep
  (free-space, 10%), matched-F (5%), and relative ranking — with no element-wise Cp and no absolute-F-
  across-models.
  </done>
</task>

<task type="auto">
  <name>Task 2: SPM-gated cross-validation test (auto-skip) + run it locally</name>
  <files>tests/test_vl_spm_cross_validation.py</files>
  <action>
  Create `tests/test_vl_spm_cross_validation.py`. Module-level `pytestmark` EXACTLY like
  `tests/test_spm_spectral_dcm_validation.py:29-36`:
  ```python
  from validation.run_validation import check_matlab_available
  pytestmark = [
      pytest.mark.spm,
      pytest.mark.slow,
      pytest.mark.skipif(not check_matlab_available(), reason="MATLAB/SPM12 not available"),
  ]
  ```
  Test `test_vl_matches_spm_on_matched_reciprocal_problem`:
  - `result = run_vl_spectral_dcm_validation(seed=42, n_regions=2, max_iter=64)`.
  - Print a comparison table (Ep free-space VL vs SPM, matched F, ranking agreement, ep_asymmetry).
  - HARD assertions (these are the pass/fail gates):
    1. `assert result["ep_comparison"]["within_tolerance"]` — Ep within ~10% in free-parameter space
       (S1/S2).
    2. `assert result["ranking"]["agreement_rate"] >= 0.80` — relative cross-model ranking agreement
       (S3-safe).
    3. `assert result["matched_f_comparison"]["within_tolerance"]` — matched free energy within 5%
       on the IDENTICAL injected CSD. This is a HARD gate per the user decision (strict-5%-F). If it
       fails, the test fails; the printed table must surface `relative_error` so the gap is visible.
  - S4 sanity assertion: `a01, a10 = result["ep_asymmetry"]; assert a01 != a10` (the asymmetric
    reciprocal ground truth must produce an asymmetric SPM Ep.A; equality would signal a transpose).
  - NEGATIVE guards (assert the suite does NOT do the forbidden comparisons): assert
    `"Cp" not in result or result.get("Cp") is None` style is wrong (Cp may be loaded); instead
    assert that there is no element-wise Cp metric key in `result` — e.g.
    `assert not any("cp" in k.lower() and "compar" in k.lower() for k in result)`. Add a comment: the
    cross-model criterion is `ranking["agreement_rate"]` (relative), never absolute F across masks.

  NumPy docstrings, `from __future__ import annotations`, native type hints.

  AFTER writing the test, RUN it locally (this machine has MATLAB R2022a + SPM12):
  `pytest tests/test_vl_spm_cross_validation.py -m spm -q -s`. This is a LOCAL run (NOT M3 — it needs
  MATLAB). If MATLAB is genuinely unavailable it must SKIP, not fail. If the matched-F 5% gate fails
  on the real run, do NOT silently relax it — report the actual relative_error and the printed table
  in the SUMMARY, and surface it as a CHECKPOINT/finding for the user (the user explicitly chose the
  strict 5% gate and the same-CSD path precisely to make it achievable; a genuine miss is a real
  result to escalate, not to weaken).
  </action>
  <verify>
  `pytest tests/test_vl_spm_cross_validation.py -m spm -q -s` — PASSES locally if MATLAB present (Ep
  10% + ranking 0.80 + matched-F 5% + asymmetry), or SKIPS cleanly if MATLAB absent. `ruff check
  tests/test_vl_spm_cross_validation.py` clean; `mypy` no new errors. Also confirm the suite skips
  gracefully: `pytest tests/test_vl_spm_cross_validation.py -m "not spm" -q` collects 0 / skips.
  </verify>
  <done>
  The SPM-gated test exists, runs locally against MATLAB SPM12, and asserts (HARD) Ep within 10% in
  free space, ranking agreement >= 0.80, matched-F within 5% on the identical injected CSD, and Ep
  asymmetry preserved (S4) — auto-skipping when MATLAB is unavailable. The real-run outcome
  (pass, or the actual matched-F relative_error if it misses) is recorded in the SUMMARY.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_vl_spm_cross_validation.py -m spm -q -s` passes locally (MATLAB present) or skips cleanly (absent).
- VL fit uses hyperprior_mean=8.0, hyperprior_precision=128.0, prior_mean_a_offset=a_mask/128.0 (S2).
- Comparison is theta_post["A_free"] (free space) vs SPM Ep_A — never the parameterized "A" (S1).
- Both engines fit the IDENTICAL observed_csd (same-CSD injection from 32-01) — matched-F 5% is like-for-like.
- Cross-model ranking via compare_model_ranking (relative delta-F, >=3 masks, >=0.80); no element-wise Cp, no absolute-F-across-models (S3).
- Matched problem is reciprocal-ASYMMETRIC N=2 (0.15/0.10); S4 asymmetry assertion holds.
- `ruff check validation/run_vl_validation.py tests/test_vl_spm_cross_validation.py` clean; `mypy` no new errors.
</verification>

<success_criteria>
The Phase 28 VL engine is cross-validated against MATLAB `spm_nlsi_GN` on a prior-matched
reciprocal-asymmetric N=2 spectral problem: posterior means agree within ~10% in free-parameter
space, free energy agrees within 5% on the identical injected CSD, and model ranking agrees via
relative delta-F — all reusing the existing `validation/` bridge plus only the two new pieces
(`run_vl_validation.py`, `compare_free_energies`), with the C-order/S4 contract green throughout.
VLSPM-01/02/03 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/32-spm12-cross-validation/32-03-SUMMARY.md`. Record the
ACTUAL matched-F relative_error from the real MATLAB run (and whether the strict 5% gate passed) —
this is the headline cross-validation number.
</output>
