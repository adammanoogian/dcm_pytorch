---
phase: 35-single-dipole-lead-field-scalp-projection-erpdcmforward
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - validation/export_to_mat.py
  - validation/matlab_scripts/run_spm_erp_dcm_leadfield.m
  - cluster/scripts/erp_cross_validation.py
  - cluster/sbatch/erp_cross_validation_leadfield.sbatch
  - validation/data/erp_leadfield_fixtures.mat
autonomous: false

must_haves:
  truths:
    - "export_erp_dcm_leadfield reuses the locked _MS_ 5-source reference net (N=5, Cnd=2) + export_erp_dcm_multisource machinery and ADDS the spatial spec to the DCM struct: dipfit.type='LFP', dipfit.Ns=5, dipfit.Nc=5, P.L = ones(1,5) double, P.J = sparse(1,3,1,1,8) one-hot -- all dims cast to double (the int64->spm_Ce footgun, commit a27828b)"
    - "run_spm_erp_dcm_leadfield.m (NEW) mirrors run_spm_erp_dcm_multisource.m scaffolding (env paths, loud try/catch, getenv IO, M.f='spm_fx_cmc_nodelay', asserts nargout(M.f)==2 and x0==zeros(5,8)) and forms L_full = spm_lx_erp(P, dipfit) (Nc,8N) -- spm_lx_erp.m:31-33"
    - "Per-condition scalp ERP reuses the spm_gen_erp SOURCE trajectory (recompute ysrc = spm_int_L(spm_gen_Q(P,X(c,:)), M, U), self-contained, Open Q6 RESOLVED) and projects y_scalp{c} = ysrc * L_full' (ns,Nc); diff_wave = y_scalp{2} - y_scalp{1} (deviant - standard)"
    - "The fixture erp_leadfield_fixtures.mat contains L_full (Nc,8N)=(5,40), per-condition y_scalp {Cnd}x(ns,Nc)=(128,5), diff_wave (ns,Nc)=(128,5), and a provenance meta (SPM $Id strings, dt, ns, D=1, nargout_Mf==2, P.J, P.L, dipfit.type='LFP', Nc, the _MS_ edge list)"
    - "The delay operator is forced OFF (D=1) and asserted in the MATLAB script; float64 throughout; x0==zeros(5,8) asserted (matches the Phase-34 multi-source reference exactly)"
    - "cluster/scripts/erp_cross_validation.py gains a leadfield mode (--mode leadfield) mirroring main_multisource: export -> matlab -batch run_spm_erp_dcm_leadfield -> round-trip shape/meta check -> JSON record (record-don't-crash, exit 0 on soft miss); the default single/multisource modes are unchanged"
    - "The .mat fixture is scp'd back from M3 (validation/data/ is mutagen-ignored -- the data/ footgun) and committed to git (V4 reviewed change)"
    - "Additive-only: the single-source export_erp_dcm and the multi-source export_erp_dcm_multisource stay byte-identical (existing fixtures unaffected); the new sbatch is a thin clone of erp_cross_validation_multisource.sbatch with no pip install"
  artifacts:
    - path: "validation/export_to_mat.py"
      provides: "export_erp_dcm_leadfield (additive; LFP spatial spec dipfit/P.J/P.L on the _MS_ net)"
      contains: "def export_erp_dcm_leadfield"
    - path: "validation/matlab_scripts/run_spm_erp_dcm_leadfield.m"
      provides: "spm_lx_erp LFP L_full + per-condition scalp ERP + diff_wave + meta"
      contains: "spm_lx_erp"
    - path: "cluster/scripts/erp_cross_validation.py"
      provides: "leadfield mode entrypoint (export -> matlab batch -> round-trip check -> JSON)"
      contains: "leadfield"
    - path: "cluster/sbatch/erp_cross_validation_leadfield.sbatch"
      provides: "M3 SLURM submission (comp/16G/1h, no pip) for the leadfield fixture job"
      contains: "run_spm_erp_dcm_leadfield"
    - path: "validation/data/erp_leadfield_fixtures.mat"
      provides: "L_full (5,40), y_scalp {2}x(128,5), diff_wave (128,5), meta -- the LEAD-05 parity target"
      min_lines: 0
  key_links:
    - from: "validation/export_to_mat.py:export_erp_dcm_leadfield"
      to: "DCM struct with dipfit.type='LFP', P.J=sparse(1,3,1,1,8), P.L=ones(1,5)"
      via: "additive spatial spec on the locked _MS_ 5-source net"
      pattern: "dipfit"
    - from: "validation/matlab_scripts/run_spm_erp_dcm_leadfield.m"
      to: "spm_lx_erp(P, dipfit) + ysrc * L_full'"
      via: "LFP lead field then project the recomputed spm_gen_erp source trajectory"
      pattern: "L_full"
    - from: "cluster/scripts/erp_cross_validation.py:leadfield mode"
      to: "erp_leadfield_fixtures.mat round-trip shape/meta check"
      via: "record-don't-crash JSON, exit 0 on soft miss"
      pattern: "erp_leadfield_fixtures"
---

<objective>
Generate the byte-frozen SPM12 LFP lead-field + scalp-ERP fixtures the Wave-3 parity ladder
asserts the pure-torch lead field against. This is the only M3/MATLAB piece of Phase 35:
local MATLAB FlexLM is unreachable, so the `spm_lx_erp` LFP lead field and the end-to-end
scalp ERP for the locked 5-source MMN reference (D=1) are computed on the cluster
(R2022a + Carrick spm12, comp partition) and committed.

Purpose: LEAD-05 is a parity gate -- the pure-torch scalp ERP must match
`spm_gen_erp + spm_lx_erp` (LFP mode) to <=1e-7. That requires a frozen MATLAB ground truth:
`L_full = kron(P.J, L_spatial)` in LFP identity mode and the projected source trajectory.
LFP mode is the cleanest possible target (trivial diagonal lead field, no head model, no
`spm_cond_units`, no MNI coords) -- the defensible head-model-free gate (Open Q1 RESOLVED:
LFP-only for the Phase-35 gate; the ECD gain export is deferred to Phase 36 where coords exist).

Output: `export_erp_dcm_leadfield` (additive in `export_to_mat.py`),
`run_spm_erp_dcm_leadfield.m` (NEW), the leadfield mode in the cluster harness + sbatch, and
`validation/data/erp_leadfield_fixtures.mat` (committed) with `L_full`, per-condition
`y_scalp`, `diff_wave`, and a provenance `meta`.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-single-dipole-lead-field-scalp-projection-erpdcmforward/35-RESEARCH.md

# The exporter + locked _MS_ 5-source net to reuse (export_erp_dcm_multisource at :727; _MS_ at :662)
@validation/export_to_mat.py
# The MATLAB scaffolding to mirror (env paths, try/catch, spm_fx_cmc_nodelay D=1 wrapper, nargout asserts)
@validation/matlab_scripts/run_spm_erp_dcm_multisource.m
# The cluster harness to extend (--mode {single,multisource}; add leadfield)
@cluster/scripts/erp_cross_validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: export_erp_dcm_leadfield (additive LFP spatial spec) + run_spm_erp_dcm_leadfield.m</name>
  <files>validation/export_to_mat.py, validation/matlab_scripts/run_spm_erp_dcm_leadfield.m</files>
  <action>
(a) APPEND `export_erp_dcm_leadfield(...)` to `validation/export_to_mat.py`, reusing the locked
`_MS_*` 5-source reference net (`_MS_SOURCE_NAMES`, `_MS_N=5`, the forward/backward/lateral edge
tuples, `_MS_A_LIVE/_MS_A_DEAD/_MS_B_EDGE/_MS_B_DIAG`, `_ms_log_block`, `_erp_gaussian_u_grid`)
and the `export_erp_dcm_multisource` machinery. ADD the spatial spec to the DCM struct:
  - `dipfit.type = 'LFP'`, `dipfit.Ns = 5`, `dipfit.Nc = 5`.
  - `P.L = np.ones((1, 5))` (shape `(1,m)`, double).
  - `P.J = sparse one-hot (1,8) at MATLAB column 3` (the `e_2` contributing-state vector --
    `spm_L_priors.m:108`). Encode as a `(1,8)` double array with `1.0` at index 2.
  - Cast ALL dims to double (the int64->`spm_Ce` footgun -- commit a27828b; same as the
    multisource exporter).
  - Freeze `dipfit`/`P.J`/`P.L` + the `_MS_` edge list in `DCM.meta`.
The single-source `export_erp_dcm` and multi-source `export_erp_dcm_multisource` must remain
byte-identical (existing fixtures unaffected). Add a focused unit test (laptop, no MATLAB) in
the existing `tests/test_export_erp_multisource.py` OR a new `tests/test_export_erp_leadfield.py`
asserting the exported struct has `dipfit.type=='LFP'`, `P.L` shape `(1,5)` all ones, `P.J`
one-hot at index 2, and all-double dims.

(b) Write `validation/matlab_scripts/run_spm_erp_dcm_leadfield.m` (NEW), mirroring
`run_spm_erp_dcm_multisource.m` scaffolding (env paths via `getenv('SPM12_PATH')`, loud
try/catch, `getenv` input/output paths, `M.f='spm_fx_cmc_nodelay'`, `M.x=zeros(N,8)`,
`assert(nargout(M.f)==2)`, `assert(all(M.x(:)==0))`). Then:
```matlab
dipfit.type = 'LFP'; dipfit.Ns = N; dipfit.Nc = N;
L_full = spm_lx_erp(P, dipfit);            % (Nc, 8N)  spm_lx_erp.m:31-33
for c = 1:size(DCM.U.X,1)
    Qc         = spm_gen_Q(P, DCM.U.X(c,:));
    ysrc       = spm_int_L(Qc, M, DCM.U);  % (ns, 8N)  (Phase-34 verified path)
    y_scalp{c} = ysrc * L_full';           % (ns, Nc)  spm_lx_erp header y = L*x
end
diff_wave = y_scalp{2} - y_scalp{1};       % deviant - standard
```
Record `meta.D=1`, `meta.nargout_Mf`, `meta.N`, `meta.Nc`, `meta.dt`, `meta.ns`, `meta.P_J`,
`meta.P_L`, `meta.dipfit_type='LFP'`, the SPM `$Id` strings (`spm_lx_erp`, `spm_erp_L`,
`spm_gen_Q`, `spm_int_L`), and the `_MS_` edge list. `save(output_path, 'L_full', 'y_scalp',
'diff_wave', 'meta')`. Recompute `ysrc` self-contained (Open Q6 RESOLVED -- one fixture, one
provenance header, no cross-fixture coupling to the Phase-34 `.mat`).
  </action>
  <verify>
`pytest tests/test_export_erp_leadfield.py -q` (or the augmented multisource export test) passes
the struct-shape assertions on the laptop (no MATLAB). `python -c "from validation.export_to_mat
import export_erp_dcm_leadfield; print('ok')"`. `ruff check validation/export_to_mat.py`.
MATLAB syntax sanity: grep the `.m` for `spm_lx_erp`, `dipfit.type`, `nargout(M.f)`, `D=1` /
`assert`, and the `save` line (the script is UNEXECUTED locally -- FlexLM unreachable; execution
is Task 2 on M3). `git diff` on `export_to_mat.py` shows only the appended function (+ any new
`_MS_`-derived helper) -- existing exporters untouched.
  </verify>
  <done>
`export_erp_dcm_leadfield` writes a DCM struct with `dipfit.type='LFP'`, `P.L=ones(1,5)`,
`P.J` one-hot at index 2, all-double dims, on the locked `_MS_` net; the struct-shape unit test
passes; `run_spm_erp_dcm_leadfield.m` exists with the `spm_lx_erp` + projection + `meta` block
and the `nargout==2` / `x0==0` / D=1 asserts. Existing exporters byte-identical.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Submit + harvest the M3 leadfield fixture job; commit the .mat</name>
  <files>cluster/scripts/erp_cross_validation.py, cluster/sbatch/erp_cross_validation_leadfield.sbatch, validation/data/erp_leadfield_fixtures.mat</files>
  <action>
(a) EXTEND `cluster/scripts/erp_cross_validation.py`: add a `leadfield` mode (e.g.
`--mode leadfield` + a `main_leadfield()`) mirroring `main_multisource` -- export via
`export_erp_dcm_leadfield` -> `matlab -batch run_spm_erp_dcm_leadfield` -> round-trip shape/meta
check (`L_full (5,40)`, `y_scalp {2}x(128,5)`, `diff_wave (128,5)`, `meta.D==1`,
`meta.nargout_Mf==2`, `meta.N==5`, `meta.Nc==5`, `meta.dipfit_type=='LFP'`, `P.J` one-hot at 2)
-> JSON record. Record-don't-crash: a soft miss is RECORDED in JSON and exits 0; only an
unexpected exception exits 1. The default `single`/`multisource` modes stay unchanged.

(b) Add `cluster/sbatch/erp_cross_validation_leadfield.sbatch` -- a thin clone of
`erp_cross_validation_multisource.sbatch` (comp partition, 16G, `--time=01:00:00`, NO pip
install, exports `MATLAB_PATH=/usr/local/matlab/r2022a/bin/matlab` +
`SPM12_PATH=/home/aman0087/fc37/Carrick/spm12`) invoking the script with `--mode leadfield`.

(c) Submit on M3 and harvest. NOTE (per the hard constraints): the ORCHESTRATOR runs M3 directly
(ssh unlocked), so this is a blocking checkpoint -- BUT the executor MAY submit + harvest itself:
ssh is unlocked, `sbatch cluster/sbatch/erp_cross_validation_leadfield.sbatch`, poll the job,
then `scp` the produced `validation/data/erp_leadfield_fixtures.mat` back (validation/data/ is
Mutagen-IGNORED -- the data/ footgun, so it will NOT sync automatically), and `git add` + commit
the `.mat` (V4 reviewed change). Heed the Mutagen `models/` ignore footgun when syncing
`src/pyro_dcm/models/` if a code re-sync is needed first.
  </action>
  <how-to-verify>
1. `cluster/results/erp_cross_validation_<jobid>.json` shows `checks_pass=true` (or the recorded
   soft-miss reason).
2. `validation/data/erp_leadfield_fixtures.mat` exists locally and is committed
   (`git log --oneline -1 -- validation/data/erp_leadfield_fixtures.mat`).
3. Quick load check:
   `python -c "import scipy.io as sio; d=sio.loadmat('validation/data/erp_leadfield_fixtures.mat'); print(d['L_full'].shape, d['diff_wave'].shape)"`
   prints `(5, 40) (128, 5)`.
4. `meta` confirms `D=1`, `nargout_Mf=2`, `N=5`, `Nc=5`, `dipfit_type='LFP'`, SPM `$Id` strings
   present.
  </how-to-verify>
  <resume-signal>Type "approved" once the M3 job has produced erp_leadfield_fixtures.mat with checks_pass and it is committed to git, or describe the failure (e.g. SPM error line) to diagnose.</resume-signal>
</task>

</tasks>

<verification>
- M3 job produces `validation/data/erp_leadfield_fixtures.mat` with `L_full (5,40)`,
  `y_scalp {2}x(128,5)`, `diff_wave (128,5)`, and a provenance `meta` (SPM `$Id`, `dt`, `ns`,
  `D=1`, `nargout==2`, `P.J`, `P.L`, `dipfit.type='LFP'`, `Nc=5`, `_MS_` edge list).
- The `.mat` is scp'd back (data/ is mutagen-ignored) and committed to git.
- `export_erp_dcm_leadfield` struct-shape unit test green on the laptop (no MATLAB).
- Additive-only: `export_erp_dcm` + `export_erp_dcm_multisource` byte-identical; the cluster
  harness default modes unchanged; no pip in the sbatch.
- `ruff check` clean on `export_to_mat.py` + `erp_cross_validation.py`; the `.m` and `.sbatch`
  pass a bash/ast sanity grep.
</verification>

<success_criteria>
- LEAD-05 (fixture half): the frozen MATLAB ground truth exists -- `L_full` (LFP `kron(P.J,
  L_spatial)`), per-condition `y_scalp`, and `diff_wave` -- computed via `spm_lx_erp` +
  `spm_gen_erp`/`spm_int_L` for the locked 5-source MMN net at D=1, committed. MET when the
  `.mat` loads with the expected shapes + `meta`.
- LFP-only scope honored: `dipfit.type='LFP'`; no `spm_cond_units`, no MNI coords (ECD deferred
  to Phase 36).
- Additive + provenance: existing exporters/fixtures untouched; `meta` carries the SPM `$Id`s
  and the D=1 / `nargout==2` / `x0==0` assertions.
</success_criteria>

<output>
After completion, create
`.planning/phases/35-single-dipole-lead-field-scalp-projection-erpdcmforward/35-02-SUMMARY.md`.
Record: the M3 job id(s), the `meta` provenance (SPM `$Id` strings, D=1, Nc=5), the exact
fixture array shapes, the commit hash of the `.mat`, and confirmation that Wave 3 can assert the
pure-torch `build_lead_field`/`project_to_scalp` element-wise against these arrays.
</output>
