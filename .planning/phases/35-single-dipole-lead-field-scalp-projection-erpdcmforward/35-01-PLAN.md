---
phase: 35-single-dipole-lead-field-scalp-projection-erpdcmforward
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pyro_dcm/forward_models/erp_leadfield.py
  - src/pyro_dcm/inference/forward_models.py
  - src/pyro_dcm/simulators/erp_simulator.py
  - src/pyro_dcm/forward_models/__init__.py
  - tests/test_erp_leadfield.py
autonomous: true

must_haves:
  truths:
    - "build_lead_field(p_j, l_spatial) computes L_full = kron(P.J, L_spatial) with P.J as the FIRST kron operand, column-major state-block order (column index = state*n + source), matching the proven cmc_flatten (x.T.reshape(-1)) -- spm_lx_erp.m:33 (LEAD-01)"
    - "cmc_default_pj() returns the (8,) one-hot e_2 (1.0 at index 2 = superficial-pyramidal voltage), asserted argmax==2 AND != 6 (the dp-voltage inversion trap) -- spm_L_priors.m:108 (LEAD-02 guard)"
    - "A distinct-valued L_spatial + non-trivial P.J kron guard: build_lead_field block s occupies columns [s*n:(s+1)*n] and equals P.J[s]*L_spatial; a C-order (reshape) flatten would put the block at source*8+state and FAIL this (LEAD-02 kron column-major guard, pitfall C5.2)"
    - "lfp_spatial(ones(n), n) == eye(n) (LFP default identity, Nc==n); build_lead_field(e_2, I_n) has the sp-voltage block (s=2) == I_n and all other state blocks == 0 -- spm_erp_L.m:112 (LEAD-01)"
    - "project_to_scalp(states, l_full, x0) computes y = (states - x0) @ l_full.T; through the identity LFP lead field y[..., j] equals source j's sp-voltage trace; x0 defaults to zeros(8n) (CMC M1) -- spm_lx_erp.m header (LEAD-01)"
    - "class ERPDCMForward appended to inference/forward_models.py implements all 8 ForwardModel protocol members (residual_is_complex==False; param_count; pack/unpack frozen ordering; build_prior_cov from cmc priors; identity build_precision over Cnd*ns*Nc; predict->(Cnd,ns,Nc).reshape(-1); build_result) -- zero edits to the Protocol, the existing forward classes, or variational_laplace.py (LEAD-04)"
    - "ERPDCMForward.predict handles BOTH the (Cnd,ns,Nc) main-loop call AND the flat FD-Jacobian call via an observed.ndim guard (mirrors LatentCircuitForward.predict), and stacks the canonical internal layout (Cnd,ns,Nc) then C-order reshape(-1) (locked stacking layout, pitfall B3/gap4)"
    - "simulate_erp_dcm gains an optional l_full arg; when supplied it projects the existing (Cnd,ns,n,8) source states to scalp and adds keys 'scalp' (Cnd,ns,Nc) and 'difference_wave_scalp' = scalp[1]-scalp[0]; existing source-state keys are byte-untouched (LEAD-03)"
    - "The scalp difference wave is asserted NON-ZERO on the planted reference net (B is wired); the negative-going/frontal SIGN is explicitly NOT asserted here (deferred to Phase 36 where ECD orientation + MNI coords exist) -- Fact 6 (LEAD-03 scope split)"
    - "LEAD-06 VL round-trip: run_variational_laplace_generic(ERPDCMForward(...), scalp_obs, ...) on a small (n=2-3) planted CMC net recovers planted A/C/G/T within tolerance; laptop wall-time MEASURED and recorded (protocol confirmation, NOT a parity gate)"
  artifacts:
    - path: "src/pyro_dcm/forward_models/erp_leadfield.py"
      provides: "cmc_default_pj, lfp_spatial, ecd_spatial, build_lead_field, project_to_scalp"
      contains: "torch.kron"
    - path: "src/pyro_dcm/inference/forward_models.py"
      provides: "class ERPDCMForward (8-member ForwardModel protocol implementor, additive append)"
      contains: "class ERPDCMForward"
    - path: "src/pyro_dcm/simulators/erp_simulator.py"
      provides: "simulate_erp_dcm scalp projection path (l_full arg + scalp/difference_wave_scalp keys)"
      contains: "difference_wave_scalp"
    - path: "tests/test_erp_leadfield.py"
      provides: "structural guards (P.J=index2, kron column-major, LFP identity, projection, difference-wave non-zero, float64, predict ndim) + LEAD-06 VL round-trip"
      contains: "cmc_default_pj"
  key_links:
    - from: "src/pyro_dcm/forward_models/erp_leadfield.py:build_lead_field"
      to: "torch.kron(p_j.reshape(1,8), l_spatial)"
      via: "column-major state-block kron matching cmc_flatten"
      pattern: "torch\\.kron"
    - from: "src/pyro_dcm/inference/forward_models.py:ERPDCMForward.predict"
      to: "project_to_scalp + integrate_local_linearization + apply_condition_modulation + cmc_network_f"
      via: "lazy import inside predict (mirror TaskDCMForward lazy import); per-condition integrate->project->stack(Cnd,ns,Nc)"
      pattern: "project_to_scalp"
    - from: "tests/test_erp_leadfield.py:LEAD-06"
      to: "run_variational_laplace_generic(ERPDCMForward(...), ...)"
      via: "VL engine dispatch on the 8 protocol members, zero engine edits"
      pattern: "run_variational_laplace_generic"
---

<objective>
Build the pure-torch single-dipole lead field, the scalp projection, the additive
`ERPDCMForward` (a fourth `ForwardModel` protocol implementor), and the simulator scalp
path -- then prove the structural lead-field guards and the VL protocol round-trip on the
laptop. This is Wave 1: everything is pure-torch over the frozen, parity-verified
Phase-33/34 modules, so it lands green BEFORE any MATLAB fixture exists.

Purpose: Phase 34 left a per-source CMC source-state trajectory `(Cnd, ns, n, 8)` that is
bit-close to `spm_gen_erp` at N=5. Phase 35 turns it into the observed scalp ERP via a
single linear map `L_full = kron(P.J, L_spatial)` and exposes the whole forward to
Variational Laplace for free reuse. Catching the `P.J=index-2` and kron-column-major logic
in isolation (V5 ladder discipline) means any later scalp-ERP divergence cannot be blamed
on the lead-field algebra or the protocol wiring.

Output: `erp_leadfield.py` (NEW), `class ERPDCMForward` appended to
`inference/forward_models.py`, the `simulate_erp_dcm` scalp extension, and
`tests/test_erp_leadfield.py` (structural guards + LEAD-06 VL round-trip) -- all green on
the laptop, all additive (`git diff` shows only the new file + the appended class + new
simulator keys + `__init__` exports; existing class bodies untouched).
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-single-dipole-lead-field-scalp-projection-erpdcmforward/35-RESEARCH.md

# The additive 4th-implementor precedent + the protocol (read before appending ERPDCMForward)
@src/pyro_dcm/inference/forward_models.py
# The simulator to extend (source states -> scalp); the (Cnd,ns,n,8) return contract
@src/pyro_dcm/simulators/erp_simulator.py
# The frozen Phase-33/34 network forward + condition modulation + flatten (reuse, do NOT edit)
@src/pyro_dcm/forward_models/erp_coupled_system.py
@src/pyro_dcm/forward_models/cmc_neural_mass.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: erp_leadfield.py + structural guards (TESTS-FIRST: P.J=index-2, kron column-major)</name>
  <files>src/pyro_dcm/forward_models/erp_leadfield.py, src/pyro_dcm/forward_models/__init__.py, tests/test_erp_leadfield.py</files>
  <action>
Write the guard tests FIRST (RED), then the module (GREEN). This ordering is a hard quality
gate -- the P.J and kron guards are the single most important lead-field correctness checks
(pitfalls C5.1/C5.2) and must exist before any implementation.

(a) In `tests/test_erp_leadfield.py`, author the structural guards (laptop, no MATLAB, all
float64):
  - **P.J guard (LEAD-02):** `cmc_default_pj()` returns an (8,) one-hot; assert `argmax == 2`,
    assert `!= 6` (the dp-voltage inversion trap), assert it sums to 1.0. Cite
    `spm_L_priors.m:108` in the test docstring.
  - **kron column-major guard (LEAD-02):** build a DISTINCT-valued `L_spatial`
    (`(arange(Nc*n)+1).reshape(Nc, n)` with Nc=n=3) and a non-trivial `p_j`
    (e.g. `[0,0,1,0,0,0,2,0]`); compute `build_lead_field(p_j, L_spatial)` and assert, for
    each state `s`, that columns `[s*n:(s+1)*n]` equal `p_j[s]*L_spatial` element-wise. Add an
    explicit comment that a C-order `reshape` flatten would place the block at `source*8+state`
    and fail this. Cite `spm_lx_erp.m:33`.
  - **LFP identity:** `lfp_spatial(torch.ones(n), n)` equals `eye(n)`; `build_lead_field(e_2,
    eye(n))` has the sp-voltage block (`s=2`, columns `[2n:3n]`) equal to `eye(n)` and all
    other state blocks equal to 0.
  - **Projection through identity LFP:** for a random `states (ns, 8n)`,
    `project_to_scalp(states, build_lead_field(e_2, eye(n)))[..., j]` equals source `j`'s
    sp-voltage column (`states[:, 2*n + j]`). Confirm `x0=None` defaults to zeros and the
    `(states - x0)` subtraction is written explicitly.
  - **float64 guard:** the lead field + projection outputs are `torch.float64` (pitfall N1).

(b) Then write `src/pyro_dcm/forward_models/erp_leadfield.py` (NEW) with the five functions
exactly per `35-RESEARCH.md` "Per-file implementation spec":
  - `cmc_default_pj() -> Tensor` -- the (8,) one-hot `e_2` (1.0 at index 2). Docstring cites
    `spm_L_priors.m:108`; notes the free `pC.J` indices are `[0,6]` var 1/32, held FIXED in v1.
  - `lfp_spatial(p_l: Tensor, n: int) -> Tensor` -- `torch.diag(p_l)`, `(Nc=m, n)`; default
    `p_l = ones(n)` -> identity. Cite `spm_erp_L.m:112`.
  - `ecd_spatial(g_ecd: Tensor, p_l: Tensor) -> Tensor` -- `L[:,i] = G[:,:,i] @ P.L[:,i]`;
    `g_ecd (Nc,3,n)` MATLAB-exported gain POST `spm_cond_units`; `p_l (3,n)`; returns `(Nc,n)`.
    Cite `spm_erp_L.m:76`. Built now, EXERCISED in Phase 36 (no fixture here).
  - `build_lead_field(p_j: Tensor, l_spatial: Tensor) -> Tensor` --
    `torch.kron(p_j.reshape(1, 8), l_spatial)`, returns `(Nc, 8n)`. Both operands 2-D. Cite
    `spm_lx_erp.m:33`; docstring explains the column-major correspondence to `cmc_flatten`.
  - `project_to_scalp(states, l_full, x0=None) -> Tensor` -- `(states - x0) @ l_full.T`; x0
    defaults to `zeros(8n)`; handle `states` of shape `(ns, 8n)` OR `(Cnd, ns, 8n)`; float64.
    Cite `spm_lx_erp.m` header.
  Do NOT import `parameterize_A` or any `models/` symbol. Citation = SPM source file + line
  only (do NOT fabricate a `[REF-xxx]` bib key -- Zotero REF-ERP-* still "verify").

(c) Append the five new names to `src/pyro_dcm/forward_models/__init__.py` imports + `__all__`
(additive append only, mirror the existing cmc/erp export block).
  </action>
  <verify>
`pytest tests/test_erp_leadfield.py -k "pj or kron or lfp or project or float64" -q` passes
(write the tests, watch them go RED before the module exists if run incrementally, then GREEN).
`python -c "import torch; from pyro_dcm.forward_models.erp_leadfield import cmc_default_pj, build_lead_field, lfp_spatial, project_to_scalp; pj=cmc_default_pj(); n=3; L=build_lead_field(pj, lfp_spatial(torch.ones(n), n)); print(pj.argmax().item(), L.shape, L.dtype)"`
prints `2 torch.Size([3, 24]) torch.float64`.
`ruff check src/pyro_dcm/forward_models/erp_leadfield.py && ruff format --check src/pyro_dcm/forward_models/erp_leadfield.py`.
  </verify>
  <done>
`cmc_default_pj()` argmax==2 (!=6); the distinct-valued kron column-major guard passes (block
s == p_j[s]*L_spatial at columns [s*n:(s+1)*n]); LFP identity + projection-through-identity
guards pass; all lead-field outputs float64. `erp_leadfield.py` exports the five functions;
`__init__` re-exports them.
  </done>
</task>

<task type="auto">
  <name>Task 2: ERPDCMForward (additive append) + simulator scalp path</name>
  <files>src/pyro_dcm/inference/forward_models.py, src/pyro_dcm/simulators/erp_simulator.py, tests/test_erp_leadfield.py</files>
  <action>
(a) APPEND `class ERPDCMForward` to `inference/forward_models.py` AFTER `LatentCircuitForward`
(the exact additive 4th-implementor precedent). DO NOT edit the `ForwardModel` Protocol, the
existing forward classes, or `variational_laplace.py`. Implement all 8 members per the
`35-RESEARCH.md` "ForwardModel protocol contract" table:
  - Constructor stores ERP-specific needs as args (NOT protocol methods, pitfall B2):
    `l_full (Nc,8n)`, `x_design (Cnd,n_effects)`, `b_masks (list[(n,n)])`, `a_masks
    (tuple of 4 (n,n) -- the fwd/fwd/bwd/bwd routing graph)`, `c_mask (n,n_inp)`, integration
    grid `dt, ns, ons_ms, dur_ms, sus`. Cast tensors to float64.
  - `residual_is_complex` property -> `False`.
  - `param_count(n)` = `4*n*n + n*n_inp + 4*n + 4*n + n + 2*n_inp` (A{1..4}+C+T+G+S+R; L/J held
    FIXED in the precomputed `l_full`, Open Q3 RESOLVED -> lock this FROZEN ordering).
  - `pack_params(**kw)` -> `cat([A_free(4nn), C_free(n*n_inp), T(4n), G(4n), S(n), R(2*n_inp)])`
    -- FROZEN ordering; `unpack_params` reverses the identical slice order into the dict
    `{A_free(4,n,n), C_free, T, G, S, R}`.
  - `build_prior_cov(n, pv, a_mask)` -- flatten the `cmc_prior_moments` variances IN PACK ORDER
    (A `mask/16`, C `mask/32`, T/G `1/32`, S `1/64`, R `1/16`), zeroing absent A/C entries via
    the stored `a_masks`/`c_mask` so `_spm_svd` drops them (same idiom as
    `LatentCircuitForward.build_prior_cov`). IGNORE the engine-supplied scalar `a_mask`; use the
    OWN stored 4-block `a_masks` (Open Q4 RESOLVED -- mirror how `TaskDCMForward` stores `c_mask`
    internally).
  - `build_precision(observed)` -> `([torch.eye(observed.numel(), dtype=float64)], 1)` (identity
    v1; AR(1) `spm_Q` deferred to a later milestone).
  - `predict(theta, observed, n, **context)` -- lazy-import `cmc_network_f`,
    `apply_condition_modulation`, `integrate_local_linearization`, `cmc_flatten`,
    `erp_gaussian_input`, `project_to_scalp` (mirror `TaskDCMForward`'s lazy import so
    import-time coupling stays zero). Per the research `predict` sketch: unpack -> rebuild the
    Gaussian drive from `R` via `erp_gaussian_input` -> `x0 = zeros(8n)` -> loop conditions:
    `apply_condition_modulation(p, x_design[c])` -> `integrate_local_linearization(f_c, x0,
    inputs, dt)` -> `project_to_scalp(traj, l_full)` -> stack to `(Cnd, ns, Nc)` ->
    `if observed.ndim >= 3: y = y[:, :observed.shape[1]]` -> `return y.reshape(-1)`. Document the
    locked `(Cnd,ns,Nc)` C-order stacking layout in the docstring BEFORE the return (gap 4).
  - `build_result(theta, a_mask, n, **context)` -> `{"theta_post": {...A,C,G,T,S,R, "B":...},
    "predicted_output": (Cnd,ns,Nc)}`.

(b) EXTEND `simulators/erp_simulator.py`: add an optional `l_full: Tensor | None = None` arg to
`simulate_erp_dcm`. When supplied, project the existing per-condition flat trajectory
`(ns, 8n)` (before the `(ns,n,8)` reshape) via `project_to_scalp` to `(ns, Nc)`, stack to
`(Cnd, ns, Nc)`, and add keys `"scalp": (Cnd,ns,Nc)` and `"difference_wave_scalp":
scalp[1]-scalp[0]` (None when Cnd<2). Keep ALL existing `states`/`pst`/`inputs`/
`difference_wave` keys byte-untouched (new keys only). Extend the docstring: the scalp
difference wave's NON-ZEROness is the Phase-35 gate; the negative-going/frontal SIGN is Phase 36
(needs ECD orientation + MNI coords -- Fact 6). Do NOT assert sign here.

(c) Add tests to `tests/test_erp_leadfield.py`:
  - `ERPDCMForward` is a `runtime_checkable` `ForwardModel` instance; `param_count`,
    `pack`/`unpack` round-trip (pack(unpack(theta))==theta), `build_precision` returns an
    identity of size `Cnd*ns*Nc`.
  - `predict` under BOTH shapes: a `(Cnd,ns,Nc)` observed and a flat `observed.reshape(-1)`
    return identical flat output (the FD-vs-main-loop ndim guard, pitfall B3).
  - simulator scalp: `simulate_erp_dcm(..., l_full=build_lead_field(e_2, eye(n)))` returns
    `scalp (Cnd,ns,Nc)`; `difference_wave_scalp` is finite and `max|diff| > 0` on the planted
    B-wired net AND `== 0` on a B-omitted control (LEAD-03 non-zero gate). Do NOT assert sign.
  </action>
  <verify>
`pytest tests/test_erp_leadfield.py -k "forward or predict or scalp or difference" -q` passes.
`python -c "from pyro_dcm.inference.forward_models import ForwardModel, ERPDCMForward; print('ok')"`.
`git diff --stat src/pyro_dcm/inference/forward_models.py` shows ONLY additions (append a class;
no deletions inside existing class bodies); confirm `class ERPDCMForward` is after
`class LatentCircuitForward`. `git diff src/pyro_dcm/simulators/erp_simulator.py` shows only the
new `l_full` arg + the two new keys + docstring (existing keys untouched).
`ruff check src/pyro_dcm/inference/forward_models.py src/pyro_dcm/simulators/erp_simulator.py`.
  </verify>
  <done>
`ERPDCMForward` implements the 8 protocol members; `isinstance(ERPDCMForward(...),
ForwardModel)` is True; pack/unpack round-trips; `predict` returns identical output for the
3-D and flat-FD `observed`; the simulator returns `scalp`+`difference_wave_scalp` with the diff
non-zero (B wired) / zero (control). Protocol + VL engine + existing forward classes
unmodified.
  </done>
</task>

<task type="auto">
  <name>Task 3: LEAD-06 VL round-trip (protocol confirmation, laptop, MEASURE walltime)</name>
  <files>tests/test_erp_leadfield.py</files>
  <action>
Add the LEAD-06 VL round-trip to `tests/test_erp_leadfield.py` (laptop, no MATLAB, single
seed). This is a protocol-wiring confirmation, NOT a parity gate -- document it as such.
  - Plant CMC params on a SMALL net (n=2 or 3 for speed; net size decision RESOLVED per Open Q5):
    a live A graph (reciprocal edges so it is identifiable), a C mask driving the input
    source(s), a non-trivial `B` (deviant), and a perturbed `G[:,0]` precision knob -- so
    recovery has signal.
  - `l_full = build_lead_field(cmc_default_pj(), lfp_spatial(ones(n), n))`.
  - `simulate_erp_dcm(p, x_design, n, l_full=l_full)` -> `scalp (Cnd,ns,Nc)`; add light Gaussian
    noise so the residual is non-degenerate.
  - `result = run_variational_laplace_generic(ERPDCMForward(l_full=l_full, x_design=...,
    a_masks=..., b_masks=..., c_mask=..., dt=0.004, ns=128, ...), scalp_obs, a_mask=<union
    graph>, n_regions=n, max_iter<=32, ...)`.
  - Assert `result.theta_post["A"]` / `C` / `G` / `T` recovered within a tolerance of planted
    (per-region RMSE or an R2-style metric; reuse the VLREC metric idioms already in the repo).
    Tolerance is loose -- this confirms the param_count -> build_prior_cov -> _spm_svd -> predict
    -> ReML -> build_result chain runs end-to-end, NOT SPM parity.
  - MEASURE and PRINT the laptop wall-time. The CLAUDE.md >3 min rule applies: if a SINGLE-seed
    n=2-3 / <=32-iter fit exceeds ~3 min on the laptop, STOP and record that it must move to M3
    (do NOT silently run a long job). A multi-seed recovery sweep is explicitly OUT of scope here
    (defer to a non-blocking M3 job).
  Mark the test `@pytest.mark.vl` (and `slow` if it approaches the budget) consistent with the
  existing VL-test markers.
  </action>
  <verify>
`pytest tests/test_erp_leadfield.py -m vl -q` passes and the printed wall-time is recorded in
the test output (and later in the SUMMARY). Full file: `pytest tests/test_erp_leadfield.py -q`
green. Regression: `pytest tests/test_erp_coupled_system.py tests/test_spm_erp_multisource_validation.py -q`
still green (the frozen network forward + integrator + bridge are untouched).
  </verify>
  <done>
The VL round-trip recovers planted A/C/G/T within tolerance on the n=2-3 net in a single
<=32-iter laptop fit; wall-time measured and under ~3 min (else flagged for M3); the test is
documented as protocol confirmation, not parity. Phase-33/34 suites stay green.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_erp_leadfield.py -q` all green (structural guards + forward + simulator +
  VL round-trip).
- `pytest tests/test_erp_coupled_system.py tests/test_spm_erp_dcm_validation.py tests/test_spm_erp_multisource_validation.py -q`
  still green (additive-only; the frozen Phase-33/34 forward, condition-B, integrator, and
  bridge are byte-untouched).
- `git diff --stat` shows ONLY: new `erp_leadfield.py`, new `test_erp_leadfield.py`, the appended
  `ERPDCMForward` class + new simulator keys + the two `__init__`/import appends -- NO edits to
  existing class bodies, the Protocol, or `variational_laplace.py`.
- `ruff check` + `ruff format --check` clean on all changed files; mypy delta no worse than the
  pre-existing baseline.
- float64 enforced at the lead-field + projection boundary; `cmc_default_pj()` argmax==2 (!=6);
  the kron column-major guard passes against a distinct-valued `L_spatial`.
</verification>

<success_criteria>
- LEAD-01: `erp_leadfield.py` builds `L_full = kron(P.J, L_spatial)` column-major + projects
  `y = (x - x0) @ L_full.T`; LFP diagonal spatial model (identity default). MET when the LFP
  identity + projection guards pass.
- LEAD-02: `P.J` default == state index 2 (not 6); kron column-major verified against a
  distinct-valued `L_spatial`. MET when both guard tests pass.
- LEAD-03 (Wave-1 portion): scalp difference wave computed and asserted NON-ZERO (B wired) /
  zero (control); sign deferred to Phase 36. MET when the simulator difference test passes.
- LEAD-04: `ERPDCMForward` implements the 8-member protocol, additive append, identity precision
  v1, `predict -> (Cnd,ns,Nc)`. MET when `isinstance(..., ForwardModel)` + pack/unpack + predict
  ndim tests pass with zero protocol/engine edits.
- LEAD-06: VL round-trip recovers planted CMC params on a small net. MET when the recovery test
  passes within tolerance and wall-time is recorded.
</success_criteria>

<output>
After completion, create
`.planning/phases/35-single-dipole-lead-field-scalp-projection-erpdcmforward/35-01-SUMMARY.md`.
Record: the FROZEN pack/unpack ordering (`A(4nn)+C+T(4n)+G(4n)+S(n)+R(2n_inp)`, L/J fixed in
`l_full`), the locked `(Cnd,ns,Nc)` C-order stacking layout, the measured VL round-trip
wall-time, and confirmation that Wave 2/3 can assert against `build_lead_field` + `project_to_scalp`.
</output>
