---
phase: 35-single-dipole-lead-field-scalp-projection-erpdcmforward
plan: 03
type: execute
wave: 3
depends_on: ["35-01", "35-02"]
files_modified:
  - tests/test_spm_erp_leadfield_validation.py
autonomous: true

must_haves:
  truths:
    - "Rung 1 (L_full exact, no integrator): build_lead_field(cmc_default_pj(), lfp_spatial(ones(5),5)) matches the exported L_full element-wise <=1e-12; PLUS the distinct-valued kron column-major check and the P.J==index-2 (not 6) guard -- the single most important lead-field guard (LEAD-02, C5)"
    - "Rung 2 (scalp scheme rung): driving the exp-Euler loop with SPM's OWN per-condition Qupd -> source traj ~machine-eps -> project_to_scalp -> matches y_scalp{c} on RELATIVE error max|diff|/max|y| <= 1e-12 (isolates projection from Jacobian method; 34-03-D1 relative-error convention at N=5)"
    - "Rung 3 (scalp FD-Jacobian rung): build the operator from the spm_diff-matched J0 (the _spm_diff_jacobian helper, dx=exp(-8)) -> project -> matches y_scalp{c} <=1e-8 (bit-exact-to-SPM confirmation rung)"
    - "Rung 4 (THE LEAD-05 GATE, shipped-jacrev production path, <=1e-7): full production integrate_local_linearization(cmc_network_f,...) -> project_to_scalp -> matches y_scalp{c} <=1e-7. PRODUCTION-PATH GATE DECISION: MEASURE the scalp jacrev floor first; because the LFP lead field is IDENTITY (P.L=ones -> no amplification) the propagated Phase-34 source floor (~4.7e-8) projects BELOW 1e-7, so the production path is GATED directly at <=1e-7 (unlike Phases 33/34 which could only MEASURE the shipped path). Record the measured floor in the test + SUMMARY"
    - "Rung 5 (difference wave, LEAD-03): production-path scalp[1]-scalp[0] matches exported diff_wave <=1e-7 AND max|diff_wave| > 0 (non-zero -- B is wired via _MS_B_EDGE/_MS_B_DIAG). The negative-going/frontal SIGN is NOT gated here (deferred to Phase 36, Fact 6); the source-level sign direction MAY be recorded as a non-gating diagnostic"
    - "The locked observation stacking layout (Cnd,ns,Nc) + C-order reshape(-1) is honored: the .mat y_scalp cell {Cnd}x(ns,Nc) is torch.stack'd to (Cnd,ns,Nc) and flattened identically to ERPDCMForward.predict (gap 4)"
    - "The test reconstructs the EXACT P by importing the _MS_ constants + _ms_log_block + _erp_gaussian_u_grid from validation.export_to_mat (identical P+drive -> pitfall V1 satisfied), reusing the _reference_p / _spm_diff_jacobian / fixture-loader pattern from tests/test_spm_erp_multisource_validation.py"
    - "Assertions are torch-vs-frozen-arrays (deterministic f64) so the suite RUNS AND PASSES on the laptop; @pytest.mark.spm/slow retained for the optional M3 re-run; skipif(not erp_leadfield_fixtures.mat.exists)"
  artifacts:
    - path: "tests/test_spm_erp_leadfield_validation.py"
      provides: "the staged scalp parity ladder (5 rungs) asserting build_lead_field/project_to_scalp against the frozen LFP fixtures"
      contains: "erp_leadfield_fixtures"
  key_links:
    - from: "tests/test_spm_erp_leadfield_validation.py"
      to: "validation/data/erp_leadfield_fixtures.mat (L_full, y_scalp, diff_wave, meta)"
      via: "scipy.io.loadmat fixture loader, skipif(not exists)"
      pattern: "erp_leadfield_fixtures"
    - from: "tests/test_spm_erp_leadfield_validation.py:_reference_p"
      to: "validation.export_to_mat _MS_ constants + _ms_log_block + _erp_gaussian_u_grid"
      via: "import the locked topology so P+drive are identical (pitfall V1)"
      pattern: "_MS_"
    - from: "tests/test_spm_erp_leadfield_validation.py:rung4"
      to: "integrate_local_linearization(cmc_network_f,...) + project_to_scalp"
      via: "production path measured scalp jacrev floor gated <=1e-7 (LFP identity = no amplification)"
      pattern: "project_to_scalp"
---

<objective>
The scalp parity gate: assert the pure-torch lead field + projection element-wise against the
frozen `spm_lx_erp` LFP fixtures, in a staged ladder (L_full exact -> scalp scheme rung ->
scalp FD-Jacobian rung -> the production-path <=1e-7 GATE -> difference wave). This is Wave 3:
torch-vs-frozen-arrays, deterministic f64, so it RUNS AND PASSES on the laptop (the laptop run
is authoritative -- 33-03-D1/34-03-D2).

Purpose: LEAD-05 is the headline Phase-35 parity gate. The staged ladder (V5 discipline) means
a failure localizes to exactly one stage: the kron/P.J algebra (rung 1), the projection (rung
2), the Jacobian method (rung 3), the production path (rung 4), or the difference (rung 5).
The KEY Phase-35 finding lives in rung 4: because the LFP lead field is the IDENTITY (no
amplification), the shipped jacrev source floor (~4.7e-8 from Phase 34) projects BELOW the
<=1e-7 scalp tolerance -- so for the first time the PRODUCTION integrator path can be GATED
directly at <=1e-7, not merely measured-not-gated (as in Phases 33/34). This must be MEASURED
and the measured floor recorded.

Output: `tests/test_spm_erp_leadfield_validation.py` (NEW) -- 5 rungs, green on the laptop,
gated on the committed `erp_leadfield_fixtures.mat`. Additive-only (one new test file, zero
source edits).
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-single-dipole-lead-field-scalp-projection-erpdcmforward/35-RESEARCH.md

# The ladder + helper pattern to mirror (_reference_p, _spm_diff_jacobian, fixture loader, the 6 rungs)
@tests/test_spm_erp_multisource_validation.py
# The lead field + projection to assert (built in 35-01)
@src/pyro_dcm/forward_models/erp_leadfield.py
# The _MS_ constants + helpers to import for the exact P + drive (pitfall V1)
@validation/export_to_mat.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture loader + Rungs 1-3 (L_full exact, scalp scheme, scalp FD-Jacobian)</name>
  <files>tests/test_spm_erp_leadfield_validation.py</files>
  <action>
Create `tests/test_spm_erp_leadfield_validation.py` mirroring
`tests/test_spm_erp_multisource_validation.py`:
  - `_FIXTURE_PATH = validation/data/erp_leadfield_fixtures.mat`; module-level
    `pytest.mark.skipif(not _FIXTURE_PATH.exists())`; an `fx()` fixture loading the `.mat` via
    `scipy.io.loadmat` and exposing `L_full`, `y_scalp` (cell -> per-condition `(ns,Nc)`),
    `diff_wave`, `meta`.
  - Reuse `_reference_p()` reconstructing the EXACT `P` by importing the `_MS_` constants +
    `_ms_log_block` + `_erp_gaussian_u_grid` from `validation.export_to_mat` (identical P + drive
    -- pitfall V1). Reuse the `_spm_diff_jacobian(dx=exp(-8))` helper + `_update_operator` +
    `integrate_local_linearization`. Mark `@pytest.mark.spm` / `slow` (optional M3 re-run) but
    keep the body fixture-keyed so it runs+passes on the laptop.
  - `test_pre_asserts`: `meta.D==1`, `meta.nargout_Mf==2`, `meta.N==5`, `meta.Nc==5`,
    `meta.dipfit_type=='LFP'`, float64 throughout, `P.J` one-hot at index 2.
  - **Rung 1 (L_full exact, LEAD-02, the most important guard):** `L_spatial =
    lfp_spatial(ones(5), 5)` (identity), `p_j = cmc_default_pj()`,
    `build_lead_field(p_j, L_spatial)` vs the exported `L_full` element-wise `<=1e-12`. PLUS the
    distinct-valued kron column-major check (block `s` == `p_j[s]*L_spatial` at cols
    `[s*5:(s+1)*5]`) and the `P.J argmax==2, !=6` guard.
  - **Rung 2 (scalp scheme rung):** drive the exp-Euler loop with SPM's OWN per-condition `Qupd`
    (recompute from `_reference_p` via the `_spm_diff_jacobian`-matched operator, or reuse the
    multisource pattern) -> source traj ~machine-eps -> `project_to_scalp(traj, L_full)` -> assert
    vs `y_scalp{c}` on RELATIVE error `max|diff|/max|y| <= 1e-12` (34-03-D1: states reach ~O(40)
    at N=5, so gate the scale-invariant relative floor, not absolute).
  - **Rung 3 (scalp FD-Jacobian rung):** build the operator from the `spm_diff`-matched `J0`
    (the `_spm_diff_jacobian` helper) -> integrate -> `project_to_scalp` -> assert vs `y_scalp{c}`
    `<=1e-8`.
Honor the locked `(Cnd,ns,Nc)` C-order stacking when comparing: `torch.stack` the per-condition
`(ns,Nc)` to `(Cnd,ns,Nc)` and flatten identically to `ERPDCMForward.predict`.
  </action>
  <verify>
`pytest tests/test_spm_erp_leadfield_validation.py -k "pre_asserts or rung1 or rung2 or rung3" -q`
passes on the laptop (fixture present). Print the measured floors per rung. `ruff check
tests/test_spm_erp_leadfield_validation.py`. If the fixture is absent the tests SKIP cleanly
(not error).
  </verify>
  <done>
Rung 1 L_full element-wise `<=1e-12` (+ kron column-major + P.J index-2 guards); rung 2 scalp
relative error `<=1e-12`; rung 3 scalp FD-Jacobian `<=1e-8`. All pre-asserts (D=1, Nc=5, LFP,
float64) pass. P reconstructed from the imported `_MS_` constants.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rung 4 (LEAD-05 production-path <=1e-7 GATE, MEASURED) + Rung 5 (difference wave)</name>
  <files>tests/test_spm_erp_leadfield_validation.py</files>
  <action>
Add the two gate rungs to `tests/test_spm_erp_leadfield_validation.py`:
  - **Rung 4 -- THE LEAD-05 GATE (shipped-jacrev production path, `<=1e-7`):** run the FULL
    production `integrate_local_linearization(cmc_network_f, x0, inputs, dt)` per condition
    (exact-AD Jacobian, the shipped path) -> `project_to_scalp(traj, L_full)` -> assert vs
    `y_scalp{c}` `<=1e-7`. FIRST MEASURE the scalp jacrev floor and PRINT it. Bake the
    production-path gate decision into the test body + an inline comment: the Phase-34 shipped
    jacrev SOURCE floor was 4.70e-8 (`34-03-SUMMARY` rung 6); the LFP default lead field is the
    IDENTITY (`P.L=ones` -> `L_spatial=I`), so the projection does NOT amplify -> the scalp jacrev
    floor stays ~4.7e-8 `< 1e-7`. Therefore GATE the production integrator directly at `<=1e-7`
    (unlike Phases 33/34, where 4.7e-8 > 1e-8 forced measure-not-gate). Add the caveat comment:
    if a non-identity LFP gain `P.L != 1` is ever used, the projection scales the floor by
    `max|P.L|` -- MEASURE and re-confirm `<=1e-7` then. Rungs 2-3 remain as diagnostic
    localization rungs.
  - **Rung 5 -- difference wave (LEAD-03):** production-path `scalp[1] - scalp[0]` vs the exported
    `diff_wave` `<=1e-7`, AND assert `max|diff_wave| > 0` (non-zero -- `B` is wired:
    `_MS_B_EDGE`/`_MS_B_DIAG`). Do NOT gate the negative-going/frontal SIGN (deferred to Phase 36,
    Fact 6); OPTIONALLY record the source-level sign direction (deviant sp-voltage vs standard at
    the precision nodes) as a non-gating diagnostic print.
Tolerances are element-wise forward agreement only (V2 -- no absolute-F, no `Cp`; the forward has
no normalization freedom). Anchor to the Phase-33/34 measured floors (matrix_exp 8.6e-11; scheme
8.2e-13 rel at N=5; jacrev source 4.7e-8) as small multiples (V3).
  </action>
  <verify>
`pytest tests/test_spm_erp_leadfield_validation.py -q` -- all 5 rungs green on the laptop; the
rung-4 measured scalp jacrev floor is PRINTED and is `< 1e-7` (record the exact value).
Regression: `pytest tests/test_erp_leadfield.py tests/test_spm_erp_multisource_validation.py -q`
still green. `git diff --stat` shows ONLY the one new test file (zero source edits). `ruff check`
+ `ruff format --check` clean.
  </verify>
  <done>
Rung 4 production-path scalp ERP `<=1e-7` (gate PASSES; measured jacrev floor recorded `< 1e-7`,
confirming the LFP-identity no-amplification finding); rung 5 difference wave `<=1e-7` AND
non-zero. LEAD-05 met; LEAD-03 non-zero met (sign deferred). Additive-only.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_spm_erp_leadfield_validation.py -q` -- all 5 rungs green on the laptop
  (deterministic f64 against the committed fixture).
- The rung-4 production-path gate PASSES at `<=1e-7` and the measured scalp jacrev floor is
  recorded (`< 1e-7`), validating the LFP-identity no-amplification finding (the load-bearing
  Phase-35 assumption).
- Rung 5 difference wave `<=1e-7` AND non-zero; sign NOT gated (Phase 36).
- Additive-only: one new test file, zero source edits (`git diff --stat` confirms); the
  Phase-33/34 ladders + `test_erp_leadfield.py` stay green.
- `ruff check` + `ruff format --check` clean; tests SKIP cleanly when the fixture is absent.
- The locked `(Cnd,ns,Nc)` C-order stacking is honored; P reconstructed from the imported `_MS_`
  constants (pitfall V1).
</verification>

<success_criteria>
- LEAD-05 (PARITY GATE): scalp ERP matches `spm_gen_erp + spm_lx_erp` (LFP mode) on frozen
  fixtures `<=1e-7` via the production `integrate_local_linearization` path. MET when rung 4
  passes and the measured jacrev floor is recorded `< 1e-7`.
- LEAD-02: `L_full` exact `<=1e-12` (kron column-major + P.J index-2). MET when rung 1 passes.
- LEAD-03: difference wave `<=1e-7` AND non-zero; sign deferred to Phase 36. MET when rung 5
  passes.
- Production-path gate decision (the key Phase-35 finding) is MEASURED, recorded, and baked into
  the test as the LEAD-05 gate with the non-identity-`P.L` caveat.
</success_criteria>

<output>
After completion, create
`.planning/phases/35-single-dipole-lead-field-scalp-projection-erpdcmforward/35-03-SUMMARY.md`.
Record: the measured scalp floors per rung (especially the rung-4 production-path jacrev floor
and its margin below 1e-7), confirmation the LFP-identity no-amplification finding held, the
difference-wave non-zero magnitude, and that LEAD-05/02/03 are gated green on the laptop.
</output>
