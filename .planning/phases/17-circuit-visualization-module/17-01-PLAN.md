---
plan_id: "17-01"
phase: 17-circuit-visualization-module
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pyro_dcm/utils/circuit_viz.py           # NEW
  - src/pyro_dcm/utils/__init__.py              # re-export edit
  - tests/test_circuit_viz.py                   # NEW
  - .planning/REQUIREMENTS.md                   # append VIZ-NN block
autonomous: true

# Planner-level locked decisions resolving the 5 escalation questions from
# 17-RESEARCH.md. These are binding for executors.
locked_decisions:
  - id: V1
    summary: "CircuitVizConfig gains a pass-through `extras: dict = field(default_factory=dict)` field. `load()` collects every top-level JSON key NOT in the first-class field list into `extras`; `to_dict()` merges `extras` LAST so first-class fields always win on key collision."
    rationale: "Resolves open question 1. `configs/heart2adapt_dcm_config.json` contains `_study`, `_description`, `node_info`, `node_positions`, `svg_edges`, `b_overlays` which are NOT on the handoff dataclass. Round-trip A-01 requires preservation. `extras` is non-breaking, forward-compat, and avoids bloating the first-class surface with HEART2ADAPT-specific fields the renderer treats as optional."
    scope: "CircuitVizConfig dataclass + to_dict + load."
  - id: V2
    summary: "`from_model_config` REQUIRES `model_cfg['region_colors']` — raises `ValueError` with expected-vs-actual message if absent or if `len(region_colors) != len(regions)`. No matplotlib / tab10 fallback."
    rationale: "Resolves open question 2. Matches project convention 'Error messages must include expected vs actual values' (CLAUDE.md). Implicit color synthesis produces non-reproducible JSON across matplotlib versions; explicit caller responsibility is cleaner. `palette` (a separate dict for semantic color-role names) remains OPTIONAL via kwarg per handoff docstring."
    scope: "from_model_config input validation."
  - id: V3
    summary: "JSON Schema codification (a `.schema.json` file) is DEFERRED to a later v0.4.x patch. Phase 17 treats the `CircuitVizConfig` dataclass + HANDOFF_viz.md as the informal schema source-of-truth."
    rationale: "Resolves open question 3. Avoids new `jsonschema` dependency and schema-drift-vs-runtime-mismatch risk. Dataclass field list is narrow (13 first-class + `extras`) and testable directly."
    scope: "out of scope for Phase 17."
  - id: V4
    summary: "`from_posterior` KEEPS the handoff's pre-flattened signature `(planned, posterior_means: dict[str, list[list[float]]])`. A sibling module-level convenience helper `flatten_posterior_for_viz(posterior, mat_order, b_masks=None)` is provided for the common Pyro-DCM workflow but is NOT the primary API."
    rationale: "Resolves open question 4. Handoff signature is unambiguous; caller controls the A-transform choice (`A` vs `A_free`) and the B-source choice (`B` stacked vs `B_free_j` raw + `parameterize_B`). Auto-flattening hides these decisions. Helper provides convenience without making it load-bearing."
    scope: "from_posterior signature + new helper function in circuit_viz.py."
  - id: V5
    summary: "Headless-browser render acceptance is OUT OF SCOPE. Acceptance gates are: (i) JSON schema round-trip equality, (ii) first-class key set matches the v1 schema, (iii) structural renderer-field-access patterns per RESEARCH.md 'Renderer behavior on missing fields' table (all optional fields have safe fallbacks per the template grep)."
    rationale: "Resolves open question 5. Matches user directive 'visualizer is distinct in its acceptance from fitting work'. Playwright/puppeteer infra is disproportionate for a ~150-LOC serializer."
    scope: "tests/test_circuit_viz.py acceptance strategy."
  - id: V6
    summary: "NaN/Inf validation: `from_posterior` raises `ValueError` with the offending matrix key if any cell is NaN or Inf. Policy uses `math.isnan`/`math.isinf` on the flattened list values (cheaper than torch); applied BEFORE the deepcopy. Error message format: `f'posterior_means[{key!r}] contains NaN/Inf at [row={i}, col={j}]; expected finite float for dcm_circuit_explorer/v1'`."
    rationale: "Research Risk 9. `json.dumps` with `allow_nan=False` would raise later but with a less-localised message; post-SVI NaN is a known Phase 16 scenario. Catching at the serializer boundary gives callers a clear pointer to the failing site."
    scope: "from_posterior input validation."
  - id: V7
    summary: "`mat_order` emission is ALWAYS deterministic: `['A'] + sorted(model_cfg['B_matrices'].keys()) + (['C'] if 'C_matrix' in model_cfg else [])`. Never trust dict insertion order from caller-supplied `B_matrices`."
    rationale: "Research Risk 4. Python dict ordering is insertion-ordered since 3.7, but callers may construct B_matrices in arbitrary key order. Sorted keys guarantees reproducible JSON across runs and test seeds."
    scope: "from_model_config mat_order construction."
  - id: V8
    summary: "Tensor inputs in `model_cfg` values (torch.Tensor / ndarray) are accepted and converted to `list[list[float]]` via a private `_to_list_of_list(x)` helper that dispatches on type: torch.Tensor -> `.detach().cpu().tolist()`; ndarray -> `.tolist()`; list -> passthrough. Raises with expected-vs-actual on unsupported types."
    rationale: "Research A-08. Callers building `model_cfg` from live `task_dcm_model` state will have tensors, not pre-converted lists. Accepting both keeps the API ergonomic without sacrificing determinism (tolist is lossless for float64)."
    scope: "_to_list_of_list private helper + from_model_config matrix field serialization."

must_haves:
  truths:
    - "`src/pyro_dcm/utils/circuit_viz.py` (NEW) defines `CircuitVizConfig` dataclass with fields: `schema: str = 'dcm_circuit_explorer/v1'`, `status: str = 'planned'`, `meta: dict`, `palette: dict`, `regions: list[str]`, `region_colors: list[str]`, `matrices: dict`, `mat_order: list[str]`, `phenotypes: list[dict]`, `hypotheses: list[dict]`, `drugs: list[dict]`, `peb: dict`, `fitted_params: dict | None = None`, AND `extras: dict = field(default_factory=dict)` (V1). Matches handoff verbatim except for the `extras` pass-through addition."
    - "`CircuitVizConfig.to_dict()` returns exactly the 13 first-class top-level keys from the handoff schema (`_schema`, `_status`, `meta`, `palette`, `regions`, `region_colors`, `matrices`, `mat_order`, `phenotypes`, `hypotheses`, `drugs`, `peb`, `fitted_params`) PLUS any keys present in `self.extras` (merged last; first-class keys win on collision). This enables round-trip preservation of `_study`, `_description`, `node_info`, `node_positions`, `svg_edges`, `b_overlays` from `configs/heart2adapt_dcm_config.json`."
    - "`CircuitVizConfig.export(path)` writes `json.dumps(self.to_dict(), indent=2)` to `Path(path)` and returns the path. Calls `_validate_no_nan()` internally before writing (V6 scope extension — also applies to export())."
    - "`CircuitViz.from_model_config(model_cfg, *, phenotypes=None, hypotheses=None, drugs=None, peb=None, palette=None) -> CircuitVizConfig` REQUIRES `model_cfg['regions']`, `model_cfg['region_colors']`, `model_cfg['A_prior_mean']`; raises `ValueError` with expected-vs-actual on missing or length-mismatched `region_colors` (V2). Optional model_cfg keys: `B_matrices: dict`, `C_matrix: list[list[float]] | Tensor`, `B_modulators: dict`, `C_inputs: list[str]`, `meta: dict`, `peb_covariates: list[dict]`."
    - "`from_model_config` emits `mat_order = ['A'] + sorted(model_cfg.get('B_matrices', {}).keys()) + (['C'] if 'C_matrix' in model_cfg and len(model_cfg['C_matrix']) > 0 else [])` (V7). This is sort-deterministic regardless of caller dict insertion order."
    - "`from_model_config` builds `matrices` as `{'A': {'label': 'A — Intrinsic', 'color': '#374151', 'vals': <A_prior_mean as list[list[float]]>}}` plus per-B entries `{'B{j}': {'label': mods.get('label'), 'color': mods.get('color'), 'modulator': mods.get('modulator'), 'modulator_display': mods.get('modulator_display'), 'vals': <B_matrices[key] as list[list[float]]>}}` (label/color/modulator fields OMITTED from the emitted dict if the modulator sub-dict doesn't supply them — None values MUST NOT be written to JSON) plus `'C': {'label': 'C — Driving', 'color': '#6B7280', 'vals': <C_matrix>, 'inputs': model_cfg.get('C_inputs', ['u'])}` when C is in mat_order."
    - "`from_model_config` accepts `torch.Tensor`, `numpy.ndarray`, and `list[list[float]]` values for `A_prior_mean`, `B_matrices[key]`, and `C_matrix` via private `_to_list_of_list` helper (V8). All matrix `vals` in the output dict are `list[list[float]]` (never tensor, never ndarray, never nested tuple). Test A-07 is a direct regression gate for this invariant."
    - "`from_model_config` synthesizes `meta` as `{**model_cfg.get('meta', {})}` (shallow copy; pass-through); and `peb` as `{**({'covariates': model_cfg['peb_covariates']} if 'peb_covariates' in model_cfg else {}), **(peb or {})}` — the caller `peb` kwarg overrides any value derived from `model_cfg['peb_covariates']` (explicit > derived)."
    - "`from_model_config` maps the kwargs `phenotypes`, `hypotheses`, `drugs`, `palette` directly to the dataclass fields; defaults to `[]`/`[]`/`[]`/`{}` respectively when None. Empty collections pass through to `to_dict()` as `[]`/`{}` (not missing keys) — test A-10 regression gate."
    - "`CircuitViz.from_posterior(planned, posterior_means) -> CircuitVizConfig` is implemented VERBATIM per handoff (`docs/HANDOFF_viz.md:193-212`) BUT augmented with a `_validate_no_nan(posterior_means)` call BEFORE `copy.deepcopy(planned)` (V6). `_validate_no_nan` iterates over each matrix's flat values and raises `ValueError` with the offending `(key, i, j)` tuple if any NaN/Inf found."
    - "`CircuitViz.from_posterior` does NOT mutate the `planned` argument (deepcopy semantics). Test A-03 is a direct regression gate."
    - "`CircuitViz.load(path) -> CircuitVizConfig` is implemented VERBATIM per handoff (`docs/HANDOFF_viz.md:214-232`) BUT augmented with extras collection: after populating the 13 first-class fields from `data.get(...)`, collect every remaining top-level key into `cfg.extras`: `cfg.extras = {k: v for k, v in data.items() if k not in _FIRST_CLASS_KEYS}` where `_FIRST_CLASS_KEYS` is a module-level frozenset of the 13 JSON key names."
    - "A module-level convenience helper `flatten_posterior_for_viz(posterior: dict, mat_order: list[str], b_masks: list[torch.Tensor] | None = None) -> dict[str, list[list[float]]]` is exported (V4). It consumes the `extract_posterior_params` output dict and returns the `dict[str, list[list[float]]]` payload `from_posterior` expects. Handles `'A' -> posterior['A']['mean'].tolist()` (preferred; otherwise falls back to `parameterize_A(posterior['A_free']['mean']).tolist()` via import from `pyro_dcm.forward_models.neural_state` IF that path is needed), `'C' -> posterior['C']['mean'].tolist()`, and `'B{j}' -> posterior['B']['mean'][j].tolist()` if `'B'` key present, else `parameterize_B(posterior[f'B_free_{j}']['mean'].unsqueeze(0), b_masks[j].unsqueeze(0)).squeeze(0).tolist()`. The helper is DOCUMENTED in the module docstring and the CircuitViz class docstring as the recommended flatten path but is NOT auto-invoked."
    - "`src/pyro_dcm/utils/__init__.py` adds imports and __all__ entries for `CircuitViz`, `CircuitVizConfig`, and `flatten_posterior_for_viz`. Style matches existing block: `from pyro_dcm.utils.circuit_viz import CircuitViz, CircuitVizConfig, flatten_posterior_for_viz` + extend `__all__` list."
    - "`tests/test_circuit_viz.py` (NEW) implements 10 structural acceptance tests A-01 through A-10 (from 17-RESEARCH.md 'Acceptance evidence') plus 2 Pyro integration smoke tests B-01, B-02 in a `TestPyroIntegration` class marked `@pytest.mark.slow`. Total ~12 test methods. Uses `torch.manual_seed(42)` + `pyro.set_rng_seed(42)` for deterministic B-01."
    - "`.planning/REQUIREMENTS.md` is appended with a new `### Circuit Visualization (v0.4.0)` section containing requirements VIZ-01 through VIZ-10 (see REQUIREMENTS.md append block below). Traceability table is updated to include VIZ entries mapped to Phase 17."

  artifacts:
    - path: "src/pyro_dcm/utils/circuit_viz.py"
      provides: "CircuitVizConfig dataclass + CircuitViz class (from_model_config, from_posterior, load) + flatten_posterior_for_viz helper"
      min_lines: 150
      max_lines: 300
      exports: ["CircuitViz", "CircuitVizConfig", "flatten_posterior_for_viz"]
      must_contain:
        - "class CircuitVizConfig:"
        - "class CircuitViz:"
        - "def from_model_config("
        - "def from_posterior("
        - "def load("
        - "def to_dict("
        - "def export("
        - "def flatten_posterior_for_viz("
        - "extras: dict"
        - "_FIRST_CLASS_KEYS"
    - path: "src/pyro_dcm/utils/__init__.py"
      provides: "Re-exports CircuitViz, CircuitVizConfig, flatten_posterior_for_viz"
      must_contain:
        - "from pyro_dcm.utils.circuit_viz import"
        - "CircuitViz"
        - "CircuitVizConfig"
        - "flatten_posterior_for_viz"
    - path: "tests/test_circuit_viz.py"
      provides: "Structural + integration tests A-01..A-10 + B-01..B-02"
      min_lines: 200
      must_contain:
        - "def test_roundtrip_heart2adapt"      # A-01
        - "def test_from_posterior_flips_status"  # A-02
        - "def test_from_posterior_no_mutation"   # A-03
        - "def test_to_dict_top_level_keys"       # A-04
        - "def test_mat_order_deterministic"      # A-05
        - "def test_schema_version"               # A-06
        - "def test_vals_are_list_of_list"        # A-07
        - "def test_tensor_input_accepted"        # A-08
        - "def test_export_roundtrip"             # A-09
        - "def test_empty_optional_collections"   # A-10
        - "class TestPyroIntegration"             # B-01/B-02
    - path: ".planning/REQUIREMENTS.md"
      provides: "VIZ-01 through VIZ-10 requirements appended"
      must_contain:
        - "### Circuit Visualization (v0.4.0)"
        - "VIZ-01"
        - "VIZ-10"

  key_links:
    - from: "src/pyro_dcm/utils/circuit_viz.py::flatten_posterior_for_viz"
      to: "src/pyro_dcm/models/guides.py::extract_posterior_params (return dict shape)"
      via: "documented consumption of posterior['A']['mean'], posterior['C']['mean'], posterior['B']['mean'][j] (with B_free_{j} + parameterize_B fallback)"
      pattern: 'posterior\[["\x27]A["\x27]\]\[["\x27]mean["\x27]\]'
    - from: "src/pyro_dcm/utils/__init__.py"
      to: "src/pyro_dcm/utils/circuit_viz.py"
      via: "re-export"
      pattern: "from pyro_dcm.utils.circuit_viz import"
    - from: "tests/test_circuit_viz.py::test_roundtrip_heart2adapt"
      to: "configs/heart2adapt_dcm_config.json"
      via: "CircuitViz.load + to_dict equality check via extras pass-through"
      pattern: 'configs[\\/]heart2adapt_dcm_config\.json'
    - from: "tests/test_circuit_viz.py::TestPyroIntegration::test_smoke_planned_to_fitted"
      to: "src/pyro_dcm/models/task_dcm_model.py (b_masks path) + extract_posterior_params"
      via: "50-step SVI run on 3-region bilinear synthetic data"
      pattern: 'task_dcm_model|extract_posterior_params'
---

<objective>
Implement `src/pyro_dcm/utils/circuit_viz.py` — a pure-Python serializer that emits `dcm_circuit_explorer/v1` JSON consumed by `docs/dcm_circuit_explorer_template.html`. Ships the `CircuitVizConfig` dataclass, the `CircuitViz` class with `from_model_config`/`from_posterior`/`load` static methods, and a `flatten_posterior_for_viz` helper bridging `extract_posterior_params` output to the `from_posterior` input shape.

Purpose: Phase 17 is the first (and currently only) phase of the v0.4.0 Circuit Explorer milestone. It unlocks pre-fit circuit visualization (planned view) and post-SVI posterior attachment (fitted view) for any Pyro-DCM model config. Acceptance is strictly serialization/schema-structural per user directive — no RECOV/RMSE/coverage/shrinkage gates apply.

Output: NEW file `src/pyro_dcm/utils/circuit_viz.py` (~150-300 LOC), NEW `tests/test_circuit_viz.py` (~200+ LOC, 12 tests), 3-line re-export edit to `src/pyro_dcm/utils/__init__.py`, and an append-only block of VIZ-01..10 requirements to `.planning/REQUIREMENTS.md`. ZERO edits to any file outside `src/pyro_dcm/utils/`, `tests/`, and `.planning/` — no changes to `task_dcm_model`, `extract_posterior_params`, `parameterize_A`, `parameterize_B`, connectivity/guides/simulators.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-circuit-visualization-module/17-RESEARCH.md
@docs/HANDOFF_viz.md
@configs/heart2adapt_dcm_config.json
@src/pyro_dcm/utils/__init__.py
@src/pyro_dcm/models/guides.py
@src/pyro_dcm/models/task_dcm_model.py
@src/pyro_dcm/forward_models/neural_state.py

<!-- CLAUDE.md project rules are authoritative: NumPy docstrings, `from __future__ import annotations`, Python 3.10+ native type syntax, line length 88, trailing underscore for fitted attributes. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CircuitVizConfig + CircuitViz core with extras pass-through</name>
  <files>src/pyro_dcm/utils/circuit_viz.py</files>
  <action>
Create `src/pyro_dcm/utils/circuit_viz.py` (NEW). Module header:

```python
"""DCM Circuit Explorer JSON serializer (dcm_circuit_explorer/v1).

Serializes a Pyro-DCM model config and/or fitted SVI posterior into the JSON
schema consumed by ``docs/dcm_circuit_explorer_template.html``. See
``docs/HANDOFF_viz.md`` for the authoritative schema and Phase 17 PLAN for
design decisions V1-V8.
"""
from __future__ import annotations
```

1. Imports: `json`, `copy`, `math`, `pathlib.Path`, `dataclasses.dataclass`/`field`, `typing.Any`, `torch` (for runtime isinstance checks on tensors; guard numpy import behind `try/except ImportError` and treat numpy-arrays as optional support).

2. Module-level constant `_FIRST_CLASS_KEYS: frozenset[str]` containing EXACTLY the 13 top-level JSON keys emitted by `CircuitVizConfig.to_dict()`: `{"_schema", "_status", "meta", "palette", "regions", "region_colors", "matrices", "mat_order", "phenotypes", "hypotheses", "drugs", "peb", "fitted_params"}`. Add a docstring noting V1 decision.

3. `_to_list_of_list(x)` private function (V8): accepts `torch.Tensor` (dispatch on isinstance), `numpy.ndarray` (if numpy available), or `list`/`tuple`. Returns `list[list[float]]`. For Tensor: `x.detach().cpu().tolist()`. For ndarray: `x.tolist()`. For list/tuple: passthrough with a nested list conversion. Raises `TypeError` with expected-vs-actual message for any other type. NumPy-style docstring.

4. `_validate_no_nan(posterior_means: dict[str, list[list[float]]]) -> None` (V6): for each matrix key, iterate flat values and raise `ValueError` with the offending `(key, i, j)` tuple if any NaN/Inf. Use `math.isnan` + `math.isinf`. NumPy-style docstring.

5. `@dataclass` `CircuitVizConfig` — VERBATIM per `docs/HANDOFF_viz.md:105-147` BUT:
   - Add `extras: dict = field(default_factory=dict)` as the LAST field (V1).
   - `to_dict()` returns the 13-key dict per handoff AND merges `extras` LAST as `{**d, **{k: v for k, v in self.extras.items() if k not in _FIRST_CLASS_KEYS}}` (first-class keys always win on collision).
   - `export(path)` calls `_validate_no_nan(self.fitted_params)` first (if `fitted_params is not None`) then writes `json.dumps(self.to_dict(), indent=2)`. Returns `Path(path)`.
   - Docstring: cite D1 / HANDOFF_viz.md + note the `_schema` JSON key vs `schema` dataclass attribute rename.

6. `CircuitViz` class — all three methods as `@staticmethod`:

   a. `from_model_config(model_cfg, *, phenotypes=None, hypotheses=None, drugs=None, peb=None, palette=None) -> CircuitVizConfig`:
      - Validate `'regions'` present (ValueError expected-vs-actual if not).
      - Validate `'region_colors'` present AND `len(region_colors) == len(regions)` per V2 — raise ValueError with expected-vs-actual.
      - Validate `'A_prior_mean'` present.
      - Build `matrices` dict. ORDER OF INSERTION for deterministic dict:
        - `matrices['A'] = {'label': 'A — Intrinsic', 'color': '#374151', 'vals': _to_list_of_list(model_cfg['A_prior_mean'])}`.
        - For key in `sorted(model_cfg.get('B_matrices', {}).keys())`: build sub-dict with `label`/`color`/`modulator`/`modulator_display` from `model_cfg.get('B_modulators', {}).get(key, {})` — OMIT None-valued fields from the emitted dict — plus `vals: _to_list_of_list(model_cfg['B_matrices'][key])`.
        - If `'C_matrix' in model_cfg` and C is non-empty: `matrices['C'] = {'label': 'C — Driving', 'color': '#6B7280', 'vals': _to_list_of_list(model_cfg['C_matrix']), 'inputs': list(model_cfg.get('C_inputs', ['u']))}`.
      - `mat_order = ['A'] + sorted(B_keys) + (['C'] if 'C' in matrices else [])` (V7).
      - `peb_out = {}`; if `'peb_covariates' in model_cfg`: `peb_out['covariates'] = list(model_cfg['peb_covariates'])`; if `peb` kwarg supplied: `peb_out.update(peb)` (explicit kwarg wins).
      - Return `CircuitVizConfig(schema='dcm_circuit_explorer/v1', status='planned', meta=dict(model_cfg.get('meta', {})), palette=dict(palette or {}), regions=list(model_cfg['regions']), region_colors=list(model_cfg['region_colors']), matrices=matrices, mat_order=mat_order, phenotypes=list(phenotypes or []), hypotheses=list(hypotheses or []), drugs=list(drugs or []), peb=peb_out, fitted_params=None)`.
      - NumPy-style docstring with Parameters / Returns / Raises / Examples sections. Cite V1/V2/V7/V8.

   b. `from_posterior(planned, posterior_means)` — verbatim handoff `docs/HANDOFF_viz.md:193-212` PLUS `_validate_no_nan(posterior_means)` called BEFORE `copy.deepcopy` (V6). NumPy-style docstring.

   c. `load(path) -> CircuitVizConfig` — verbatim handoff `docs/HANDOFF_viz.md:214-232` PLUS extras collection: after populating the 13 first-class fields via `data.get(...)`, set `cfg.extras = {k: v for k, v in data.items() if k not in _FIRST_CLASS_KEYS}` (V1). NumPy-style docstring.

7. Module-level helper `flatten_posterior_for_viz(posterior, mat_order, b_masks=None) -> dict[str, list[list[float]]]` (V4):
   - For each key in `mat_order`:
     - `'A'` -> `posterior['A']['mean'].detach().cpu().tolist()` if `'A'` in posterior; else compute from `A_free` via `from pyro_dcm.forward_models.neural_state import parameterize_A; parameterize_A(posterior['A_free']['mean']).detach().cpu().tolist()`.
     - `'C'` -> `posterior['C']['mean'].detach().cpu().tolist()`.
     - `'B{j}'` pattern (regex or startswith): try `posterior['B']['mean'][j].detach().cpu().tolist()` if `'B'` key present; else `b_masks` required and use `from pyro_dcm.forward_models.neural_state import parameterize_B; parameterize_B(posterior[f'B_free_{j}']['mean'].unsqueeze(0), b_masks[j].unsqueeze(0)).squeeze(0).detach().cpu().tolist()`.
   - Return the flat dict. Raises ValueError if a requested key cannot be flattened (e.g., B key without `'B'` site AND without `b_masks`). Docstring documents this as the RECOMMENDED (not required) flatten path.

8. All functions/methods have NumPy-style docstrings. Cite `docs/HANDOFF_viz.md:<line-range>` or `[REF-001]`-style where relevant. Line length ≤ 88. Zero wildcard imports; absolute imports only.

Do NOT add a `save` method or a `to_html` method — HANDOFF_viz.md doesn't define them. The handoff's `export(path)` method IS the save path (named `export` not `save` per verbatim handoff; the RESEARCH doc occasionally calls it `save` loosely but the signature is `export`). PLAN success criterion 3 from ROADMAP.md says "`CircuitViz.load(viz.save(path))` reads back equal"; implement this round-trip via the `export` method — the test references `CircuitViz.load(cfg.export(path))` not `viz.save(path)`.
  </action>
  <verify>
```bash
python -c "from pyro_dcm.utils.circuit_viz import CircuitViz, CircuitVizConfig, flatten_posterior_for_viz; cfg = CircuitVizConfig(); d = cfg.to_dict(); assert set(d.keys()) == {'_schema', '_status', 'meta', 'palette', 'regions', 'region_colors', 'matrices', 'mat_order', 'phenotypes', 'hypotheses', 'drugs', 'peb', 'fitted_params'}; print('OK')"
python -c "from pyro_dcm.utils.circuit_viz import CircuitViz; c = CircuitViz.load('configs/heart2adapt_dcm_config.json'); print('extras keys:', sorted(c.extras.keys()))"
ruff check src/pyro_dcm/utils/circuit_viz.py
mypy src/pyro_dcm/utils/circuit_viz.py
```
  </verify>
  <done>
  Import works. `CircuitVizConfig().to_dict()` emits exactly the 13 first-class keys. `CircuitViz.load('configs/heart2adapt_dcm_config.json')` succeeds and `cfg.extras` contains at least `_study`, `_description`, `node_info`, `node_positions`, `svg_edges`, `b_overlays` (the keys present in the reference JSON but not in the first-class set). Ruff + mypy clean on the new file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire re-exports and write 12 tests (A-01..A-10 + B-01/B-02)</name>
  <files>src/pyro_dcm/utils/__init__.py,tests/test_circuit_viz.py</files>
  <action>
Part 2a — `src/pyro_dcm/utils/__init__.py` edit (additive):

Add to the existing imports block (after the `ode_integrator` block, before `__all__`):

```python
from pyro_dcm.utils.circuit_viz import (
    CircuitViz,
    CircuitVizConfig,
    flatten_posterior_for_viz,
)
```

Extend `__all__` (keep existing entries intact; append at the end with a `# Phase 17` comment marker matching the `# Phase 14` precedent on line 14):

```python
    "CircuitViz",                   # Phase 17
    "CircuitVizConfig",             # Phase 17
    "flatten_posterior_for_viz",    # Phase 17
```

Part 2b — `tests/test_circuit_viz.py` (NEW):

Module header: `"""Phase 17 acceptance tests for CircuitViz serializer."""` + `from __future__ import annotations`. Standard imports: `json`, `math`, `copy`, `pathlib.Path`, `pytest`, `torch`. Target imports: `from pyro_dcm.utils.circuit_viz import CircuitViz, CircuitVizConfig, flatten_posterior_for_viz`.

Shared fixture `minimal_3region_bilinear_cfg()` returning:
```python
{
    "regions": ["R0", "R1", "R2"],
    "region_colors": ["#111111", "#222222", "#333333"],
    "A_prior_mean": [[-0.5, 0, 0], [0, -0.5, 0], [0, 0, -0.5]],
    "B_matrices": {"B1": [[0, 0, 0], [0.4, 0, 0], [0, 0, 0]]},
    "C_matrix": [[0.5], [0], [0]],
    "C_inputs": ["u_drive"],
    "B_modulators": {"B1": {"label": "B1 — mod", "color": "#888888",
                             "modulator": "u_mod", "modulator_display": "u(t)"}},
    "meta": {"title": "Test DCM", "subtitle": "3-region bilinear",
             "tags": ["3-region", "SVI"]},
    "peb_covariates": [{"name": "beta_x", "display": "β_x", "desc": "test cov"}],
}
```

Implement exactly 12 test functions (+ shared fixtures), matching the must_haves `must_contain` list:

1. **`test_roundtrip_heart2adapt`** (A-01): `cfg = CircuitViz.load('configs/heart2adapt_dcm_config.json'); roundtrip = json.loads(json.dumps(cfg.to_dict())); original = json.loads(Path('configs/heart2adapt_dcm_config.json').read_text()); assert roundtrip == original`. Skip with `pytest.skip` if the config file is missing (defensive — it's committed, but don't hard-fail a dev branch without it).

2. **`test_from_posterior_flips_status`** (A-02): `planned = CircuitViz.from_model_config(minimal_cfg)`, assert `.status == 'planned'` and `.fitted_params is None`; build dummy posterior `{'A': ..., 'B1': ..., 'C': ...}`; `fitted = CircuitViz.from_posterior(planned, posterior)`; assert `.status == 'fitted'`, `.fitted_params is not None`, `sorted(fitted.fitted_params.keys()) == ['A', 'B1', 'C']`.

3. **`test_from_posterior_no_mutation`** (A-03): `planned_before = copy.deepcopy(planned); fitted = CircuitViz.from_posterior(planned, posterior); assert planned.status == planned_before.status and planned.fitted_params == planned_before.fitted_params`.

4. **`test_to_dict_top_level_keys`** (A-04): assert `set(CircuitViz.from_model_config(minimal_cfg).to_dict().keys()) == {13 expected keys}`. (If `extras` is populated, to_dict may have MORE keys — for a `from_model_config`-built config, extras is empty so this equality holds. Document the assertion explicitly as "no extras contributed by from_model_config".)

5. **`test_mat_order_deterministic`** (A-05): construct minimal_cfg variant with `B_matrices={'B3': ..., 'B1': ..., 'B2': ...}` (shuffled insertion order) and C present; assert `cfg.mat_order == ['A', 'B1', 'B2', 'B3', 'C']`.

6. **`test_schema_version`** (A-06): assert `CircuitViz.from_model_config(minimal_cfg).to_dict()['_schema'] == 'dcm_circuit_explorer/v1'`.

7. **`test_vals_are_list_of_list`** (A-07): for each matrix key, assert `isinstance(m['vals'], list)` and each row is `list` and each cell is `int` or `float`.

8. **`test_tensor_input_accepted`** (A-08): pass `torch.tensor([[-0.5, 0.3], [0.1, -0.5]], dtype=torch.float64)` as `A_prior_mean` (with regions=['R0','R1'], matching colors, skip B/C); assert `cfg.matrices['A']['vals'] == [[-0.5, 0.3], [0.1, -0.5]]` exactly.

9. **`test_export_roundtrip`** (A-09) — uses `tmp_path`: `out = cfg.export(tmp_path / 'test.json'); roundtripped = CircuitViz.load(out); assert roundtripped.to_dict() == cfg.to_dict()`.

10. **`test_empty_optional_collections`** (A-10): build a truly minimal cfg with NO phenotypes/hypotheses/drugs/peb kwargs; assert `d['phenotypes'] == []`, `d['hypotheses'] == []`, `d['drugs'] == []`, `d['peb'] == {}`.

**Additional regression tests** (complementing A-series, kept flat — not a separate class):

- `test_region_colors_missing_raises`: missing `region_colors` -> `pytest.raises(ValueError, match='region_colors')`.
- `test_region_colors_length_mismatch_raises`: `len(region_colors) != len(regions)` -> `pytest.raises(ValueError, match='length')`.
- `test_nan_in_posterior_raises`: `from_posterior(planned, {'A': [[float('nan'), 0], [0, 0]]})` -> `pytest.raises(ValueError, match='NaN')`.
- `test_inf_in_posterior_raises`: `from_posterior(planned, {'A': [[float('inf'), 0], [0, 0]]})` -> `pytest.raises(ValueError, match='Inf')`.
- `test_extras_roundtrip_preserved`: build a `CircuitVizConfig(extras={'_study': 'TEST', 'node_info': {'n': 3}})`, `d = cfg.to_dict(); assert d['_study'] == 'TEST' and d['node_info'] == {'n': 3}`.

11-12. **`class TestPyroIntegration`** marked `@pytest.mark.slow` (2 methods):
   - **`test_smoke_planned_to_fitted`** (B-01): build 3-region bilinear simulation via `simulate_task_dcm` (or inline if too heavy; use the smallest shape that exercises the serializer), construct `task_dcm_model` with `b_masks=[torch.eye(3).zero_().fill_diagonal_(0) + ...minimal-mask]` and a trivial 2-breakpoint `stim_mod`; run `run_svi` for **50 steps** (B-01 is a smoke, not a recovery test); call `extract_posterior_params(guide, model_args, num_samples=50)`; use `flatten_posterior_for_viz(posterior, cfg.mat_order, b_masks=b_masks)` to produce the flat dict; pass through `CircuitViz.from_posterior(planned, flat)`; assert `fitted.to_dict()['_status'] == 'fitted'` and `fitted.to_dict()['fitted_params']` has keys matching `mat_order`. Seed via `pyro.set_rng_seed(42)` + `torch.manual_seed(42)` for determinism.
   - **`test_extract_posterior_shapes_match_serializer`** (B-02): shape-documentation test — assert `posterior['A']['mean'].dim() == 2` and `.tolist()` produces `list[list[float]]` of shape `(3, 3)`. This is a pure shape contract assertion; run SVI for ONLY 5 steps to minimize runtime.

All @slow tests must be skipped by default (no `--runslow`). The fast A-series tests MUST run in under 10 seconds total.
  </action>
  <verify>
```bash
# Fast tests pass (A-series + regression):
pytest tests/test_circuit_viz.py -v -m "not slow"

# Slow integration tests pass (B-01, B-02) — only when explicitly requested:
pytest tests/test_circuit_viz.py -v -m slow

# Lint + type-check:
ruff check tests/test_circuit_viz.py src/pyro_dcm/utils/__init__.py
mypy src/pyro_dcm/utils/__init__.py

# Re-export contract:
python -c "from pyro_dcm.utils import CircuitViz, CircuitVizConfig, flatten_posterior_for_viz; print('OK')"
```
  </verify>
  <done>
  All A-series (A-01..A-10) tests pass in <10s. The 5 additional regression tests pass. Both B-class integration tests pass when run with `-m slow`. `pyro_dcm.utils` package-level import of `CircuitViz`/`CircuitVizConfig`/`flatten_posterior_for_viz` succeeds (confirms `__init__.py` re-export wiring). Ruff + mypy clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append VIZ-01..10 requirements to REQUIREMENTS.md + update Traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit `.planning/REQUIREMENTS.md`. THIS IS AN APPEND-ONLY EDIT — do NOT modify existing BILIN/SIM/MODEL/RECOV entries or their status markers.

**Location 1:** Under the `### v0.4.0 Candidates` section (currently at line ~70-75), REPLACE the single-line `- **CIRCUIT-01..N**: HEART2ADAPT 4-node circuit benchmark (study-specific).` with a structural reorganization that keeps `CIRCUIT-01..N` (HEART2ADAPT benchmark) but also introduces the new `VIZ-*` Phase 17 block.

Actually — simpler and safer: do NOT touch the v0.4.0 Candidates subsection. Instead, ADD a new section after the `## v0.3.0 Requirements` block (before the `## Future Requirements (deferred)` header), titled `## v0.4.0 Requirements` (mirroring the v0.3.0 structure). This mirrors the "each milestone has its own requirements block" convention inferrable from the current structure.

Exact append block to insert BEFORE the line `## Future Requirements (deferred)`:

```markdown
## v0.4.0 Requirements

Requirements for Circuit Explorer (v0.4.0). Each maps to a phase in the v0.4.0 milestone.

### Circuit Visualization (v0.4.0)

- [ ] **VIZ-01**: `CircuitVizConfig` dataclass exists at `src/pyro_dcm/utils/circuit_viz.py` with the 13 handoff-spec fields (`schema`, `status`, `meta`, `palette`, `regions`, `region_colors`, `matrices`, `mat_order`, `phenotypes`, `hypotheses`, `drugs`, `peb`, `fitted_params`) PLUS a pass-through `extras: dict` field (V1 decision) and `to_dict()` / `export()` methods.
- [ ] **VIZ-02**: `CircuitViz.from_model_config(model_cfg, *, phenotypes=None, hypotheses=None, drugs=None, peb=None, palette=None)` produces a `CircuitVizConfig` with `status='planned'` and `fitted_params=None`. Requires `regions`, `region_colors` (length-matched to regions — V2 decision; raises `ValueError` otherwise), and `A_prior_mean` in `model_cfg`.
- [ ] **VIZ-03**: `CircuitViz.from_posterior(planned, posterior_means)` returns a NEW `CircuitVizConfig` (deepcopy; does not mutate `planned`) with `status='fitted'` and `fitted_params=posterior_means`. Validates `posterior_means` contains no NaN/Inf BEFORE deepcopy (V6 decision; raises `ValueError` with offending `(key, i, j)` tuple).
- [ ] **VIZ-04**: `CircuitViz.load(path)` reads `dcm_circuit_explorer/v1` JSON and reconstructs a `CircuitVizConfig`. All top-level JSON keys NOT in the 13-key first-class set are preserved in `cfg.extras` (V1; enables round-trip of `_study`, `_description`, `node_info`, `node_positions`, `svg_edges`, `b_overlays`).
- [ ] **VIZ-05**: `mat_order` is deterministic: `['A'] + sorted(B_matrices.keys()) + (['C'] if C_matrix present and non-empty else [])` (V7 decision). Never relies on caller dict insertion order.
- [ ] **VIZ-06**: `from_model_config` accepts `torch.Tensor`, `numpy.ndarray`, and `list[list[float]]` as matrix values via `_to_list_of_list` helper (V8). All emitted `matrices[key]['vals']` are `list[list[float]]` (never tensor, never ndarray).
- [ ] **VIZ-07**: Module-level helper `flatten_posterior_for_viz(posterior, mat_order, b_masks=None)` is exported (V4). Consumes `extract_posterior_params(...)` output and returns the `dict[str, list[list[float]]]` payload `from_posterior` expects. Handles A / C / B_j flattening with `B_free_{j}` + `parameterize_B` fallback when the `B` deterministic site is absent.
- [ ] **VIZ-08**: Round-trip equality: `CircuitViz.load(cfg.export(path)).to_dict() == cfg.to_dict()` for any `cfg` produced by `from_model_config` or `from_posterior`, including `configs/heart2adapt_dcm_config.json` via `extras` pass-through.
- [ ] **VIZ-09**: `pyro_dcm.utils` package re-exports `CircuitViz`, `CircuitVizConfig`, and `flatten_posterior_for_viz` (consistent with the existing `ode_integrator` re-export precedent in `src/pyro_dcm/utils/__init__.py`).
- [ ] **VIZ-10**: Zero upstream edits — no changes to `task_dcm_model`, `extract_posterior_params`, `parameterize_A`, `parameterize_B`, `create_guide`, `run_svi`, or any file outside `src/pyro_dcm/utils/`, `tests/`, and `.planning/`. Verified by `git diff --name-only main...HEAD` matching the allowed path set.
```

**Location 2:** Update the `## Traceability` table. APPEND (do not renumber) after the existing `RECOV-08 | Phase 16 | Pending` row:

```markdown
| VIZ-01 | Phase 17 | Pending |
| VIZ-02 | Phase 17 | Pending |
| VIZ-03 | Phase 17 | Pending |
| VIZ-04 | Phase 17 | Pending |
| VIZ-05 | Phase 17 | Pending |
| VIZ-06 | Phase 17 | Pending |
| VIZ-07 | Phase 17 | Pending |
| VIZ-08 | Phase 17 | Pending |
| VIZ-09 | Phase 17 | Pending |
| VIZ-10 | Phase 17 | Pending |
```

**Location 3:** Update the `**Coverage:**` block (lines ~129-132). APPEND three lines (do NOT modify the existing v0.3.0 coverage numbers):

```markdown
- v0.4.0 requirements: 10 total
- Mapped to phases: 10/10 (all mapped)
- Unmapped: 0
```

**Location 4:** Update the `**Per-phase distribution:**` block (lines ~134-138). APPEND:

```markdown
- Phase 17 (Circuit Visualization Module): 10 requirements (VIZ-01..10)
```

**Location 5:** Update the footer `*Last updated:*` line:

OLD: `*Last updated: 2026-04-18 after Phase 15 verification passed (MODEL-01..07 Complete; 19/27 v0.3.0 reqs done; Phase 16 RECOV-01..08 pending)*`

NEW: `*Last updated: 2026-04-24 — v0.4.0 milestone opened; VIZ-01..10 appended for Phase 17 (Circuit Visualization Module). v0.3.0 Phase 16 RECOV-01..08 still pending cluster re-run.*`

Do not edit the file title, D1-D5 table, v0.3.0 Requirements body, or the Out of Scope table. Preserve the blank-line separator between sections.
  </action>
  <verify>
```bash
# Requirements grep — VIZ-01..10 present with correct format:
grep -n "VIZ-01\|VIZ-02\|VIZ-03\|VIZ-04\|VIZ-05\|VIZ-06\|VIZ-07\|VIZ-08\|VIZ-09\|VIZ-10" .planning/REQUIREMENTS.md

# Traceability table has 10 new rows:
grep -c "Phase 17" .planning/REQUIREMENTS.md   # expect 11: 10 traceability + 1 "Per-phase distribution"

# v0.3.0 entries untouched:
grep -c "Phase 16 | Pending" .planning/REQUIREMENTS.md   # expect 8 (RECOV-01..08)

# Section header present:
grep "## v0.4.0 Requirements" .planning/REQUIREMENTS.md
```
  </verify>
  <done>
  `grep VIZ-` returns 10 checkbox lines + 10 traceability lines + 1 per-phase-distribution line. v0.3.0 BILIN/SIM/MODEL/RECOV entries and their status markers are byte-identical to pre-edit. `*Last updated:*` footer line is replaced. No duplicate `## v0.4.0 Requirements` headers.
  </done>
</task>

</tasks>

<verification>

## Phase-level verification

Runs after all 3 tasks complete.

1. **Module import + re-export contract:**
   ```bash
   python -c "from pyro_dcm.utils import CircuitViz, CircuitVizConfig, flatten_posterior_for_viz; print('OK')"
   python -c "from pyro_dcm.utils.circuit_viz import CircuitViz, CircuitVizConfig, flatten_posterior_for_viz; print('OK')"
   ```

2. **A-series acceptance tests (the core gate):**
   ```bash
   pytest tests/test_circuit_viz.py -v -m "not slow"
   # Expect: 10 A-tests pass + 5 regression tests pass (15 total fast tests), <10s
   ```

3. **B-series integration smoke:**
   ```bash
   pytest tests/test_circuit_viz.py -v -m slow
   # Expect: 2 slow tests pass (B-01, B-02), ~30-90s each
   ```

4. **HEART2ADAPT round-trip (A-01, the hardest assertion):**
   ```bash
   python -c "
   import json
   from pathlib import Path
   from pyro_dcm.utils import CircuitViz
   cfg = CircuitViz.load('configs/heart2adapt_dcm_config.json')
   rt = json.loads(json.dumps(cfg.to_dict()))
   original = json.loads(Path('configs/heart2adapt_dcm_config.json').read_text())
   assert rt == original, f'Round-trip failed: {set(original) - set(rt)} missing, {set(rt) - set(original)} added'
   print('HEART2ADAPT round-trip OK; extras keys:', sorted(cfg.extras.keys()))
   "
   ```

5. **Zero upstream edits (VIZ-10 gate):**
   ```bash
   git diff --name-only main...HEAD | grep -v -E '^(src/pyro_dcm/utils/|tests/|\.planning/)' | head -5
   # Expect: EMPTY output. Any line other than these paths is a VIZ-10 violation.
   ```

6. **Full regression suite (confirm no unintended breakage):**
   ```bash
   pytest tests/ -v -m "not slow" --tb=short
   # Expect: all existing tests still pass. Phase 17 adds tests; it does not touch any upstream code.
   ```

7. **Ruff + mypy hygiene:**
   ```bash
   ruff check src/pyro_dcm/utils/circuit_viz.py src/pyro_dcm/utils/__init__.py tests/test_circuit_viz.py
   mypy src/pyro_dcm/utils/circuit_viz.py
   # Expect: clean (0 errors) on all three files.
   ```

</verification>

<success_criteria>

All five provisional success criteria from ROADMAP.md Phase 17 section are satisfied:

1. ✅ `CircuitViz.from_model_config(...).to_dict()` returns a dict with `_status == 'planned'` and `fitted_params is None`. (Task 1 implementation + Task 2 tests A-02/A-04/A-06/A-10 verify.)

2. ✅ `CircuitViz.from_posterior(planned, flatten_posterior_for_viz(extract_posterior_params(...), planned.mat_order, b_masks))` produces a dict with `_status == 'fitted'` and populated `fitted_params`. (Task 1 helper + Task 2 test B-01 verify end-to-end.)

3. ✅ Round-trip: `CircuitViz.load(cfg.export(path)).to_dict() == cfg.to_dict()` passes on the reference 3-region bilinear fixture AND on `configs/heart2adapt_dcm_config.json`. (Task 2 tests A-09 + A-01 verify.)

4. ✅ Schema tolerance: bare 3-region bilinear with empty `phenotypes/hypotheses/drugs` produces valid JSON; all optional fields have renderer-safe fallbacks per 17-RESEARCH.md "Renderer behavior on missing fields" table. Structural assertion only — no headless browser per V5. (Task 2 tests A-07/A-10 verify.)

5. ✅ Zero upstream edits — `git diff` shows changes ONLY in `src/pyro_dcm/utils/circuit_viz.py`, `src/pyro_dcm/utils/__init__.py`, `tests/test_circuit_viz.py`, and `.planning/REQUIREMENTS.md`. (Phase-level verification step 5 enforces this.)

Plus the 10 VIZ-NN requirements (VIZ-01..10) are all checkable against the artifact file list and are `Pending` in the Traceability table until `/gsd:verify-phase 17` flips them to `Complete`.

</success_criteria>

<output>
After completion, create `.planning/phases/17-circuit-visualization-module/17-01-SUMMARY.md` describing:
- Three artifacts delivered (circuit_viz.py, test_circuit_viz.py, __init__.py re-export edit)
- REQUIREMENTS.md append (VIZ-01..10 + traceability + coverage)
- Test results: A-series count passing, B-series count passing, total LOC
- Any deviations from the locked decisions V1-V8 (should be none; flag if any arose)
- Known deferrals logged for future milestones (JSON-schema codification per V3; SVG-fidelity browser tests per V5; auto-flatten primary path per V4)
</output>
