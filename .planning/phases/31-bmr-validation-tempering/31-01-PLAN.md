---
phase: 31-bmr-validation-tempering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_bmr_vlbmr01_recovery.py
  - benchmarks/bmr_recovery.py
autonomous: true
gap_closure: false

must_haves:
  truths:
    - "A real VL fit on synthetic sparse ground truth produces a full A_free posterior covariance that rank_connections consumes"
    - "The top-K essential off-diagonal edges by single-prune cost equal the true present edges (set equality)"
    - "A positive separation_gap is reported and the cut (separation_after_rank) lands at K"
    - "Recovery holds across >=5 seeds for spectral N=2 and N=4; fraction recovering true structure is reported"
  artifacts:
    - path: "benchmarks/bmr_recovery.py"
      provides: "Reusable helpers: sparse ground-truth A builder + VL-result -> (A_free mean, full A_free cov, prior tensors, offdiag/true-edge index sets) extractor"
      min_lines: 60
    - path: "tests/test_bmr_vlbmr01_recovery.py"
      provides: "@pytest.mark.vl end-to-end VLBMR-01 recovery test (N=2 and N=4, >=5 seeds)"
      min_lines: 80
  key_links:
    - from: "tests/test_bmr_vlbmr01_recovery.py"
      to: "result.sigma_post[:N*N, :N*N]"
      via: "full A_free covariance slice (A_free packed first)"
      pattern: "sigma_post\\[: *N *\\* *N"
    - from: "tests/test_bmr_vlbmr01_recovery.py"
      to: "rank_connections"
      via: "rank off-diagonal prunable indices, assert top-K == true edges"
      pattern: "rank_connections\\("
---

<objective>
VLBMR-01 (the PRIMARY, defensible result): prove that BMR relative-evidence
ranking on a real VL posterior recovers the true circuit structure. VL-fit a
SPARSE-ground-truth spectral circuit, slice the full A_free posterior
covariance, rank the off-diagonal connections by single-prune cost via
`rank_connections`, and assert the top-K essential edges == the true present
edges with a positive separation gap reported.

Purpose: Establishes the headline model-comparison claim for v0.7.0 — relative
ranking + separation gap is the defensible BMR result (absolute ΔF is broken by
Laplace overconfidence; do NOT re-litigate). This is the foundation 31-03 builds
on.

Output: `benchmarks/bmr_recovery.py` (reusable fit→tensors helper) and
`tests/test_bmr_vlbmr01_recovery.py` (the `@pytest.mark.vl` recovery test).
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/31-bmr-validation-tempering/31-RESEARCH.md

# The functions to call (read signatures directly)
@src/pyro_dcm/model_selection/bmr.py

# The proven fit-plumbing reference (mirror sigma_post slicing + OFFDIAG)
@cluster/scripts/lc_vl_bmr_selection.py

# The spectral VL fit pattern (run_variational_laplace_generic + prior_variance=1/64)
@benchmarks/runners/spectral_vl.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sparse ground-truth + VL-result helpers in benchmarks/bmr_recovery.py</name>
  <files>benchmarks/bmr_recovery.py</files>
  <action>
Create `benchmarks/bmr_recovery.py` with `from __future__ import annotations`,
NumPy-style docstrings, absolute imports, line length 88, ruff/mypy clean.

Provide three functions:

1. `make_sparse_ground_truth_A(n_regions, present_edges, *, self_connection=-0.5,
   strength=0.15) -> torch.Tensor`
   - Build an (N,N) float64 A: diagonal = `self_connection`; for each
     `(i, j)` in `present_edges` (a list of off-diagonal `(row, col)` pairs),
     set `A[i, j] = strength`. All other off-diagonals stay 0.
   - This makes "true present edges" unambiguous by construction (research
     §VLBMR-01: prefer constructed sparse truth over reading off a dense A —
     guards Pitfall R2). Assert no `(i, j)` has `i == j` (diagonal is structural,
     never an edge) with an error message naming the offending pair.
   - Assert the resulting A is stable (`max real eig < 0`) so the spectral
     simulator's stability assertion passes; if not, raise ValueError naming the
     max real eigenvalue (expected `< 0`, actual value).

2. `offdiag_indices(n_regions) -> list[int]`
   - Return `[i*N + j for i in range(N) for j in range(N) if i != j]`
     (C-order flat index `i*N + j`; guards Pitfall S4). Mirror
     `lc_vl_bmr_selection.py:64`.

3. `bmr_tensors_from_vl_result(result, n_regions, *, prior_variance) ->
   tuple[torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor]`
   - Return `(posterior_mean, posterior_cov, prior_mean, prior_cov)` ready for
     `rank_connections`:
     - `posterior_mean = result.theta_post["A_free"].reshape(-1).double()`  (N*N,)
     - `posterior_cov  = result.sigma_post[: N*N, : N*N].double()`          (N*N,N*N)
       (A_free is ALWAYS the first packed block — research §"where the full
       A_free covariance lives"; mirror `lc_vl_bmr_selection.py:94-95`).
     - `prior_mean = torch.zeros(N*N, dtype=torch.float64)`
     - `prior_cov  = prior_variance * torch.eye(N*N, dtype=torch.float64)`
   - Add a one-line round-trip assertion inside the helper that `i*N + j` for a
     sample `(i, j)` maps back to `(i, j)` via `divmod(idx, N)` (guards S4).

Do NOT add a `rank_connections` call here — keep this file pure plumbing the
test (and later 31-03) reuse.
  </action>
  <verify>
`python -c "import sys; sys.path.insert(0,'src'); from benchmarks.bmr_recovery import make_sparse_ground_truth_A, offdiag_indices; import torch; A=make_sparse_ground_truth_A(4, [(1,0),(2,1),(3,2)]); print(A.shape, offdiag_indices(4))"`
prints `torch.Size([4, 4])` and the 12 off-diagonal indices.
`ruff check benchmarks/bmr_recovery.py` clean.
  </verify>
  <done>
`benchmarks/bmr_recovery.py` exists with the three helpers; sparse A builder
rejects diagonal edges and unstable A; `bmr_tensors_from_vl_result` slices
`sigma_post[:N*N,:N*N]`; ruff/mypy clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: VLBMR-01 end-to-end recovery test (spectral N=2/N=4, >=5 seeds)</name>
  <files>tests/test_bmr_vlbmr01_recovery.py</files>
  <action>
Create `tests/test_bmr_vlbmr01_recovery.py` — a `@pytest.mark.vl` test module.
`from __future__ import annotations`, absolute imports, NumPy docstrings,
ruff/mypy clean, error messages with expected vs actual.

Module docstring: state this is VLBMR-01, the PRIMARY defensible result —
RELATIVE ranking + separation gap, NEVER absolute ΔF (cite Pitfall C1 / cluster
job 55772525). Reference `rank_connections`.

Implement a parametrized test
`test_vlbmr01_relative_ranking_recovers_structure` over
`(n_regions, present_edges)` cases:
  - N=2: present edge `[(1, 0)]` (one true edge, K=1).
  - N=4: present chain `[(1, 0), (2, 1), (3, 2)]` (K=3) — matches the
    latent-circuit chain convention so results are comparable.
Use `SEEDS = list(range(42, 47))` (5 seeds — research §tolerances allows >=5 for
a binary structure-recovery assertion; each spectral fit is ~1.4–7.5s).

Per seed:
  1. `torch.manual_seed(seed)`, `np.random.seed(seed)`.
  2. `A_true = make_sparse_ground_truth_A(N, present_edges)` (from
     `benchmarks.bmr_recovery`).
  3. `sim = simulate_spectral_dcm(A_true, TR=2.0, n_freqs=32, seed=seed)`;
     `csd_obs = sim["csd"].to(torch.complex128)`, `freqs = sim["freqs"].double()`.
  4. VL fit (mirror `spectral_vl.py:104-114`):
     `forward = SpectralDCMForward()`;
     `result = run_variational_laplace_generic(forward, observed=csd_obs,
        a_mask=torch.ones(N,N,dtype=torch.float64), n_regions=N, max_iter=64,
        prior_variance=1.0/64.0, context={"freqs": freqs})`.
  5. `pm, pc, prm, prc = bmr_tensors_from_vl_result(result, N,
        prior_variance=1.0/64.0)`.
  6. `prunable = offdiag_indices(N)`;
     `ranked = rank_connections(pm, pc, prm, prc, prunable)`.
  7. `true_idx = {i*N + j for (i, j) in present_edges}`; `K = len(true_idx)`.
     `topk = {e["index"] for e in ranked["ranked"][:K]}`.
  8. Record `recovered = topk == true_idx` and `ranked["separation_gap"]`.

Assertions (gate):
  - For each seed where recovered, also assert
    `ranked["separation_gap"] > 0` and
    `ranked["separation_after_rank"] == K` (cut lands between essential and
    non-essential) — message includes expected K vs actual
    `separation_after_rank`.
  - Aggregate: assert `sum(recovered) == len(SEEDS)` (all seeds recover at the
    best, coverage=1.0 substrate). If a seed fails, the message lists that
    seed's `topk` vs `true_idx` (a real finding worth surfacing — research
    §tolerances).
  - Do NOT assert any absolute-nat threshold on `separation_gap` (Pitfall C1).
    Print the per-seed `separation_gap` and the
    `n_recovered/len(SEEDS)` fraction for the report.

Add `pytest.importorskip` only if needed; otherwise rely on the `vl` marker.
  </action>
  <verify>
`pytest tests/test_bmr_vlbmr01_recovery.py -m vl -q` passes (both N=2 and N=4
parametrizations green) in well under 3 min on laptop. Output prints per-seed
separation_gap and the recovered fraction.
`ruff check tests/test_bmr_vlbmr01_recovery.py` clean.
  </verify>
  <done>
The `@pytest.mark.vl` test passes for spectral N=2 (K=1) and N=4 (K=3) over 5
seeds: top-K essential off-diagonal edges == true present edges (set equality),
`separation_gap > 0`, `separation_after_rank == K`, recovered fraction reported.
No absolute-ΔF gate anywhere.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_bmr_vlbmr01_recovery.py -m vl -q` green, runtime < 3 min on laptop.
- The test uses `result.sigma_post[:N*N,:N*N]` for the FULL A_free covariance
  (NOT the diagonal-only `extract_vl_posterior_generic` std).
- Prunable indices are off-diagonal only (diagonal never pruned); index mapping
  is C-order `i*N + j` (S4 guard present, round-trip asserted).
- No assertion gates on absolute ΔF or a separation-gap magnitude (C1 guard).
- `ruff check` and `mypy` clean on both new files.
</verification>

<success_criteria>
VLBMR-01 satisfied: BMR relative-evidence ranking on a real VL posterior recovers
the true circuit structure (top-K essential edges == true present edges) for
spectral N=2 and N=4 across >=5 seeds, with a positive separation gap reported
and the cut landing at K — the primary, defensible model-comparison result.
</success_criteria>

<output>
After completion, create `.planning/phases/31-bmr-validation-tempering/31-01-SUMMARY.md`.
Commit per task: `feat(31-01): {task}` (no Co-Authored-By; stage files individually).
</output>
