---
phase: 27-publication-artifacts
plan: 03
type: execute
wave: 2
depends_on: ["27-01", "27-02"]
files_modified:
  - docs/03_methods_reference/equations.md
autonomous: false

must_haves:
  truths:
    - "equations.md quick-reference table covers all v0.6.0 modules and their equations"
    - "All publication artifacts are consistent: REFERENCES.md IDs match methods.md citations, figure script function names match methods section references"
    - "User has reviewed the pipeline schematic figure and methods section for scientific accuracy"
  artifacts:
    - path: "docs/03_methods_reference/equations.md"
      provides: "Complete equation quick-reference including v0.6.0 modules"
      contains: "latent_circuit_dcm_model"
  key_links:
    - from: "docs/03_methods_reference/equations.md"
      to: ".planning/REFERENCES.md"
      via: "REF-XXX citation IDs in table"
      pattern: "REF-0[7-9][0-9]"
---

<objective>
Update the equations quick-reference with v0.6.0 modules, perform cross-
consistency checks between REFERENCES.md, methods.md/tex, and figure
scripts, and get user verification that the methods narrative and pipeline
schematic are scientifically accurate.

Purpose: Ensures all publication artifacts are internally consistent and
scientifically reviewed before the paper is assembled.

Output: Updated equations.md + verified consistency.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REFERENCES.md
@docs/03_methods_reference/equations.md
@docs/03_methods_reference/methods.md
@docs/03_methods_reference/methods.tex
@scripts/generate_publication_figures.py
@.planning/phases/27-publication-artifacts/27-01-SUMMARY.md
@.planning/phases/27-publication-artifacts/27-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update equations.md and cross-check consistency</name>
  <files>docs/03_methods_reference/equations.md</files>
  <action>
1. Update equations.md to add v0.6.0 equation entries. The file currently
   has tables for Task-Based DCM, Spectral DCM, and Regression DCM. Add
   new sections:

   **Bilinear Extension** (add to Task-Based DCM section):

   | Name | Formula | Reference | Implementation |
   |------|---------|-----------|----------------|
   | Bilinear state equation | dx/dt = Ax + Sum_j u_j B^(j) x + Cu | [REF-001] Eq. 1 | `neural_state.py` |
   | Effective connectivity | A_eff(t) = A + Sum_j u_j(t) B^(j) | [REF-001] | `neural_state.py` |
   | B parameterization | B_j = mask_j * B_free_j, diag=0 | SPM12 convention | `task_dcm_model.py` |

   **Direct Observation Model** (new section):

   | Name | Formula | Reference | Implementation |
   |------|---------|-----------|----------------|
   | Direct observation | y(t) = C_obs x(t) + epsilon | Phase 20 | `latent_circuit_dcm_model.py` |
   | LC A prior | A_free ~ N(0, 1/16) | Phase 20-03 decision | `latent_circuit_dcm_model.py` |
   | LC noise model | epsilon ~ N(0, 1/noise_prec) | -- | `latent_circuit_dcm_model.py` |

   **CT-RNN** (new section):

   | Name | Formula | Reference | Implementation |
   |------|---------|-----------|----------------|
   | CT-RNN dynamics | tau dh/dt = -h + f(W_rec h + W_in u + b) | [REF-080] | `continuous_time_rnn.py` |
   | Euler discretization | h_{t+1} = h_t + (dt/tau)(-h_t + f(...)) | [REF-080] | `continuous_time_rnn.py` |
   | Output R-squared gate | R2 >= 0.90 on PCA-projected readout | Phase 21 | `latent_extraction.py` |

   **Bayesian Model Reduction** (new section):

   | Name | Formula | Reference | Implementation |
   |------|---------|-----------|----------------|
   | BMR log evidence | ln p(y|m_r) = ln p(y|m_f) + correction | [REF-070] Eq. 1-5 | TBD (Phase 23) |

2. Perform cross-consistency checks:

   a) For every REF-XXX in methods.md, verify it exists in REFERENCES.md.
      Report any missing entries.
   b) For every \cite{} in methods.tex, note the expected Zotero key.
      Report placeholder keys that need user attention.
   c) Verify that figure function names in generate_publication_figures.py
      correspond to the results described in the methods sections.

   Print a consistency report as a comment block at the end of the task
   output, listing:
   - All REF-XXX IDs used in methods.md
   - All citation keys used in methods.tex
   - Any mismatches found
   - Any references in methods that lack REFERENCES.md entries
  </action>
  <verify>
- equations.md has sections for Bilinear Extension, Direct Observation
  Model, CT-RNN, and Bayesian Model Reduction
- grep for "latent_circuit_dcm_model" in equations.md returns results
- grep for "continuous_time_rnn" in equations.md returns results
- Cross-consistency report shows no unresolved mismatches (placeholder
  keys are acceptable and flagged)
  </verify>
  <done>
equations.md contains all v0.6.0 equation entries. Cross-consistency check
passes: all REF-XXX IDs in methods.md exist in REFERENCES.md, all LaTeX
citation keys are documented, and any placeholder keys are flagged for
user attention after Zotero update.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete set of v0.6.0 publication artifacts:
1. REFERENCES.md with all new reference entries (REF-070 through REF-110)
2. Extended methods.md and methods.tex covering direct observation model,
   bilinear DCM, CT-RNN training, DCM interpretability pipeline, BMR,
   SBI, and hybrid VAE-DCM
3. Updated equations.md quick-reference
4. Pipeline schematic figure in figures/ (PNG + PDF)
5. Figure generation script for all v0.6.0 figures
  </what-built>
  <how-to-verify>
1. Open `figures/pipeline_schematic.png` and verify:
   - Pipeline stages are clearly labeled and ordered correctly
   - Color coding distinguishes data processing, model training, and
     DCM inference stages
   - Arrows show correct flow direction
   - Annotations are readable and scientifically accurate

2. Review `docs/03_methods_reference/methods.md` new sections:
   - Section 1.6 (bilinear) correctly describes dx/dt = Ax + Sum_j u_j B_j x + Cu
   - Section 1.7 (direct observation) correctly describes y = C_obs x + epsilon
   - Section 6 (DCM interpretability) correctly describes the pipeline
   - Sections 7-9 (BMR, SBI, VAE-DCM) are technically accurate

3. Verify REFERENCES.md entries are correct (DOIs, author names, years)

4. Confirm which references need to be added to Zotero:
   - REF-080 (Langdon & Engel 2025)
   - REF-100 (TRIBE)
   - Any other pending entries
  </how-to-verify>
  <resume-signal>
Type "approved" if the publication artifacts are scientifically accurate.
If issues found, describe specific corrections needed (e.g., "equation X
is wrong", "reference Y has wrong DOI", "pipeline schematic missing
stage Z").
  </resume-signal>
</task>

</tasks>

<verification>
1. equations.md updated with v0.6.0 content
2. Cross-consistency check between REFERENCES.md, methods.md, methods.tex,
   and figure script completed
3. No orphan REF-XXX IDs (all cited references exist in REFERENCES.md)
4. User has verified pipeline schematic and methods narrative
</verification>

<success_criteria>
- equations.md has 4+ new equation sections for v0.6.0 modules
- Cross-consistency check reveals zero unresolved mismatches
- User approves the pipeline schematic figure and methods section
  as scientifically accurate
- All placeholder citation keys are documented for Zotero update
</success_criteria>

<output>
After completion, create `.planning/phases/27-publication-artifacts/27-03-SUMMARY.md`
</output>
