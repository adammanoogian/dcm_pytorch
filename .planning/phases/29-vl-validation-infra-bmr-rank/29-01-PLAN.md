---
phase: 29-vl-validation-infra-bmr-rank
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmarks/config.py
  - config.py
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "Constructing BenchmarkConfig with no VL args succeeds and all four VL fields default to None"
    - "Every existing benchmark caller and test passes unchanged (zero behavior change)"
    - "config.MATLAB_PATH resolves to the local MATLAB binary and is overridable via MATLAB_PATH env var"
    - "pytest -m vl selects only vl-marked tests; pytest -m 'not vl' deselects them without an unknown-marker warning"
  artifacts:
    - path: "benchmarks/config.py"
      provides: "BenchmarkConfig with optional VL fields (max_iter, hyperprior_mean, hyperprior_precision, prior_mean_a_offset)"
      contains: "hyperprior_mean"
    - path: "config.py"
      provides: "MATLAB_PATH constant centralized at project root"
      contains: "MATLAB_PATH"
    - path: "pyproject.toml"
      provides: "vl pytest marker registration"
      contains: "vl:"
  key_links:
    - from: "benchmarks/config.py"
      to: "RUNNER_REGISTRY method=vl routing"
      via: "optional None-default dataclass fields consumed by VL runners"
      pattern: "max_iter|hyperprior_mean|hyperprior_precision|prior_mean_a_offset"
---

<objective>
Lay the configuration foundation for all v0.7.0 VL validation work: add the
optional VL fields to `BenchmarkConfig`, centralize `MATLAB_PATH` in the
project-root `config.py`, and register the `vl` pytest marker. This is the
dependency root that the VL runners (Plan 29-04) and the SPM12 cross-validation
in Phase 32 consume.

Purpose: VL runners need extra config (`max_iter`, SPM12 prior-matching params)
that SVI runners do not. Adding them as None-default optional fields keeps every
existing benchmark caller and test working with zero behavior change (VLINFRA-01).
`MATLAB_PATH` must move out of the hardcoded literal in `validation/run_validation.py`
so Phase 32's `run_vl_validation.py` can read it from one place (VLINFRA-05).
Output: Extended `BenchmarkConfig`, a `MATLAB_PATH` constant, and a `vl` marker.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Files this plan edits (read current state before editing)
@benchmarks/config.py
@config.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional VL fields to BenchmarkConfig</name>
  <files>benchmarks/config.py</files>
  <action>
Add four optional fields to the `BenchmarkConfig` dataclass (after the existing
`fixtures_dir: str | None = None` field, preserving field order so positional
construction is unaffected):

    max_iter: int | None = None
    hyperprior_mean: float | None = None
    hyperprior_precision: float | None = None
    prior_mean_a_offset: float | None = None

All four MUST default to `None` so existing callers (every `BenchmarkConfig(...)`,
`quick_config`, `full_config`, and all current tests) are byte-for-byte unaffected.
Do NOT touch `quick_config` / `full_config` — they forward `**kwargs`, so the new
fields are reachable but never required.

Extend the class docstring `Parameters` block with NumPy-style entries documenting
each new field and WHY it exists:
- `max_iter`: VL Gauss-Newton iteration cap (passed to
  `run_variational_laplace_generic(..., max_iter=...)`); `None` lets the runner use
  the engine default of 128.
- `hyperprior_mean`: SPM12 spectral-DCM hyperprior mean `hE` (8.0); `None` uses the
  engine's data-dependent default. Used only for SPM12-matched runs.
- `hyperprior_precision`: SPM12 hyperprior precision `ihC` (128.0); `None` uses
  engine default.
- `prior_mean_a_offset`: SPM12 A prior-mean offset `a_mask / 128`; `None` means a
  zero offset. Used only for SPM12-matched runs (Phase 32).

Add a one-line docstring note under the new fields citing the v0.7.0 dt/T
constraint so it is discoverable here (Pitfall N1): "Task DCM VL requires dt >= 0.1
to keep the (T*N, T*N) precision matrix tractable; see VLROBUST-02."

Keep `from __future__ import annotations` and the 88-char line limit. Run ruff.
  </action>
  <verify>
`python -c "from benchmarks.config import BenchmarkConfig; c=BenchmarkConfig(variant='spectral', method='vl'); assert c.max_iter is None and c.hyperprior_mean is None and c.hyperprior_precision is None and c.prior_mean_a_offset is None; print('ok')"`
prints `ok`. `ruff check benchmarks/config.py` and `ruff format --check benchmarks/config.py` clean.
`pytest tests/ -k config -q` (any existing config tests) pass unchanged.
  </verify>
  <done>
BenchmarkConfig has four new None-default VL fields, documented; constructing it
with only `variant`/`method` leaves all four as None; no existing caller breaks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Centralize MATLAB_PATH in config.py</name>
  <files>config.py</files>
  <action>
Add a `MATLAB_PATH` constant to the project-root `config.py`, following the exact
pattern of the existing `TAPAS_RDCM_PATH` (env-var overridable, NumPy-style docstring
entry in the module `Constants` block).

Add after the `TAPAS_RDCM_PATH` block:

    MATLAB_PATH: Path = Path(
        os.environ.get(
            "MATLAB_PATH",
            "C:/Program Files/MATLAB/R2022a/bin/matlab",
        )
    )

The default literal MUST match the value currently hardcoded in
`validation/run_validation.py` (verify by reading that file first; the architecture
research recorded it as `C:/Program Files/MATLAB/R2022a/bin/matlab`). Add a
`MATLAB_PATH` entry to the module docstring `Constants` section explaining it is the
MATLAB binary for the SPM12 cross-validation bridge (Phase 32), overridable via the
`MATLAB_PATH` env var.

Do NOT edit `validation/run_validation.py` in this plan — the consuming refactor is
Phase 32 work. This plan only establishes the single source of truth. Run ruff.
  </action>
  <verify>
`python -c "import config; print(config.MATLAB_PATH)"` prints a path ending in
`matlab` (or the env override). `ruff check config.py` clean.
  </verify>
  <done>
`config.MATLAB_PATH` exists, defaults to the R2022a binary path, is env-overridable,
and is documented; `validation/run_validation.py` is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register the vl pytest marker</name>
  <files>pyproject.toml</files>
  <action>
In `pyproject.toml`, add a `vl` marker to the existing `[tool.pytest.ini_options]`
`markers = [...]` list (currently contains `slow`, `spm`, `mne`). Match the existing
entry style exactly:

    "vl: marks tests as Variational Laplace validation (run with '-m vl', skip with '-m \"not vl\"')",

Place it alongside the others (after `spm` is fine). Do NOT modify the `[project]`
dependencies or the `mne` optional-dependency group — the milestone requires NO new
dependencies. Run ruff/toml validity is implicit; verify by listing markers.
  </action>
  <verify>
`pytest --markers | grep "@pytest.mark.vl"` shows the registered marker.
`pytest -m vl --co -q` runs without an "unknown marker" warning (collects zero or
more tests, no error).
  </verify>
  <done>
The `vl` marker is registered; `pytest -m vl` and `pytest -m "not vl"` both run
without unknown-marker warnings.
  </done>
</task>

</tasks>

<verification>
- `python -c "from benchmarks.config import BenchmarkConfig; import config; print(config.MATLAB_PATH)"` succeeds.
- `pytest --markers | grep vl` shows the marker.
- `pytest tests/ -q -m "not vl and not mne and not spm and not slow"` shows no new
  failures vs the pre-change baseline (config additions are purely additive).
- `ruff check benchmarks/config.py config.py` clean.
</verification>

<success_criteria>
VLINFRA-01: BenchmarkConfig has optional `max_iter`, `hyperprior_mean`,
`hyperprior_precision`, `prior_mean_a_offset` fields with None defaults; existing
callers unchanged.
VLINFRA-05: `vl` pytest marker registered in pyproject.toml; `MATLAB_PATH`
centralized in config.py.
</success_criteria>

<output>
After completion, create `.planning/phases/29-vl-validation-infra-bmr-rank/29-01-SUMMARY.md`.
</output>
